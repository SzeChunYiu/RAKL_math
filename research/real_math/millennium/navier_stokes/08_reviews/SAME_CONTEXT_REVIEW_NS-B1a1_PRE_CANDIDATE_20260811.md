# NS-B1a1 same-context pre-candidate expert review

**Authority:** `SAME_CONTEXT_REVIEW_ONLY / PRE_CANDIDATE / ROOT_AUTHORITY_NONE`

**Frozen target.** For a non-trivial mild bounded ancient whole-space 3D Navier–Stokes solution with finite Albritton–Barker Type-I quantity `I = sup_Q(A+C+D+E)`, test whether the standard local-energy inequality provides a finite multiscale currency across dynamically realized dyadic scales. No smallest physical scale, global `L^3`, fixed/DSS orbit, or far-field decay may be assumed.

## Delegated cell

1. **Type-I / ancient-solution PDE analyst.** Bind the exact blow-up object to Albritton–Barker: Type-I singularity ↔ non-trivial mild bounded ancient solution with finite `I`. Require the rescaling `v_k(x,t)=r_k v(x_k+r_k x,t_k+r_k^2 t)`, `q_k(x,t)=r_k^2 q(x_k+r_k x,t_k+r_k^2 t)`, uniform critical local bounds on every compact cylinder, strong `L^3_loc` velocity convergence, weak `L^(3/2)_loc` pressure convergence, and persistence of non-triviality/singularity. Local compactness alone does not provide far-field tightness.

2. **Local-energy / pressure analyst.** Expand the cutoff inequality term by term. After division by the natural kinetic scale `R`, the derivative/Laplacian term is `O(A)`, the cubic term `O(C)`, and the pressure-work term `O(D^(2/3) C^(1/3))` after subtracting the spatial pressure mean. None has a positive power of `R`. The harmonic/far-field pressure normalization must not be silently absorbed into a global pressure premise.

3. **Scaling / concentration-compactness analyst.** Reject the inference `uniform O(1) per scale => finite total scale budget`. A normalized annulus at scale `R` changes its weight when viewed at `R/2`; disjoint physical shells do not by themselves yield a summable critical quantity. Moving centers, translations and dilation escape remain hostile controls.

4. **Backward-uniqueness / rigidity analyst.** Backward uniqueness is downstream only. The exact missing rigidity interface is either (a) a source-valid finite scale potential/increment theorem that forces termination/regularity, or (b) a theorem producing a global `L^3` backward sequence so Albritton–Barker Theorem 1.2 applies. Finite `I` alone does not supply terminal vorticity data, exterior vanishing, or global far-field decay.

5. **Geometric-depletion / adversarial analyst.** Treat DSS/self-similar recurrence as a structural stress test, not as a blow-up example. Any claimed shell charge must defeat core recycling: the same nested critical core may contribute at every scale while annular increments are small. Vorticity alignment or geometric depletion is relevant only if it is propagated into a scale-integrated/global estimate.

6. **Formal-assurance analyst.** Keep the discriminator below theorem-candidate authority. Require exact statement, context hash, method-transfer disanalogies, dual memory review, and hash-linked trace before running it. A negative result must be scoped to absolute shell summation, not all local-energy, pressure, or temporal routes.

7. **Novelty / source-boundary analyst.** Current primary-source anchors support the ingredients but not the desired terminal theorem: Albritton–Barker for Type-I/ancient equivalence and compactness; Bradshaw–Tsai for local-energy/DSS hostile structure; Lei–Yang–Yuan for a separate backward-uniqueness mechanism. No novelty or nonexistence-in-literature claim is authorized.

## Consensus before discriminator

`RUN_NEGATIVE_FIRST_DISCRIMINATOR / NO_CANDIDATE / NO_GLOBAL_L3_PREMISE / NO_TERMINAL_SCALE_PREMISE / ROOT_AUTHORITY_NONE`.

The highest-information first test is exact homogeneity of the absolute local-energy contributions. If all are only scale-critical after normalization, the residual is not “find a sharper constant”; it is “produce a signed/telescoping/no-recrossing or independently summable increment with an anti-reuse theorem.”
