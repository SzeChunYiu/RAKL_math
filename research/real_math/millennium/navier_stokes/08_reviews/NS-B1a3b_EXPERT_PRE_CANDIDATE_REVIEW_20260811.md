# NS-B1a3b same-context expert cell — pre-candidate review

Authority: `SAME_CONTEXT_INTERNAL_REVIEW / SEARCH_CONTROL_ONLY / NOT_INDEPENDENT_REVIEW`

Frozen context: `sha256:ed24b30e6c8b6e6682584c516ca1f2e2e0b30ecc1fca2f738d808c176e7d59e4`  
Fibre snapshot: `sha256:633eff45bbd8592b80bcb0b260030be2dd609c1472f4e974d432fec21583ebe1`

## Cell and delegation

1. **Type-I / ancient-solution PDE specialist** — guards the Albritton–Barker hypothesis ledger and distinguishes arbitrary divergence-free test fields from actual ancient Navier–Stokes solutions. Decision: a functional hostile family may prune only an estimate-from-`I` route; it cannot refute a dynamics-specific coherence theorem.
2. **Vorticity-geometry specialist** — binds the Giga–Miura high-vorticity uniform-continuity criterion and the geometric meaning of `xi=omega/|omega|`. Decision: any direction test must stay inside a region where vorticity is nonzero and quantitatively bounded away from zero.
3. **Harmonic-analysis / Lorentz–BMO specialist** — audits the recent Grujic `arXiv:2607.08866v2` coordinate. Decision: test the exact weighted mean-oscillation scale `phi(r)=1/|log r|`; do not conflate ordinary BMO, VMO, uniform continuity, and weighted BMO.
4. **Scaling / adversarial-construction specialist** — is delegated the first hostile world. Proposed architecture only: localize a rotating vector potential so that `u=curl A` is smooth, compactly supported and divergence-free; choose amplitude so first-derivative energy stays uniformly bounded while the vorticity direction rotates on frequency `N`.
5. **Pressure / nonlocality and gluing specialist** — guards scope. Decision: setting the auxiliary pressure coordinate to zero is permitted only for a functional `A+C+D+E` calibration, not for a Navier–Stokes trajectory; any positive PDE theorem must restore pressure/evolution compatibility.
6. **Formal assurance / RAKL v3 specialist** — verifies current-main semantics, stale-pin boundary, context-before-candidate chronology, episode/diagnosis separation and hash chaining. Decision: all new evidence remains proposal/shadow and current-main v3 use does not imply current-main dependency execution.
7. **Novelty / source-boundary specialist** — prevents theorem and novelty inflation. Decision: a successful hostile family would be a scoped representation/transfer obstruction at most; the current 2026 preprint is a source calibration, not independently validated theorem authority.

## Discussion and consensus

The cell compared the two live children from merged `NS-B1a3-C001`. Continuing `NS-B1a3a` would remain inside the already dense compactness/tail/profile-leakage failure family. `NS-B1a3b` is orthogonal and is explicitly live in the current DAG, so the search rotates to vorticity geometry.

The strongest cheap falsifier is **not** to prove or disprove a geometric regularity theorem. It is to ask whether finite `I` itself controls the required direction coherence. If a uniformly finite-`I` smooth divergence-free family can have arbitrarily bad uniform-continuity modulus and weighted-BMO norm on a nonzero-vorticity core, then every proof of geometric inheritance must use genuine Navier–Stokes dynamics beyond the retained finite-`I` ledger.

The cell also records a source disanalogy before computation: Grujic `2607.08866v2` assumes a narrower critical-point concentration structure (`omega=Phi |x|^{-2}` with associated superlevel localization), so it cannot be applied to every finite-`I` ancient limit without a separate inheritance theorem even if the direction norm were available.

## Authorized next action

Run only the registered functional hostile calibration, with exact scaling/units and weighted mean-oscillation checks. If it fails to separate the coordinates, rotate again. If it separates them, record a scoped failure of the **finite-I-only geometric-inheritance shortcut**, keep dynamics-specific vorticity depletion live, and do not promote root or theorem authority.
