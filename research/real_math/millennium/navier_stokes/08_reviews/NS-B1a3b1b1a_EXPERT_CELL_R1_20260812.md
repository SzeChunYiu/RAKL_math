# NS-B1a3b1b1a same-context expert cell — pre-source-proof review

Authority: proposal/shadow same-context role separation only; independent mathematical review credit = 0/3.

## PDE regularity / singular-set analyst
Delegation: bind Barker's exact terminal singular-set theorem and identify what regularity is quantitative away from the terminal exceptional set.
Pre-action view: finite terminal singular-set cardinality is promising for the center-count coordinate but does not by itself encode a vorticity level-to-radius modulus. Reject any transfer that silently replaces a fixed spatial neighborhood by `R_lambda = O(lambda^-1/2)`.

## Harmonic-analysis / geometric-depletion analyst
Delegation: type-check the target consumer coordinate against Grujić v2.
Pre-action view: the consumer needs a shrinking morphology scale tied to vorticity level. A finite number of centers is only one coordinate; the radius law and the separate global weak-`L^{3/2}` vorticity/log-BMO direction hypotheses remain explicit.

## Blow-up / compactness analyst
Delegation: audit terminal-versus-preterminal quantifiers, moving centers, dilation/translation leakage, and whether source conclusions survive the intended blow-up state space.
Pre-action view: terminal singular points can localize where regularity fails at `t=0`, but the target concerns high-vorticity super-level sets on slices approaching the terminal time. Require a same-theory quantitative bridge, not topological convergence.

## Pressure / local-energy analyst
Delegation: identify which source steps use pressure/local energy and whether any inferred vorticity morphology depends on nonlocal pressure control.
Pre-action view: pressure can be essential on the source side even though the target vorticity consumer is written without explicit pressure. Keep this as a typed producer interface; do not erase it during transfer.

## Adversarial falsification analyst
Delegation: run the cheapest old-failure regression first.
Pre-action falsifier: search for an estimate of the form `dist(A_lambda(t), S_*) <= C lambda^-1/2`. If the proof provides only regularity outside every fixed `delta`-neighborhood of a finite set, classify the missing modulus rather than interpolating one from cardinality.

## Formal assurance / provenance analyst
Delegation: bind exact source version, atom/context/memory hashes, pre-action chronology, proposal/shadow TaskEpisode authority, and root nonpromotion.
Pre-action view: issue #180 freezes the discriminator before source-proof inspection for this atom. Any source absence is a scoped transfer result, not a theorem of impossibility. Same-context checks do not count toward the required three isolated reviews.

## Research-value / metrology analyst
Delegation: judge whether the cycle changes routing and record seven-axis semantic retention without counting repository growth.
Pre-action view: this is high-information because a positive transfer would connect an equation-specific finite-center theorem to a geometric consumer, while a negative transfer precisely separates center count from critical radius. Retain only genuinely new relation/obstruction/path semantics.

## Joint decision
Proceed with a source-bound theorem/proof audit of Barker arXiv:2111.14776. Use the `lambda -> radius` exponent as the decisive discriminator. Keep Grujić finite-center consumer relaxation as a separate downstream question and do not retry the saturated fixed-history amplitude family.

Content hash: `sha256:609c030026dc6eef7d49f7f3deaf0a2515a5eacd6f5dc57b7e397cd77f35717f`.
