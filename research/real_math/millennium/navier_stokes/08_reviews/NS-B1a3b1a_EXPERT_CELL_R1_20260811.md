# NS-B1a3b1a same-context expert cell — R1

Authority: `SAME_CONTEXT_ADVERSARIAL_REVIEW / INDEPENDENT_REVIEW_CREDIT 0/3`.

The cell is deliberately multidisciplinary but shares one model/context and therefore earns no independent root-review credit.

## PDE / partial-regularity specialist

**Background:** suitable weak solutions, local energy inequality, pressure decompositions, ancient-solution compactness, backward uniqueness.

**Finding:** the scale envelopes for the hostile family correctly demonstrate a numerical `A/C/D/E` near-miss. The non-solution disclaimer is load-bearing: the example cannot be used to refute an implication that exploits the Navier-Stokes equation. Strong local velocity compactness cannot be differentiated for free, so the existing derivative-loss failure remains active. Pressure localization and backward-uniqueness hypotheses are downstream and cannot repair a missing producer certificate.

## Harmonic-analysis / critical-spaces specialist

**Background:** Lorentz spaces, BMO/Campanato norms, scaling-critical embeddings, concentration and profile decompositions.

**Finding:** the time burst of width `N^{-2}` exactly balances the `N` spatial derivative in the parabolic spacetime `E` functional, while a peak-time weak-`L^{3/2}` vorticity norm grows like `N`. The sign-changing direction field has order-one mean oscillation on radius `~N^{-1}` balls, so the `1/|log r|` BMO weight yields growth like `log N`. This rules out a bounded map from the **bare functional signature** to the consumer norms.

## Geometric-vorticity specialist

**Background:** vorticity direction coherence, vortex stretching depletion, high-vorticity geometry, zero-set normalization.

**Finding:** the oscillatory direction defect is genuine on balls crossing nodal planes. Values assigned on the zero-vorticity planes do not change the mean-oscillation lower bound because the planes have measure zero. This is not evidence that an actual finite-`I` Navier-Stokes blow-up has the same geometry; it is a hostile representation test only.

## Adversarial source / verification specialist

**Background:** exact theorem-signature binding, provenance, counterexample design, transfer/gluing audits.

**Finding:** the producer and consumer signatures are not silently equated. Albritton–Barker's finite-`I` theorem lives on suitable weak NSE solutions; Grujić v2 consumes stronger global time-slice amplitude/phase information. The new result must therefore be recorded as `BARE_FUNCTIONAL_PRODUCER_EMBEDDING_REFUTED`, not `FINITE_I_NSE_PRODUCER_REFUTED`. Local producer failure and global/far-field gluing failure remain separate.

## Cell synthesis

The candidate survives all four same-context attacks at its scoped statement. The residual is sharpened to an equation-specific upgrade problem. No root obligation closes, no Type-I exclusion follows, Type II is untouched, and independent mathematical review remains `0/3`.
