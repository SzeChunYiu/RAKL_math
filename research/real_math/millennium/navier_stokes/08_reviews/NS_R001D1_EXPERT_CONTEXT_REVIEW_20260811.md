# NS-R001d1 same-context expert review — heat baseline versus nonlinear depletion

**Authority:** role-separated same-context research review only. These roles are not independent mathematical reviewers.

The cell reviewed the exact Clay contract, merged `NS-R001/A1` and `NS-R001b` negative history, current framework `RAKL@15f1c3affe5bf85ba41ff0ab65b25ba19e0d28a3`, primary sources in the accompanying packet, and pending PR #33 only as provisional evidence.

## 1. PDE regularity / critical-spaces lead

**Background:** 3D Navier–Stokes regularity, Serrin/endpoint criteria, epsilon regularity, mild solutions.

**Finding:** the active gap is not another conditional criterion. The research object must be an estimate generated from arbitrary smooth finite-energy data that reaches a scale-critical coordinate. Positive-time heat smoothing must be kept separate from a genuinely nonlinear gain.

**Delegation:** own the exact scaling ledger and reject any candidate whose constants hide a supercritical dependence or whose conclusion is weaker than an established conditional criterion.

**Strongest objection:** an estimate that grows like the heat `L^2 -> L^3` baseline as the restart interval shrinks may not provide a uniform critical bound near a hypothetical singular time.

**Vote:** `ACCEPT_PRE_CANDIDATE_REPRESENTATION`.

## 2. Harmonic analysis / semigroup lead

**Background:** heat-kernel estimates, Leray projection, bilinear mild formulation, endpoint convolution.

**Finding:** the first cheap discriminator should audit the exact time singularity produced by a crude `L_x^2 -> L_x^3` gradient heat bound and the energy-class `L_t^2L_x^6` control. This is more informative than immediately inventing a new PDE inequality.

**Delegation:** freeze all spatial exponents, time-kernel exponents, viscosity powers and endpoint integrability before any candidate; plant a near-diagonal temporal-concentration hostile control.

**Strongest objection:** failure of the unsigned scalar convolution does not show the exact Duhamel term is large; Leray cancellation, frequency geometry or tent-space structure may repair it.

**Vote:** `ACCEPT_ENDPOINT_AUDIT / BLOCK_OVERGENERALIZATION`.

## 3. Strain–vorticity / nonlinear-mechanism lead

**Background:** strain formulation, vorticity stretching, projected strain space and nonlinear depletion.

**Finding:** Miller's projected remainder is a useful nonlinear coordinate precisely because it can separate diffusion from the projected nonlinear interaction. But the criterion is conditional, and static PR #33 evidence is not yet accepted.

**Delegation:** after the baseline audit, decide whether the residual should be expressed in velocity critical spaces or directly in the projected strain remainder. Require a witnessed reason before changing representation.

**Strongest objection:** a velocity-space endpoint failure may be irrelevant to a cancellation that is visible only after strain projection.

**Vote:** `ACCEPT_AS_FIRST_DISCRIMINATOR_ONLY`.

## 4. Adversarial scaling / temporal-concentration lead

**Background:** critical scaling, concentration counterexamples, hostile functional-analysis controls.

**Finding:** parent A1 already killed static energy-only interpolation. The new DifferenceWitness is genuine because the atom restricts to actual evolution, but the old failure should be repeated in time form: do not assume an `L^2_t` dissipation function has enough endpoint equiintegrability to control a singular Duhamel kernel.

**Delegation:** prepare the cheapest scalar near-diagonal pulse control after machine-gate passage; verify that it attacks only the chosen proof architecture.

**Strongest objection:** arbitrary scalar concentration need not be dynamically realizable by an actual Navier–Stokes solution.

**Vote:** `ACCEPT_HOSTILE_CONTROL / DIAGNOSIS_MUST_REMAIN_SCOPED`.

## 5. Formal assurance / proof-obligation lead

**Background:** RAKL statement binding, chronology, hash-chain verification, root authority boundaries.

**Finding:** candidate generation must remain blocked until this new context, dual-memory review, expert review and trace pass current `plan_math_research`. Pending PR #33 cannot be imported into accepted memory while its exact-head CI is unresolved.

**Delegation:** verify canonical hashes, exact trace order, context/memory bindings, no `CANDIDATE_PROPOSED` event, current framework pin and root issue binding.

**Strongest objection:** the migrated `success_contract.yaml` still points to old RAKL issue `#88`; application control should be corrected to `RAKL_math#4` without altering the Clay success criteria.

**Vote:** `ACCEPT_PACKET / REQUIRE_MACHINE_GATE`.

## 6. Novelty / research-value lead

**Background:** source comparison, rediscovery risk, value-of-information and method selection.

**Finding:** heat-kernel and Duhamel endpoint facts are classical territory. The value of this cycle is not a novelty claim; it is a disciplined route discriminator that can prevent repeated “parabolic smoothing implies regularity” arguments from consuming search budget.

**Delegation:** keep all later authority at `SOURCE_BOUND_ROUTE_PRUNING` unless a genuinely new proved estimate appears; if the scalar route fails, reopen function-space representation rather than relabeling the classical endpoint obstruction as new mathematics.

**Strongest objection:** recent finite-energy frequency-local results show that the correct bridge may use a more refined decomposition than a global `L^3` bootstrap.

**Vote:** `ACCEPT_SEARCH_CONTROL_VALUE / NO_NOVELTY`.

## Cell synthesis and delegation

All six roles agree on the following order:

1. freeze `NS-R001d1` around **heat baseline versus nonlinear excess**;
2. carry only the two merged failures into dual memory, each with a DifferenceWitness;
3. keep PR #33 provisional until exact-head assurance resolves;
4. run the RAKL context/memory/trace machine gate;
5. only then freeze the exact semigroup-energy endpoint candidate and hostile temporal-concentration falsifier;
6. if that proof architecture fails, classify the failure narrowly and rotate to cancellation-aware/tent-space/frequency-local/local-energy/projected-strain coordinates.

The cell explicitly rejects: immediate theorem invention, assuming local `L^3` smallness, treating heat smoothing as nonlinear depletion, importing pending evidence as canonical memory, and interpreting a scalar endpoint failure as a Navier–Stokes impossibility theorem.
