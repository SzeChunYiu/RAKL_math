# NS-B1a1 same-context expert review — scale-charge pre-candidate gate

**Atom:** `NS-B1a1`  
**Authority:** `SAME_CONTEXT_REVIEW / PROPOSAL_ONLY / NOT_INDEPENDENT_REVIEW / ROOT_AUTHORITY_NONE`  
**RAKL current main inspected:** `a521d577724dfedb3123e22cdbac457bce4e22f7`  
**RAKL_math base:** `49edbefcb3bd4bab24b154ac509ed933e8d817dc`  
**Frozen context:** `sha256:2dd3659dd73036559e41fddd35b599fc1945933730d99ad2dc60d98ba28b67ce`  
**Frozen memory review:** `sha256:5331f53e7f396aa43912f76867cde26b0f71256a06005b8ae9229a2ee2cf8a74`

## Shared exact question

For a mild bounded ancient 3D Navier–Stokes solution with finite Albritton–Barker Type-I quantity `I=sup(A+C+D+E)`, does the **standard local-energy ledger itself** provide a dimensionless scale-decaying, telescoping, monotone, or otherwise well-founded charge across every dynamically realized dyadic descendant? The first action is a hostile scaling audit, not a theorem proposal.

## Role-separated findings

### PDE / partial-regularity lead
The root remains the exact Clay alternatives. `NS-B1a1` is only a Type-I child. Finite `I` is deliberately weaker than the desired global `L^3` trace. The cheapest legitimate discriminator is whether the source-valid cutoff inequality contains any scale-improving term after normalization. Do not convert repeated epsilon-regularity threshold failure into a fixed physical cost: the threshold is dimensionless while the physical integrals shrink with scale.

### Scaling / harmonic-analysis lead
Under Navier–Stokes scaling, `A,C,D,E` are dimensionless. On `Q(2R)`, finite `I<=M` gives physical sizes: local kinetic energy `O(MR)`, cubic velocity and pressure `3/2` space-time integrals `O(MR^2)`, and dissipation `O(MR)`. Cutoff derivatives contribute `R^-2` or `R^-1`, so the local-energy RHS remains `R` times a bounded dimensionless polynomial. Dividing by the natural energy scale `R` is therefore predicted to give `O(1)`, unless an additional sign, defect, cancellation or smallness mechanism appears.

### Pressure / local-energy lead
Pressure work must use `q-[q]_{B_{2R}}(t)` or another licensed time-dependent normalization. Hölder gives the scale-correct pressure-velocity cutoff term `R^-1 ||q-mean||_{L^(3/2)(Q)} ||v||_{L^3(Q)} = R D^(2/3) C^(1/3)` up to cutoff constants. Earlier failure `F-NS-B1a-C001-PRESSURE-SUMMABILITY` already blocks raw far-field divergence. Any pressure rescue now needs sign/coherence, time integration, harmonic/local decomposition or another non-absolute feature.

### Concentration-compactness lead
Nested dyadic scales can reuse the same concentrating core. Physical expenditures of size `R_j` or `R_j^2` are geometrically summable, so finite physical total is not a well-founded scale rank. A contradiction needs a dimensionless monotone defect, no-recrossing theorem, genuinely disjoint nonsummable expenditure, or compactness plus rigidity.

### Adversarial falsification lead
Use two near-misses. First, repeat the same scale-invariant critical threshold on `R_j=2^-j R_0` and verify that physical lower bounds can still have a finite geometric sum. Second, use forward discretely self-similar suitable solutions only as a structural stress test: classical local-energy inequality can coexist with exact scale repetition in a different direction/setting. This is not a backward Type-I counterexample.

### Formal-methods / authority lead
The candidate may establish only: `finite I + standard cutoff bookkeeping` does not automatically produce a summable/monotone normalized scale charge. It must not be encoded as `no such Navier–Stokes charge exists`. Constants remain universal cutoff-dependent constants and pressure normalization must be explicit. No root/proof/novelty promotion is authorized.

### Novelty / research-value lead
The operation is target-specific reuse of `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` under a DifferenceWitness. If the hostile check closes as preregistered, classify this subproblem as `TRANSFER_NOVEL` (structural rank 0), not operator-novel. Its value is route pruning and localization of a sharper obstruction.

## Consensus next action

Proceed only with `NS-B1a1-C001`: audit the scale of the linear cutoff, convection, pressure-work and dissipation terms under finite `A,C,D,E`; normalize by `R`; test for positive scale decay, sign, no-recrossing or telescoping. Reject naive scale charging if the normalized ledger is merely `O(1)` per dyadic scale.

The preregistered prediction is falsified if the standard estimate alone yields a positive power of `R` in the relevant dimensionless charge, a bounded-below telescoping functional with a nonzero descendant drop, uniform no-recrossing, or a genuinely disjoint nonsummable positive expenditure.

These role passes share the same context and are not independent mathematical review.