# NS-B1a4 same-context expert cell — signed-flux / no-recrossing discriminator

**Authority:** `SAME_CONTEXT_REVIEW_ONLY / PRE_VERIFICATION / NO_INDEPENDENT_REVIEW / ROOT_AUTHORITY_NONE`  
**Framework:** `SzeChunYiu/RAKL@2f722d42db240538e1bdec146aebc7e9d5eeb8a6` (`method_version=3.0.0`)  
**Application base:** `SzeChunYiu/RAKL_math@a7301f0f0e2cab2750ac6e923efe18b5750b5af6`  
**Context packet:** `sha256:4f4201800bdb64d85704ed43c5acb44bb0a3edc4772df2a0477f4ad989c0f83a`  
**Memory review:** `sha256:8c660286fcb35e8f02d9bd1923cbbf3b08cb6ffd2274a363ae2c9f484da2bc6f`

This role-separated review concerns the already prospectively scoped verification target in issue #173. The pressure-free shear idea predates this packet, so this cycle claims no hypothesis-generation chronology credit. The purpose of the frozen packet is to control verification, scope, routing, and failure normalization.

## 1. Type-I / ancient-solution PDE lead

The exact target source class remains the Albritton–Barker finite-`I` Type-I ancient class. A local exact NSE calibration outside that global class can falsify only a proposed inference from the *local unsigned ledger plus the standard local-energy identity*; it cannot refute `global finite I + ancient NSE => P`. Any conclusion must carry this DifferenceWitness.

Recommendation: verify the local sign architecture first; if negative, retain the finite-`I`-specific dynamics question as an open child rather than declaring the entire no-recrossing route dead.

## 2. Local-energy / pressure analyst

For a smooth solution, localized kinetic energy has definite viscous dissipation but also boundary/cutoff diffusion and transport terms; the suitable local-energy inequality packages the same fact. The registered `A,C,D,E` quantities control magnitudes. A sign conclusion needs orientation/correlation information not present in those scalar magnitudes unless the equation supplies an additional identity.

Pressure localization is not enough by itself: if an exact `p=0` solution can have increasing kinetic energy in a fixed ball, pressure cannot be the universal source of one-sidedness.

## 3. Scaling / concentration-compactness analyst

The already registered scale-neutral shell failure means another absolute `O(I)` estimate is unlikely to discriminate recurrence. A viable successor must be dimensionless and signed, or must add trajectory compactness/minimality. Translation/dilation leakage remains a distinct global assembly problem.

Disagreement preserved: a local signed functional might still exist after choosing a dynamically moving region or profile-adapted center. The fixed-ball falsifier would not rule that out.

## 4. Rigidity / backward-uniqueness analyst

No backward-uniqueness theorem is admissible at this stage. A local no-recrossing lemma would still have to produce the exact terminal/exterior/global hypotheses of a named rigidity theorem. The prior `F-NS-B1a3-LOCAL-GLOBAL-INTERFACE-MISMATCH` remains active and separate.

## 5. Adversarial construction analyst

Verify the frozen family
`u(x,t)=(eps exp(k x_2 + k^2 t),0,0)`, `p=0`, `k>0`.

Checks required:
- divergence free;
- nonlinear term zero;
- `partial_t u = Delta u`;
- positive local-energy derivative on every fixed ball;
- exact scope warning: not globally finite energy, not bounded on all of `R^3`, and not an Albritton–Barker finite-`I` ancient target.

If all pass, the result is a local/magnitude-interface route-pruning calibration only.

## 6. Formal assurance / verifier-trust analyst

The verification is analytic; a small deterministic test may recheck the algebra but cannot promote it beyond the written proof. Episode, diagnosis, and obstruction/lesson must be distinct artifacts. All v3 records remain proposal/shadow. Same-context role separation gives no independent-review credit.

The root must remain `OPEN_NO_SOLUTION_CERTIFICATE`.

## 7. Novelty / source-boundary analyst

Bounded current primary-source search found no directly matching theorem that upgrades the Albritton–Barker finite-`I` magnitude ledger into the desired signed no-recrossing law. This is search routing only, not a novelty certificate. Pineau–Vicol's 2026 self-similar/near-self-similar results add symmetry/pointwise hypotheses and are therefore a near-solved structural analogue, not a direct transfer.

## Consensus and preserved disagreement

**Consensus:** run the pressure-free exact-NSE discriminator. If local energy increases with `p=0`, classify the result as `EXISTING_LEDGER_SIGN_INSUFFICIENT_ROUTE_PRUNING`; do not infer failure of a stronger finite-`I` trajectory theorem.

**Preserved disagreement:** a positive route may still use a moving/adaptive region, signed flux-correlation observable, scale-to-scale trajectory variable, almost-periodicity/minimality, or another dynamics-specific coherence coordinate. Those are not tested by the frozen fixed-region calibration.
