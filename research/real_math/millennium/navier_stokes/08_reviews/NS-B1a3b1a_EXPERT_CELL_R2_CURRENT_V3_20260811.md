# NS-B1a3b1a R2 — same-context expert cell

Authority: `SAME_CONTEXT_REVIEW_ONLY / INDEPENDENT_REVIEW_CREDIT 0/3`.

The cell reviewed atom `NS-B1a3b1a` after the R2 current-v3 pre-action receipt was frozen. Every role was given the same exact root contract, context fibre, hostile family, primary-source anchors, prior success/failure memory, and the instruction to falsify before accepting.

## Role 1 — PDE scaling and local-energy auditor

Background: 3D incompressible Navier–Stokes suitable weak solutions, parabolic scaling, Caffarelli–Kohn–Nirenberg local functionals and Type-I blow-up compactness.

Delegation: audit `A/C/D/E` for arbitrary subcylinders, cutoff terms, dimensions, endpoint `r≈N^-1`, and any illicit use of the NSE equation.

Finding: `PASS_SCOPED`. The bounds `A≲r^2`, `C≲r min(r^2,N^-2)`, `D=0`, `E≲N^2 r^2 min(r^2,N^-2)` hold uniformly for all `0<r≤1`; the two radius regimes meet without a hidden logarithm or derivative loss. Cutoff derivatives only change constants. The construction is divergence free but not an NSE solution, so it cannot attack the PDE-specific implication.

## Role 2 — harmonic-analysis/vorticity auditor

Background: Lorentz spaces, Calderón–Zygmund operators, BMO/Campanato spaces, vorticity direction criteria.

Delegation: verify weak-`L^{3/2}` growth, the exact log-BMO weight including the `L∞` term, nodal-plane normalization, and the source consumer signature.

Finding: `PASS_SCOPED`. At peak time a fixed-measure set carries `|omega_N|≥N/2`, giving weak-`L^{3/2}` norm `≳N`. A ball of radius `a/N` centered on a nodal plane has equal positive/negative phases by reflection, hence order-one mean oscillation; multiplying by `1/phi(a/N)=|log(a/N)|` gives `≳log N`. The `L∞` term is 1 and cannot cancel the lower bound. Zero-vorticity conventions outside the support do not affect this lower-bound ball. Grujić v2 additionally assumes the critical-point profile and global-in-space consumer norms; those hypotheses are not silently reduced to the two norms tested here.

## Role 3 — blow-up, pressure and gluing auditor

Background: concentration compactness, ancient-solution blow-up limits, pressure localization, backward uniqueness and local-to-global tightness.

Delegation: separate local representation failure from global tail/far-field and state-space interfaces; test for circular bootstraps.

Finding: `PASS_WITH_RESIDUALS`. The hostile family blocks only a static numerical producer. It says nothing about an NSE-specific smoothing/time-coherence upgrade. Strong local velocity compactness cannot be differentiated into the required vorticity phase. A local producer would still leave global Lorentz/far-field Biot–Savart control open. Grujić's endgame is pre-first-singular-time analyticity/escape-time regularity, not an ancient Liouville theorem. Backward uniqueness is downstream and cannot create missing producer inputs.

## Role 4 — RAKL v3 assurance and metrology auditor

Background: RAKL v3 TaskEpisode/evidence substrate, pre-action binding, protected authority, provenance and application CI.

Delegation: inspect exact current framework subject, distinguish episode→diagnosis→obstruction/lesson, verify proposal-only authority, memory priority-only use, saturation accounting and branch pin/CI.

Finding: `PASS_ON_SCHEMA_AND_SCOPE / CI_SEPARATE`. Current framework main was rebound to the observed `787c7e00...`; the official R2 `PreActionFibreReceipt-v1` uses current `EpisodeOutcome` branch values and rejects the earlier custom R1 receipt for prospective-binding credit. The application had a repeated freshness defect: changing only the submodule gitlink left `config/rakl-framework-pin.json` stale and caused fail-closed CI before pytest. The branch now aligns both subjects; this process failure is linked to `SzeChunYiu/RAKL#128`. No theorem, novelty, gluing, or independent-review authority is minted by the receipt.

## Reconciled cell decision

Retain the R1 mathematical falsifier after R2 revalidation, but narrow its reusable meaning to:

> Critical parabolic `A/C/D/E` bookkeeping has no **bare static time-trace operator** into the selected uniform vorticity amplitude/phase coordinates. Any positive producer must exploit NSE dynamics or change consumer.

Failure category: `REPRESENTATION` locally; `GLUING` remains separately open; `SOURCE/STATE_SPACE` remains separately open; `TOOLING/PROVENANCE` affected the application workflow but not the mathematical calculation.

Root status: `OPEN_NO_SOLUTION_CERTIFICATE`.
Independent mathematical review credit: `0/3`.
