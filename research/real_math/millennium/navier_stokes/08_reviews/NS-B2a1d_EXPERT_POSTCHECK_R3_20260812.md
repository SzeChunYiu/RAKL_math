# NS-B2a1d same-context expert postcheck — Hill F=1 Euler adversary

**Episode:** `TE-NS-B2a1d-R3-20260812`  
**Authority:** role-separated same-context review only; `0/3` isolated mathematical reviews.

## 1. Type-II / suitable-weak Navier–Stokes analyst

**Check.** Seregin Theorem 3.1 produces an ancient **Euler** pair after Euler scaling, with (3.5)-(3.8). The present Hill pair is checked against the displayed Euler-side class only.

**Objection resolved.** The write-up never claims Hill is in the image of the Navier–Stokes extraction. The producer-to-limit ancestry/tail theorem remains open and is recorded separately from the Euler-side rigidity failure.

## 2. Euler vortex-ring analyst

**Check.** Choi's primary-source stream function differentiates to the displayed piecewise velocity. The formulas match continuously at the unit sphere; the physical velocity decays as `O(|x|^-3)` and the core travels at nonzero speed `W=2/15`.

**Objection resolved.** No folklore-only integrability assertion is used. `L2`, `L3` and `H1` follow directly from the explicit decay; the axis is regular after rewriting the radial component in Cartesian coordinates.

## 3. Local-energy / regularity analyst

**Check.** The physical velocity is continuous and piecewise `C1` with bounded one-sided first derivatives, hence locally Lipschitz globally. Distributional Euler is source-bound by the exact traveling solution. The local kinetic-energy product rule is therefore legitimate, giving equality and hence Seregin's inequality (3.7).

**Residual.** This establishes the Euler-side local-energy interface, not any Navier–Stokes local-energy inheritance beyond what Seregin already proved.

## 4. Pressure / far-field analyst

**Check.** `U in L3` implies `U_i U_j in L^(3/2)`. The whole-space normalized pressure `R_i R_j(U_iU_j)` lies in `L^(3/2)` by Calderon–Zygmund boundedness. Translation preserves this norm. Exterior irrotationality gives the consistency check `P=c dot V-|V|^2/2+const` and `O(|x|^-3)` decay.

**Objection resolved.** The proof does not infer global pressure integrability from exterior asymptotics alone and does not leave an uncontrolled time-dependent pressure gauge inside the absolute `L^(3/2)` term.

## 5. Concentration-compactness / noncompact-symmetry analyst

**Check.** At `tau=-A^2`, core displacement is `O(A^2)` while the centered observation radius is `A`. The explicit far-field velocity is then `O(A^-6)` uniformly on `B(A)`, so every finite local `Lp` norm tends to zero for `p>1/2`, in particular `p=3,6`.

**Objection resolved.** The failure mechanism is explicitly noncompact translation/profile leakage; it is not mislabeled as dissipative decay.

## 6. Rigidity / unique-continuation analyst

**Check.** No Navier–Stokes backward-uniqueness or parabolic unique-continuation theorem is used after the limit equation changes to Euler.

**Route implication.** A future rigidity theorem must consume an additional Euler-valid, source-inherited global coordinate that actually excludes this moving-core world.

## 7. Adversarial assurance / RAKL metrology analyst

**Check.** The pre-action receipt prospectively bound the consequential verification, while the earlier noticing of the Hill candidate is explicitly denied prospective hypothesis-generation credit. The episode is proposal/shadow, the candidate lesson is not counted as retained `EXPERIENCE_PATTERN`, and root authority remains `NONE`.

**Falsifier boundary.** The exact failure is scoped to `displayed F=1 Euler-side state + centered backward local smallness => u=0`. It is not broadened to all Euler Liouville methods, all Type-II routes or Seregin's theorem.

## Consensus

`SOURCE_BOUND_EXACT_EULER_SIDE_ADVERSARY / CENTERED_LOCAL_RIGIDITY_PRUNED / PRODUCER_ANCESTRY_TAIL_GAP_REMAINS / ROOT_AUTHORITY_NONE`.
