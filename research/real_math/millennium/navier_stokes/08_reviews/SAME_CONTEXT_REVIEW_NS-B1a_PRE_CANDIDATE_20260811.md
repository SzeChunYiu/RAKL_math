# Same-context expert cell — NS-B1a dynamics-specific Type-I trace/tail bridge

**Authority:** role-separated same-context review only; **not independent mathematical review**.  
**Atom:** `NS-B1a`.  
**Frozen inputs:** child MathContextFiber, NS-B1 implication/counterexample matrix, current-main dual-memory review, source packet.  
**Question:** what is the cheapest high-partition next action before any theorem candidate about finite `I` and global trace/tail control?

## 1. PDE regularity lead

**Background:** suitable weak/mild Navier–Stokes solutions, ancient solutions, Type-I blow-up, critical regularity and Liouville methods.

**Delegation:** verify that the target bridge composes cleanly with Albritton–Barker Theorems 1.1 and 1.2 and does not assume the conclusion.

**Finding:** `I<∞ -> backward L3 sequence` would be a sufficient bridge for the Type-I ancient class, but the source theorem gives only the Liouville implication once the sequence is supplied. The next action should therefore probe which part of the Navier–Stokes equation can create global tail tightness from local Type-I control.

**Objection:** do not use `L^∞_t L^3_x`, weak Serrin, or fixed-profile assumptions as intermediate lemmas unless they are independently derived from finite `I`.

**Vote:** `EFFECTUAL_PROBE_BEFORE_THEOREM`.

## 2. Scaling / compactness lead

**Background:** critical scaling, translation/dilation noncompactness, profile decompositions and persistence of singularities.

**Delegation:** identify the exact coordinate missing from the parent functional calibration and test candidate compactness transfers.

**Finding:** the missing coordinate is global spatial tightness modulo the PDE. Local convergence and uniformly bounded `A,C,D,E` allow mass to escape or replicate across distant regions in the functional model. A minimal-element/almost-periodic route would be useful only after defining a genuinely controlled critical quantity and proving stability/minimality.

**Objection:** importing Kenig–Merle-style compactness language without a Navier–Stokes threshold tied to finite `I` would be nomenclature, not a proof.

**Vote:** `NO_UNWITNESSED_CRITICAL_ELEMENT_TRANSFER`.

## 3. Pressure / far-field geometry lead

**Background:** nonlocal pressure, localized energy inequalities, vorticity transport and shell-flux estimates.

**Delegation:** determine whether pressure or local energy immediately blocks sparse tail replication.

**Finding:** no frozen source estimate currently sums shell fluxes uniformly to a global `L^3` trace. The pressure term is load-bearing: a velocity-only shell argument can hide long-range coupling, while a pressure decomposition can itself accumulate far-field terms.

**Objection:** any proposed tail functional must state the pressure normalization/decomposition and show that constants are uniform over expanding radii.

**Vote:** `PRESSURE_AWARE_DISCRIMINATOR_REQUIRED`.

## 4. Adversarial falsification lead

**Background:** counterexample-first theorem design, hostile orbit classes, degenerate limits and assumption-smuggling audits.

**Delegation:** define the adversarial classes that the first child candidate must face.

**Finding:** retain at least five classes: fixed backward self-similar profiles; DSS/periodic renormalized orbits; genuinely non-periodic ancient trajectories; spatially sparse/intermittent tails; and translated/dilated profile sequences whose local limit loses global mass. The parent bump train is the zero-dynamics control, not a PDE counterexample.

**Objection:** if the first proposed mechanism cannot distinguish the PDE class from the zero-dynamics bump train, it has not used the required equation-specific information.

**Vote:** `CHEAPEST_PDE_CONSISTENT_ANTI_REPLICATION_TEST_FIRST`.

## 5. Formal-methods lead

**Background:** exact statement binding, dependency DAGs, chronology and machine-auditable discovery gates.

**Delegation:** ensure that child chronology is clean and no parent conclusion is silently promoted.

**Finding:** `NS-B1a` must have its own context hash, current-main memory snapshot, expert review and seven-event pre-candidate trace. The parent matrix is evidence, not the child theorem. Candidate generation remains blocked until the child gate is machine-audited.

**Objection:** do not backfill a theorem candidate into the parent trace after the atom was sharpened.

**Vote:** `PASS_PRE_CANDIDATE_REVIEW_IF_HASH_CHAIN_AND_MEMORY_AUDIT_PASS`.

## 6. Novelty / frontier lead

**Background:** primary-source mapping, neighboring-result search and rediscovery control.

**Delegation:** bound what can be claimed from the selected next action.

**Finding:** the selected bridge is an explicit research obligation exposed by source composition, not a novelty claim. Before any positive theorem is promoted, search Type-I, ancient-solution, Lorentz/Morrey/Besov, local-energy, tail-decay and backward-uniqueness literature for equivalent or stronger statements.

**Objection:** an original-looking tail functional is not progress until its implication and source novelty are independently checked.

**Vote:** `NO_NOVELTY_AUTHORITY`.

## Discussion

The cell compared four next actions:

1. assert the full `I -> backward L3 sequence` bridge immediately;
2. force almost-periodicity modulo translation/scaling;
3. derive a pressure-only global shell inequality;
4. first formulate the smallest **PDE-consistent sparse-tail discriminator**: use the ancient mild representation and pressure/local-energy structure to ask whether infinitely replicated critical packets can persist at backward times without violating a quantitative equation-specific constraint.

Actions 1–3 each assume a load-bearing property not yet derived. Action 4 directly targets the coordinate exposed by the parent calibration and can return useful information in both directions.

## Consensus and next action

Select action 4. The first mathematical candidate, on a later strict step, should be a bounded lemma about one explicit anti-replication/tail functional or a counterexample showing that the chosen PDE mechanism still permits tail escape. It must state exactly:

- the mild ancient solution class;
- the finite-`I` hypothesis actually used;
- the pressure representation/local energy identity used;
- the tail observable and its scale behavior;
- the adversarial sparse/intermittent class;
- the cheapest falsifier;
- the exact implication to a source-valid Liouville trigger.

**Consensus:** `PRE_CANDIDATE_ACCEPT / EFFECTUAL_PROBE_SELECTED / NO_THEOREM_CANDIDATE_YET / SAME_CONTEXT_ONLY / ROOT_AUTHORITY_NONE`.
