# RAKL_METHOD_CASE_STUDY — NS-B2a1

## Episode

- Active atom: `NS-B2a1 — EULER_TAIL_TIGHTNESS_OR_SIGNED_FLUX`.
- Problem signature: Type-II Euler scaling, `F(a)=1`, fixed-cylinder compactness, critical scale charges, large-radius tail, double-limit inheritance, pressure transport and Euler rigidity.
- Current framework read directly: `SzeChunYiu/RAKL@bd1a2768f0f474ff44ffa25243241f94bfaf6466`.
- Application base: `SzeChunYiu/RAKL_math@4838969ecc18a091da79a059b58b8568634289b7`.
- Application framework pin is stale at `15f1c3affe5bf85ba41ff0ab65b25ba19e0d28a3`; no pin-exact current-framework CI claim is made.

## Research method used

The cycle used an **interface-first source inheritance audit** rather than an Euler theorem search:

`freeze source/context/memory -> identify exact quantifier boundary -> inspect source inheritance -> construct cheapest topology/scaling falsifier -> retain only the surviving residual`.

Canonical RAKL surfaces used were decomposition, routing, search-query generation, source selection, claim extraction, mathematical-context translation, equivalence/similarity, contextual gluing, gap discovery, experiment selection, memory, review and saturation stopping.

## Experience that affected routing

Canonical warning `F-NS-B1a-C001-PRESSURE-SUMMABILITY` prevented a return to a raw far-field pressure-accumulation story. Pending PR #72 was used only as a noncanonical warning that absolute `F(a)=1` cutoff estimates are scale critical. Pending PR #71 was used only as a noncanonical hostile calibration for translation/profile leakage. Parabolic Navier–Stokes backward uniqueness was explicitly rejected because the extracted equation is Euler.

Because no pre-memory action ranking was frozen, this cycle does **not** claim a measured causal RAKL effect on the action choice.

## What worked

1. A manual application-side `PRE_ACTION_FIBRE_RECEIPT` was committed before the consequential source audit. This avoided repeating the retrospective chronology defect that motivated RAKL #123.
2. The source route was narrowed to an exact quantifier mismatch: convergence on each fixed `Q(a)` versus a global `lim_R limsup_k` tail statement.
3. A divergence-free critical-shell kinematic packet showed that the displayed critical scale charges plus local convergence cannot, by themselves, close the global tightness interface.
4. Same-author Liouville calibrations were checked for extra hypotheses rather than treated as generic ancient-Euler theorems.

## What failed or remains open

The audited source route did not yield a source-bound vanishing annular-tail witness. The kinematic falsifier does not test the full Euler dynamics, so it cannot establish an impossibility result. Signed flux cancellation/telescoping, recentering/minimality and alternative Euler invariants remain live.

Failure category: **local-to-global gluing / representation of the tail coordinate**, with pressure and noncompact-symmetry subcoordinates. This is not a refutation of Seregin's theorem and not a Navier–Stokes root result.

## v3 feature used or missing

Used safely in proposal/shadow mode:
- strict context-first gate;
- dual experience-memory review;
- TaskEpisode-style immutable result record;
- scoped failure experience with competing diagnoses;
- seven-axis novelty/saturation telemetry;
- method-contract surface names;
- explicit authority/non-escalation boundary.

Still missing operationally in this application:
- a canonical runtime-generated pre-action receipt rather than the manual proposal used here (`RAKL#123` remains open);
- a canonical materialized `RAKLV3State` fingerprint for the live application state;
- per-cycle token/tool/retrieval counters exposed as a receipt;
- a canonical machine-readable RAKL method-version manifest on current framework `main`.

These missing telemetry surfaces are recorded as `CANNOT_MEASURE` rather than inferred.

## Retained semantic novelty

- `KNOWLEDGE = 1`: exact source quantifier map for this child route.
- `OPERATOR = 0`.
- `EXPERIENCE_PATTERN = 0`: local-compactness/global-faithfulness failure is already a known pattern.
- `OBSTRUCTION = 1`: `O-NS-B2a1-DOUBLE-LIMIT-TAIL-INHERITANCE`.
- `RELATION = 1`: explicit source fixed-cylinder compactness -> target large-radius tail interface.
- `PATH = 0`: no successful new composition path was validated.
- `META_METHOD = 0`: the manual pre-action receipt is a trial of an already-open framework hypothesis, not a new promoted method.

## Framework-improvement hypothesis

The cycle provides one more prospective application of the RAKL #123 idea: a pre-action receipt can mechanically distinguish a properly frozen discriminator from retrospective learning. A future framework benchmark should compare automatic versus manual/no receipt on chronology defects, post-hoc discriminator changes and overhead. This single cycle does not establish that the framework change improves research outcomes.

A separate telemetry hypothesis is now visible: Paper-5-style metrology would be materially stronger if the driver emitted exact state fingerprints and resource counters automatically. One cycle is insufficient to open a new framework issue; accumulate repeated `CANNOT_MEASURE` cases first.

## Next action

Open a fresh child only after freezing its quantity of interest:

- `NS-B2a1a — PRELIMIT_ANNULAR_TIGHTNESS`, preferred if a nonnegative source quantity can be identified; or
- `NS-B2a1b — SIGNED_EULER_FLUX_TELESCOPING`, if source algebra exposes a cancellation route.

Candidate generation for an Euler Liouville theorem remains blocked until one of these global interfaces is source-bound.
