# NS-B1a3 same-context expert review — profile compactness coordinate

**Atom:** `NS-B1a3`  
**Authority:** `SAME_CONTEXT_RESEARCH_REVIEW / NOT_INDEPENDENT_PEER_REVIEW / PROPOSAL_ONLY`

## Shared question

For the Albritton–Barker Type-I ancient class with finite `I`, what equation-specific critical compactness or coherence coordinate can prevent the multi-core/tail-escape pattern that survived the previous local-energy and kinetic-energy audits? Before importing critical-element machinery, does the existing `I` functional itself distinguish one critical profile from several widely separated profiles?

## Role-separated passes

### PDE / partial-regularity lead
The source-valid target remains a backward sequence uniformly bounded in global `L^3`, or another theorem with equally explicit ancient-solution hypotheses. Existing finite `I` control is local and scale invariant; no source licenses the step `I<∞ -> global L^3 sequence`. The cheapest next action is therefore to audit the profile geometry of `I`, not to assert a compact critical element.

**Strongest objection:** a separated-packet calculation is not a Navier–Stokes solution and cannot prove that genuine ancient solutions split.  
**Recommendation:** use it only to decide whether `I` alone can serve as the minimizing/profile-counting currency.

### Critical-scaling / concentration-compactness lead
Kenig–Koch and Gallagher–Koch–Planchon obtain critical elements in genuinely global critical spaces with profile-decomposition and stability machinery. The relevant transfer obligation is not merely scale invariance; it is control of the defect of compactness. A local supremum functional can be scale critical while being insensitive to multiplicity at large translation separation.

**Strongest objection:** a non-additive norm can still admit concentration compactness through a subtler profile theorem.  
**Recommendation:** falsify only the naive route “minimize `I` and inherit one-profile compactness”; preserve critical-element programs using an additional global topology or PDE interaction theorem.

### Vorticity-geometry / coherence lead
The previous failures do not touch dynamically inherited directional coherence or depletion of vortex stretching. If `I` is max-decoupled, a geometry coordinate becomes more valuable because it could couple separated high-vorticity regions without relying on an additive scalar budget.

**Disagreement preserved:** this lead ranks a vorticity-coherence discriminator at least as highly as searching for another global critical norm after the present audit.

### Adversarial functional-analysis lead
Use two fixed smooth compactly supported divergence-free spacetime packets and translate one by distance `L`. Keep `q=0`. Cylinders that see only one packet reproduce that packet's local score. A cylinder seeing both has radius `r ≳ L`; fixed finite numerators then yield `A,E=O(L^-1)`, `C=O(L^-2)`, and `D=0`. Thus the hostile prediction is `I(F_1+T_LF_2,0) -> max(I(F_1,0),I(F_2,0))`.

**Cheapest falsifier of the falsifier:** find a cylinder intersecting both profiles whose normalized contribution stays order one despite fixed packet size/mass. The large-radius denominators make that impossible for this compact-support calibration.

### Formal-methods / authority lead
The result, if obtained, is a functional calibration and route-pruning certificate. It cannot be encoded as a Navier–Stokes theorem, a failure of concentration compactness in general, or a theorem about actual ancient solutions. Context, memory, review, and the seven-event pre-candidate trace must be frozen before the candidate proposition.

### Novelty / research-value lead
Primary sources already establish critical-element methods in `Hdot(1/2)`, `L^3`, and critical Besov settings. The proposed observation should therefore carry **no novelty claim** unless a later bounded literature audit supports one. Its immediate value is research control: it identifies an exact missing transfer assumption between those global critical spaces and the Albritton–Barker `I` class.

## Joint disposition

`PROCEED_WITH_CHEAP_FUNCTIONAL_DISCRIMINATOR / NO_PDE_CANDIDATE_YET / PRESERVE_COMPACTNESS_AND_GEOMETRY_ALTERNATIVES`.

The expected useful negative result is narrow: if `I` is asymptotically max-decoupled under large translation separation, finite `I` alone is not an additive profile currency and cannot by itself justify a one-profile minimal-critical-element step. A successful Navier–Stokes route would then need an additional global critical/tightness coordinate, a PDE-specific no-splitting theorem, a coherence mechanism, or a different Liouville trigger.
