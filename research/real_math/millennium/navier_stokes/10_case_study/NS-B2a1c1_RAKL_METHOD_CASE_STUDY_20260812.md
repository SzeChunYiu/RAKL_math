# RAKL_METHOD_CASE_STUDY — NS-B2a1c1

**Authority:** proposal/shadow method evidence only. No framework promotion, theorem promotion, or root authority.

## Atom / problem signature

`NS-B2a1c1`: compare the 2026 generalized Type-II producer `F(a)=a^(alpha-1)` with the Section-4 cutoff consumer (4.4), under `2alpha-3<=0`, `3/s1+2/l1=4`, `l1<=s1`, and audit whether the old pure-power cutoff mechanism transfers under `alpha=2-m` without importing unmatched rigidity hypotheses.

The frozen positive goal was deliberately local: decide exact parameter compatibility and identify the next same-theory rigidity interface. The Clay root remained `OPEN_NO_SOLUTION_CERTIFICATE` throughout.

## Actual fibre consulted

The cycle used the frozen packet `03_context/NS-B2a1c1_PRE_CANDIDATE_PACKET_20260812.json`. The load-bearing coordinates actually used were `F(a)=a^(alpha-1)`, `alpha<=3/2`, `3/s1+2/l1=4`, `l1<=s1`, the exact cutoff asymptotic `a^(2-3l1/2)/F(a)^((l1+1)/2)->0`, the old parameterization `alpha=2-m`, the parent F=1 incompatibility, and the separate global-tail/terminal-rigidity failures. No unrelated contextual material was counted as consulted.

## Small expert cell and delegation

Seven same-context roles were separated before the R2 candidate: a Type-II scaling/PDE role checked the rescaling and equation change; an axisymmetric-vorticity role checked the transported `omega_theta/r` functional; a functional-analysis role checked the exponent cone and strict endpoints; a source/provenance role bound the current 2026 and current v3 sources; an adversarial role attacked F=1, equality and boundary values; a gluing role separated local exponent success from finite-`g` and terminal-trace requirements; and a RAKL/metrology role audited chronology, memory authority and retained semantic novelty. These roles are one context-sharing expert cell, not independent mathematical review.

## Methods / operators / motifs tried

The current `method_specs.py` surfaces actually invoked were `decomposition`, `routing`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `memory`, `review`, and `saturation_stopping`. Gate-C routing selected **SEARCH**: the exact same-domain pure-power cutoff episode survived under a DifferenceWitness, so JUMP and GLUE were unnecessary and LIFT was not eligible. The reused motif was `MOTIF-CONSUMER-ASYMPTOTIC-COMPATIBILITY-FIRST`.

The operative mathematical move was not a new theorem technique: solve the full source-admissible exponent cone before treating a parameter-specific consumer failure as an operator-family failure.

## Prior experience retrieved, selected, rejected, or missed

Selected experience that changed routing: `F-NS-B2a1c-F1-AXIAL-44-EXPONENT-INCOMPATIBLE` forced departure from the F=1 slice; `F-NS-B2a-F1-ABSOLUTE-CUTOFF-FLUX-NONDECAY` prevented a retry of the saturated absolute F=1 cutoff route; `F-NS-B2a1-DOUBLE-LIMIT-TAIL-INHERITANCE` forced separation of local exponent algebra from global/terminal inheritance; `TE-NS-B2a1c-R2-20260812` supplied the exact predecessor episode; and the compatibility-first motif prioritized the source-interface calculation.

`OTEP-LIONS-LOCAL-TO-GLOBAL-TIGHTNESS-SPLIT` was retrieved but rejected for this atom because it addresses the general tail residual rather than the local exponent compatibility edge. Type-I finite-I geometry/compactness episodes were rejected by DifferenceWitness: their producer equation/scaling class is different. The recent asymptotic-density route was rejected because the Hill-vortex falsifier blocks bare local-nontriviality-to-positive-density transport without an added inherited relation. Cross-Millennium retrieval was not run because an exact same-domain SEARCH route already dominated; a distant transfer would add assumptions without reducing this atom. Exhaustive missed-memory coverage is `CANNOT_MEASURE` because no governed complete memory-universe receipt was materialized.

## Decision policy and observable routing change

Before memory/gates, the plausible preference was to retry the F=1 conservation route or search broadly for an Euler rigidity trigger. RAKL changed the chosen action to the generalized-F parameter audit because the predecessor failure was scoped to one asymptotic parameter slice and the same-domain SEARCH episode offered a cheaper discriminating test. Experience affected priority only; it supplied no truth authority.

## Falsifier / verification

Counterexample-first checks attacked `alpha=1`/F=1, the strict-equality boundary `E=0`, `l1->1+`, `l1=5/4`, and `alpha=3/2`. The surviving relation is

`alpha > (5-2l1)/(l1+1)`, with `1<l1<=5/4` and `alpha<=3/2`.

The interval is nonempty for every `l1>1`, and under `alpha=2-m` the cutoff exponent becomes `[m(l1+1)+3-4l1]/2`, so the strict inequality is exactly current arXiv:2402.13229v3 equation (2.11). Primary-source provenance was bound to arXiv:2606.29468v1 and current arXiv:2402.13229v3.

## Outcome and residual

Outcome: `PARTIAL_SUCCESS / SCOPED_PARAMETER_COMPATIBILITY_VERIFIED`. The parent F=1 failure is now diagnosed as parameter-specific for this consumer: the generalized power-log family reopens a nonempty cutoff-conservation parameter window.

The residual is a separate local-to-global/same-theory gluing obstruction, `O-NS-B2a1c1-FINITE-G-TERMINAL-RIGIDITY-GLUE`. The 2026 Proposition 4.1 needs finite `g(t0)` and conservation does not itself imply zero; the older Proposition 2.3 terminal-zero route adds an ancestor time-slice `L^q` hypothesis not present in the frozen generalized producer. The next atom is `NS-B2a1c1a — GENERALIZED_F_TERMINAL_TRACE_RIGIDITY`.

## Episode -> diagnosis -> obstruction/lesson separation

Episode: `TE-NS-B2a1c1-R2-20260812` records what was actually tried and observed. Diagnosis: `D-NS-B2a1c1-F1-FAILURE-WAS-PARAMETER-SPECIFIC` interprets only the local source-consumer failure. Obstruction: `O-NS-B2a1c1-FINITE-G-TERMINAL-RIGIDITY-GLUE` names the remaining producer/consumer type mismatch. Lesson candidate: `L-NS-B2a1c1-PARAMETER-CONE-BEFORE-OPERATOR-BLACKLIST` is proposal/shadow and receives zero retained EXPERIENCE_PATTERN credit until protected gates authorize it.

The mathematical repeated-failure link is to `F-NS-B2a1-DOUBLE-LIMIT-TAIL-INHERITANCE`: both have a producer-output/consumer-input mismatch, but the earlier failure concerns annular tail quantifier order whereas this one concerns a global vorticity quasi-norm plus terminal rigidity. They remain distinct failure coordinates.

## Failure categories

Local mathematical failure: none inside the verified compatible window; F=1 and equality are explicit falsified subcases. Retrieval/decomposition failure: none consequential after routing correction. Representation/source failure: wholesale transfer of old Proposition 2.3 is rejected because its ancestor `L^q` premise is unmatched. Gluing failure: finite `g(t0)` and terminal/other rigidity remain unproved from the generalized producer. Verification/tooling/meta-policy failure: the first same-cycle C001 trace artifact omitted current `ResearchTraceEntry` minimum fields. It is preserved as `F-PROC-NS-B2a1c1-TRACE-SCHEMA-MINIMUM-MISSED`; C001 is process-invalid and was **not** backfilled. A corrected Gate-D trace was frozen before a fresh R2 candidate identity.

## Seven saturation axes

Reopened: `OBSTRUCTION`, `RELATION`, and `PATH` because the generalized parameter cone changes the obstruction map and opens the terminal-rigidity successor path. Flattened/no retained novelty this cycle: `KNOWLEDGE`, `OPERATOR`, `EXPERIENCE_PATTERN`, and `META_METHOD`. The F=1 specialized consumer path remains flattened; the bare asymptotic-density family remains saturated under its recent falsifier. Root saturation is irrelevant because the root is not close to closure.

Defensible RAKL novelty class for the solved subproblem: primary **compositional**, secondary **representation**. The source equations are prior knowledge; the retained content is their exact source-compatible composition and the resulting obstruction split.

## What v3 helped, what failed, and improvement hypothesis

Helpful v3 features were the scoped TaskEpisode substrate, the separation of experience from authority, DifferenceWitness requirements, SEARCH-before-JUMP/GLUE/LIFT routing, and explicit contextual gluing. They prevented the parent F=1 failure from becoming a global method blacklist and prevented the old pure-power theorem from being imported wholesale.

The process failure exposed a missing ergonomic guard: application authors can hand-materialize a trace that looks plausible but omits current `ResearchTraceEntry` minimum fields before the runtime audit is actually exercised. **Framework-improvement hypothesis:** expose a first-class pre-write trace constructor/auditor that materializes the exact current required event fields and returns a protected preservation receipt before any candidate-write surface is available. This is a hypothesis from one observed application failure, not a framework defect verdict or promotion claim.

No framework issue/PR is opened from this single observation. Repetition should be measured before method promotion.