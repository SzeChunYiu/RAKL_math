# RAKL_METHOD_CASE_STUDY — NS-B1a3b1a-C001 R1

**Authority:** `PROPOSAL_SHADOW_CASE_STUDY / NO_ROOT_AUTHORITY`  
**Current framework binding:** RAKL `main` `8118b9c96cd426439a7e3c4b737d279a047e37f2`, incumbent method `3.0.0`, package `0.1.0`.  
**Application base:** RAKL_math `c48293364c6bdf7e0c5d93f5e01d889008c9eb61`.

## Atom, fibre, and expert cell

The active atom was `NS-B1a3b1a`: determine whether Albritton–Barker finite-`I` **numerical scale functionals by themselves** can produce Grujić v2's uniform time-slice critical vorticity amplitude and logarithmic-BMO direction signature. The frozen fibre was `sha256:f37366cf495ba3e1c12b240ad92b763b92fab553c958766953473029c67806f2` and included the Clay root contract, exact AB producer, exact Grujić v2 consumer, parent B1a3/B1a3b/B1a3b1 negative history, the derivative-loss and local/global failures, and the workflow-only cross-Millennium bridge audit with an explicit DifferenceWitness.

A same-context four-role cell was used: PDE/partial regularity; harmonic analysis/critical spaces; geometric vorticity; and adversarial source/verification. It receives `0/3` independent-review credit.

## Methods, prior experience, and routing

The pre-memory preference was a direct scaling/interpolation attempt from finite `I` to the consumer norms. The parent lesson `L-NS-B1a3b1-SCALE-STABLE-CONSUMER-MOVES-OBSTRUCTION-UPSTREAM@CANDIDATE`, derivative-loss failure, topology-inheritance failure, and local/global interface failures changed routing to a counterexample-first test of the weakest norm-only producer claim. `BARKER-PRANGE-2019-TYPEI-ALIGNMENT`, `MILLER-2020-ANISOTROPIC-VORTICITY`, and profile leakage were retrieved but rejected/deferred because they do not test the exact producer signature.

No prior time-frequency-burst motif was found in the bounded three-query repository search; that is not an exhaustive novelty claim. Cross-Millennium memory contributed only the abstract producer-output/consumer-input audit. Its DifferenceWitness blocks all mathematical transfer.

## Falsifier and verification

The hostile family uses a compactly supported vector potential `A_N=(0,0,N^-1 chi cos(N x1))`, the divergence-free field `v_N=curl A_N`, and a time burst `u_N=eta(N^2(t-t0))v_N`. Its time support is `O(N^-2)`, velocity is `O(1)`, and spatial gradient is `O(N)`. Consequently the numerical AB scale envelopes satisfy `A≲r^2`, `C≲r min(r^2,N^-2)`, `D=0`, and `E≲N^2 r^2 min(r^2,N^-2)≲1` uniformly in `N`.

At the peak time, on an interior ball, vorticity is exactly `(0,0,N cos(Nx1))`; its weak-`L^{3/2}` norm grows at least linearly. Direction alternates across nodal planes on radius `~N^-1` balls, giving order-one mean oscillation and therefore logarithmic-BMO growth `≳log N`.

The family is **not asserted to solve Navier–Stokes**. This is a representation/norm-interface falsifier, not a PDE counterexample. The positive implication `NSE + finite I -> consumer` remains open and would require equation-specific smoothing, rigidity, or another mechanism.

## Episode -> diagnosis -> obstruction/lesson

**Episode:** `EP-NS-B1a3b1a-C001-R1-20260811` records the attempted hostile near-miss and its scoped verification.

**Diagnosis:** `D-NS-B1a3b1a-FINITE-I-CONSUMER-TOPOLOGY-MISMATCH` says the obstruction is not scale incompatibility: parabolic spacetime derivative control permits time-concentrated high-frequency events and therefore cannot, by norm algebra alone, yield uniform time-slice critical vorticity amplitude/direction control.

**Reusable obstruction:** `O-NS-B1a3b1a-EQUATION-SPECIFIC-PRODUCER-UPGRADE` — a successful producer bridge must use a Navier–Stokes-specific mechanism beyond the bare `A/C/D/E` numerical signature.

**Candidate lesson:** `L-NS-B1a3b1a-PARABOLIC-MORREY-DOES-NOT-BARELY-MINT-TIMESLICE-PHASE-AMPLITUDE@CANDIDATE`. It guides priority only; it is not theorem authority.

## Local failure versus gluing failure

The new local representation failure is `F-NS-B1a3b1a-BARE-FINITE-I-TO-GRUJIC-CONSUMER-EMBEDDING`. It occurs before any blow-up limit or global gluing. Separately, Grujić's global Lorentz/far-field inputs remain blocked by `F-NS-B1a3-UNCONTROLLED-FAR-FIELD` and `F-NS-B1a3-LOCAL-GLOBAL-INTERFACE-MISMATCH`. Strong local velocity convergence still cannot be differentiated into vorticity-direction convergence. Pressure localization, noncompact symmetries, backward uniqueness hypotheses, and the pre-singularity/ancient state-space mismatch remain downstream and open. Type II is untouched.

## Saturation and novelty

Retained semantic novelty is conservatively `KNOWLEDGE=0`, `OPERATOR=0`, `EXPERIENCE_PATTERN=1`, `OBSTRUCTION=1`, `RELATION=1`, `PATH=1`, `META_METHOD=0`. Reopened axes: EXPERIENCE_PATTERN, OBSTRUCTION, RELATION, PATH. KNOWLEDGE, OPERATOR, and META_METHOD are locally flat. No global saturation claim is made. The solved scoped subproblem is novelty class `representation`, structural rank `0`.

## Current v3 feature used

The current `route_family_health` feature was used only at proposal/shadow authority. Parent and child share the declared core producer-consumer representation and root-bridge hypothesis, so continuity is `SAME_ROUTE_FAMILY`. Because no root-coordinate preservation interface is established, the new scoped result is classified `LOCAL_PROGRESS`, not root progress. This feature is explicitly non-authoritative and does not recommend abandonment.

## Framework-improvement hypothesis

A typed hostile-near-miss record could be added to the experience substrate with an explicit `equation_preservation` coordinate. The framework could then mechanically prevent a successful norm-space falsifier from being misread as a PDE counterexample while still retaining the obstruction as search-control evidence. This is a hypothesis, not a framework defect claim.

## Next action and gates

Search for or derive an equation-specific finite-`I` to time-slice vorticity amplitude/phase upgrade; if no such bridge survives source and counterexample audit, rotate to a different exact consumer rather than repeating norm-only interpolation. Root remains `OPEN_NO_SOLUTION_CERTIFICATE`, closed proof DAG is false, isolated recheck is absent, bounded root novelty search is absent, and genuinely isolated mathematical reviews remain `0/3`.
