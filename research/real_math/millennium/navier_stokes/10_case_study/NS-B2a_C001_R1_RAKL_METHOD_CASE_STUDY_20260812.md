# RAKL_METHOD_CASE_STUDY — NS-B2a-C001 R1

**Cycle:** `NS-B2a-C001-R1`  
**Authority:** `PROPOSAL_SHADOW_CASE_STUDY / NO_ROOT_AUTHORITY`  
**Framework before mathematics:** `SzeChunYiu/RAKL@f1efc79db836f7de92ef5fa800a71c224b97f6b7`, incumbent method `3.0.0`, package `0.1.0`, skill manifest `0.7.0`.  
**Application base:** `SzeChunYiu/RAKL_math@4386f1d9f2c0df4c19017c831d16cef82b61304a`.

## Atom, problem signature and actual fibre

The active atom was the exact open Type-II child from issue #65: in the `F(a)=1` branch of Seregin arXiv:2606.29468v1 Theorem 3.1, determine whether the ancient Euler limit's inherited scale-critical state actually supports a global rigidity closure, with special attention to the unproved no-incoming-flux/far-field-tightness bridge.

The frozen fibre was `sha256:42f05a5bea869d15b4c938a663385050001c4c2d15b1a2f59082ff23a0a011bc`. It actually consulted Seregin Theorem 3.1 and equations `(3.5)-(3.8)`, the `F(a)=1` example, Seregin's Section 4 cutoff-decay/conservation analogue, Gavrilov arXiv:1810.08020, prior failures `F-NS-B1a1-SCALE-NEUTRAL-LOCAL-ENERGY-LEDGER`, `F-NS-B1a3-LOCAL-GLOBAL-INTERFACE-MISMATCH`, `F-NS-B1a3-UNCONTROLLED-FAR-FIELD`, and scoped process tool `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`.

The role-separated same-context expert cell comprised: Type-II/local-energy PDE, Euler rigidity/Liouville, concentration-compactness/profile leakage, pressure/localization/far-field, adversarial construction, formal-methods/statement-binding, and novelty/research-value. These seven passes are not independent mathematical reviews and earn `0/3` root-review credit.

## Retrieved, rejected and missed experience

Selected prior experience changed routing. The B1a1 scale-neutral local-energy failure was reused only as an obstruction-morphology/process analogue, with an explicit DifferenceWitness that the current limit equation is Euler and the viscous term has disappeared. The B1a3 local/global and far-field failures were selected as gluing warnings. `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` was reused only to require exact source/target preservation accounting; no theorem content was transferred.

`F-C024-FRACTIONAL-INTEGRALITY-GAP` was retrieved but rejected because the cross-Millennium assembly analogy lacked a target mathematical applicability witness. Gavrilov's compact-support steady Euler flow was retrieved as an exact adversary but rejected as a direct counterexample after source-signature checking: a nonzero steady compact-support field makes the `F=1` normalized gradient-spacetime term grow linearly with radius. Albritton–Barker's Type-I Navier–Stokes Liouville route was rejected because its equation and global critical-state hypotheses do not match the Type-II ancient-Euler source class. Exhaustive missed-memory count is `CANNOT_MEASURE` because the connector did not materialize the canonical v3 memory index.

## v3 decision policy and obstruction-transformation routing

Before memory review, the natural preference was to attempt a direct Euler Liouville/energy-vanishing argument from Seregin `(3.5)-(3.8)`. Current v3 routing changed the action: first run the cheaper large-radius cutoff scaling and quantifier-order audit.

The content-bound obstruction fingerprint was: local source certificate -> large-radius assembler -> global rigidity target, with failure modes scale-neutral boundary flux, profile leakage, unlicensed limit exchange and incoming flux from infinity. `SEARCH` survived only as a process analogue; `JUMP` had no strict viable match; `GLUE` was blocked by the missing tail interface; `LIFT` was not licensed because bounded cross-problem coverage was unavailable and the direct scaling discriminator had not been exhausted.

Canonical `method_specs.py` process surfaces invoked were `decomposition`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `experiment_query_selection`, `memory`, `review`, `saturation_stopping`, and `prompting_context_policy`.

## Methods/operators/motifs and falsification

The selected operators were `OP-NS-B2a-F1-CUTOFF-SCALING-AUDIT`, `OP-NS-B2a-FIXED-R-TO-TAIL-QUANTIFIER-AUDIT`, `GAVRILOV-EXACT-ADVERSARY-SIGNATURE-CHECK`, and motif candidate `M-NS-SCALE-NEUTRAL-BOUNDARY-FLUX-ASSEMBLY`.

The counterexample-first discriminator was exact: before inventing an Euler Liouville theorem, determine whether the standard cutoff errors vanish at large radius under only the source's `F=1` ledger, and determine whether the cleanest exact Euler adversary actually inhabits that ledger.

For `F=1`, Seregin `(3.5)` gives `sup_t ∫_{B_R}|u|² = O(R)`, `∫_{Q_R}|∇u|² = O(R)`, and `∫_{Q_R}|p|^{3/2} = O(R²)`. The standard local multiplicative inequality gives `∫_{Q_R}|u|³ = O(R²)`. With a standard radius-`R` cutoff, the temporal term, cubic boundary flux and pressure-work term are all bounded only by `O(R)`, not `o(1)`. Thus the absolute magnitude route cannot close the large-radius limit. This does not exclude sign/cancellation or a stronger inherited tail observable.

The second verification was the compactness/gluing audit: Seregin's source convergence is obtained on each fixed cylinder before passing the blow-up subsequence. The global rigidity interface adds `R -> infinity`. Fixed-radius convergence does not itself provide a sequence-uniform tail bound or authorize that limit exchange. The pressure term has the same local-to-global issue.

## Outcome, diagnosis, obstruction and lesson

Outcome: `TRANSFER_BLOCKED_SCOPED / F1_STANDARD_CUTOFF_SCALE_NEUTRAL / GLOBAL_FLUX_TIGHTNESS_NOT_INHERITED_BY_FIXED_RADIUS_COMPACTNESS`.

Episode, diagnosis, obstruction and lesson remain separate. The immutable TaskEpisode `EP-NS-B2a-C001-R1-20260812` records what was attempted and observed. Diagnosis `D-NS-B2a-SCALE-CRITICAL-FLUX-NO-DECAY` says that the `F=1` ledger is exactly scale critical at the large-cutoff boundary, so standard absolute estimates do not manufacture vanishing global flux. The reusable obstruction candidate `O-NS-B2a-NO-INCOMING-FLUX-CERTIFICATE` is the missing source-bound bridge from fixed-radius compactness to vanishing annular energy/pressure flux. Candidate lesson `L-NS-B2a-QUANTIFIER-ORDER-BEFORE-EULER-RIGIDITY` is proposal-only: audit domain-growth exponents and limit order before invoking a global rigidity theorem.

Local mathematical failure is `NONE`: Seregin's theorem is not refuted and no local Euler contradiction was obtained. The source-route pruning failure is `F-NS-B2a-F1-ABSOLUTE-CUTOFF-NO-DECAY`. The separate local-to-global/gluing failure is `G-NS-B2a-FIXED-R-TO-GLOBAL-FLUX-TIGHTNESS`. Equation change and unique continuation are separate: the limit is Euler, so Navier–Stokes backward uniqueness is inapplicable at this node.

The next atom is `NS-B2a1`, prospectively frozen as issue #198: bind or falsify a blow-up-index-uniform annular energy/pressure-flux or no-incoming-flux/tail-tightness certificate inherited from the original suitable weak Navier–Stokes sequence through the Euler scaling. If that fails, only then search for an exact Euler rigidity theorem consuming the inherited state without hidden global hypotheses.

## Saturation and novelty accounting

Conservative retained semantic novelty for this proposal/shadow cycle is `KNOWLEDGE=0`, `OPERATOR=0`, `EXPERIENCE_PATTERN=1`, `OBSTRUCTION=1`, `RELATION=1`, `PATH=1`, `META_METHOD=0`. Raw files, commits, prose and the unpromoted lesson receive zero learning credit.

Reopened axes are `EXPERIENCE_PATTERN`, `OBSTRUCTION`, `RELATION`, and `PATH`. `KNOWLEDGE`, `OPERATOR`, and `META_METHOD` are flat for this cycle only; no global saturation claim is made. The solved scoped subproblem is classified RAKL novelty class `transfer`: an exact source-interface/process transfer obstruction was isolated. This is not a literature-novelty certificate and not new theorem authority.

## What worked, what failed, and what v3 contributed

What worked was v3's context-before-candidate gate, exact equation/state-space binding, dual success/failure memory, obstruction-transformation routing, explicit rejected retrievals, DifferenceWitnesses, role-separated expert cell, counterexample-first discriminator, hash-chained trace, and separate local-versus-gluing failure taxonomy. The practical effect was routing: these controls prevented an immediate generic Euler Liouville search and instead exposed the cheaper quantifier/tail interface first.

What failed was the direct absolute large-radius cutoff closure. That failure is mathematical at the proof-architecture level and gluing/interface level, not a refutation of either Navier–Stokes or Euler. Retrieval did not fail in a load-bearing way, but exhaustive memory recall is unmeasurable. No representation or tooling defect blocked the mathematical audit.

A proposal-only framework-improvement hypothesis is warranted but not established as a defect: add a first-class `quantifier_order/tightness_interface` coordinate to local-to-global obstruction fingerprints, because repeated PDE failures arise when fixed-domain compactness is silently exchanged with a global-domain limit. This single case supplies no method-promotion evidence; any framework change belongs in RAKL and must pass its own protected gates.
