# RAKL_METHOD_CASE_STUDY — NS-B1a3b-C001

**Cycle:** `NS-B1a3b-VORTICITY-DIRECTION-TRANSPORT-20260812`  
**Authority:** `PROPOSAL_SHADOW_CASE_STUDY / MEASUREMENT_AND_METHOD_EVIDENCE_ONLY`  
**RAKL incumbent:** method `3.0.0`, current `main` read before mathematics at `8db3343dfb764c9a139f9ba76f6f44c76eaf86de`  
**RAKL_math base:** `02c5fb7764116cf075d8dd5efd7b6fe835275ab9`

## Atom / problem signature

**Atom:** `NS-B1a3b` — orthogonal Type-I vorticity-direction / geometric-depletion child after `NS-B1a3` blocked premature global critical-element gluing.

**Signature:** local suitable-weak Type-I singularity; finite scale-invariant control; ancient blow-up extraction; derivative-defined vorticity `omega=curl u`; normalized direction `xi=omega/|omega|`; fixed double-cone / great-circle geometry; strong local velocity convergence versus weak derivative convergence; nonconvex branch mixing; source-native absolute-vorticity flux.

**Exact target:** determine whether a same-theory geometric regularity criterion can be validly glued to the Type-I blow-up limit under the actual producer topology, or whether an additional vorticity-level transport certificate is required.

## Actual frozen fibre

The cycle used the frozen `NS-B1a3b_CONTEXT_FIBER_20260812.json`, content fingerprint

`sha256:4d62297d17027481b28283c707e69e79544c3866e82e66ff559eb8071af194b7`.

The fibre bound:

- exact Clay root/success contract with root status `OPEN_NO_SOLUTION_CERTIFICATE`;
- Albritton–Barker finite-I Type-I ancient producer and its local compactness strength;
- Lei–Ren–Tian Theorem 1.1 and Corollary 1.5 as same-theory local geometry consumers/classifiers;
- Giga–Miura direction-continuity analogue and Grujic 2026 logarithmic-depletion disanalogy;
- prior `NS-B1a3` failures: global profile leakage, uncontrolled far field, local/global topology mismatch, unique-continuation hypothesis gap, and equation-change mismatch;
- explicit exclusion of Type-II, generic backward uniqueness, and stationary Leray-profile claims from this child.

No post-result context was retrofitted into the pre-candidate fibre.

## Methods / operators / motifs tried

Canonical RAKL process surfaces used conceptually and preserved in metrology were `decomposition`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `experiment_query_selection`, `synthesis`, `memory`, `review`, `saturation_stopping`, `prompting_context_policy`, and `authority_promotion`.

Operational operators/motifs were:

- `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` — reused successfully to inspect producer/consumer signatures before theorem construction;
- producer-consumer topology audit;
- nonconvex weak-closure falsifier;
- vorticity derivative-transport audit;
- source-native flux alternative audit;
- geometric-depletion / direction-coherence family;
- ancient-limit inheritance motif;
- branch-mixing adversary;
- direct-prelimit bypass;
- seven-role same-context expert cell.

No new reusable operator or motif is promoted by this single cycle.

## Prior experience: retrieved, selected, rejected, missed

### Success / analogue memory

Retrieved: `LRT-THM1.1-DOUBLE-CONE`, `LRT-COR1.5-GREAT-CIRCLE`, `AB-TYPEI-ANCIENT`, `AB-COMPACT`, `GM-TYPEI-DIRECTION-CONTINUITY`, `GRUJIC-2607.08866-LOG-BMO-DEPLETION`, `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`.

Selected: the two Lei–Ren–Tian interfaces, `AB-COMPACT`, and `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`.

Rejected/deferred: Giga–Miura because its direction-continuity hypothesis is additional and its exact theorem text was not needed as a load-bearing interface; Grujic 2026 because its `L^{3/2,infinity}` vorticity concentration and weighted-BMO direction assumptions are not consequences of the active fibre; classical coherence criteria for the same reason.

### Failure memory

Retrieved: `F-NS-B1a3-PROFILE-LEAKAGE`, `F-NS-B1a3-UNCONTROLLED-FAR-FIELD`, `F-NS-B1a3-LOCAL-GLOBAL-INTERFACE-MISMATCH`, `F-NS-B1a3-UNIQUE-CONTINUATION-HYPOTHESIS-GAP`, `F-NS-B1a3-EQUATION-CHANGE-MISMATCH`, `F-NS-B1a1-SCALE-NEUTRAL-LOCAL-ENERGY-LEDGER`, and `F-NS-B1a2-KINETIC-ENERGY-NONQUANTIZATION`.

Selected for routing: the previous interface-mismatch and equation-class lessons, specifically to prevent another implicit producer/consumer glue and to keep stationary/profile theorems out of a general ancient class.

Rejected as primary diagnoses here: far-field pressure/global tail, profile leakage, and backward uniqueness. They remain valid scoped experience but Lei–Ren–Tian is a local suitable-weak consumer, so importing those blockers into this child would be category error.

**Missed relevant memory:** `CANNOT_MEASURE` because no exhaustive relevance-labelled universe was frozen. Absence of additional relevant memory is not asserted.

## Expert cell and decision policy

Seven same-context roles were frozen before the result: PDE/partial regularity, vorticity geometry, compactness/functional analysis, scaling/ancient solutions, adversarial constructions, proof-interface/formal methods, and frontier/novelty. Every finding was cross-examined by the other roles; the review earns zero independent-review credit.

The pre-memory preference was to derive or assume a vorticity-coherence property on the finite-I ancient limit and then invoke a geometric rigidity theorem. Memory plus exact interface review changed the action to:

> first test whether the actual compactness topology transports the derivative-defined geometry at all; accept Lei–Ren–Tian as a local pre-limit consumer, and use a cheap exact weak-closure falsifier before theorem invention.

This change is observable within the cycle but is not a causal RAKL-versus-model estimate because no matched pre-memory arm was frozen.

## Falsifier / verification

The registered functional falsifier was strengthened during calculation. For `a,b>0`,

`u_n=(0,-(b/n)cos(nx_1),-(a/2)x_1+(a/(4n))sin(2nx_1))`

is smooth and divergence free, converges strongly locally to `u=(0,0,-(a/2)x_1)`, and has uniformly locally bounded gradients. Yet

`omega_n=(0,a sin^2(nx_1),b sin(nx_1))`

lies in one fixed two-sided cone about `+/-e3`, while its weak limit

`omega=(0,a/2,0)`

is transverse to that cone. The exact cone inequality is

`|omega_{n,h}| <= (a/b)|omega_{n,3}|`.

Verification surfaces separately checked divergence freedom, the curl, strong velocity convergence, uniform derivative boundedness, weak limit, and cone inequality. The construction is explicitly `FUNCTIONAL_TOPOLOGY_CALIBRATION_ONLY`, not a Navier–Stokes solution.

The scoped obstruction is falsified by any source-valid Navier–Stokes theorem that provides strong enough vorticity compactness, branch selection, Young-measure rigidity, or source-native flux transport for the exact Type-I rescalings. A direct pre-limit geometry proof bypasses rather than refutes it.

## Episode -> diagnosis -> obstruction / lesson separation

**Episode observation:** the registered Type-I velocity/local-energy compactness output does not decide the vorticity-direction range required by the selected geometry consumer.

**Diagnoses:** derivative loss is supported; more specifically, nonconvex `+/-` cone branch mixing is supported by the explicit calibration. Instability of normalization and moving high-vorticity sets is secondary. Far-field pressure is rejected as the primary cause; unique continuation and equation change are not invoked.

**Scoped obstruction candidate:** `O-NS-B1a3b-VORTICITY-DIRECTION-TRANSPORT-INTERFACE` — bare producer topology cannot carry the nonconvex directional consumer without an extra equation-specific vorticity transport certificate.

**Lesson candidate:** `L-NS-B1a3b-BARE-VELOCITY-COMPACTNESS-DOES-NOT-GLUE-DIRECTION@PROPOSED_NOT_CONSOLIDATED`. It remains candidate-only; this same-context cycle does not grant reusable lesson authority.

## Outcome / residual

**Outcome:** `PARTIAL_SUCCESS / SOURCE_BOUND_GEOMETRY_ROUTE_PRUNING_AND_REFACTORING`.

The positive finding is that Lei–Ren–Tian provides a local same-theory geometry consumer that avoids the global-L3/far-field interface from B1a3. The negative finding is that raw vorticity direction cannot be inherited by assertion through the available weak derivative topology; the exact defect is nonconvex branch mixing.

**Residual before:** broad vorticity-direction/geometric-depletion reopening after the global compactness-to-rigidity glue failed.

**Residual after:** two exact descendants:

- `NS-B1a3b1` — vorticity-flux / branch-compactness certificate: test whether finite-I dynamics supplies an oriented or absolute-flux observable stable enough under blow-up to replace raw `xi` transport;
- `NS-B1a3b2` — direct pre-limit great-circle/cone contradiction: derive an exact local geometric condition from finite-I dynamics and activate a source-valid theorem without transporting `xi` to the ancient limit.

`NS-B1a3a` global critical tightness remains open. Type-II remains open. Root remains open.

## Failure category

Primary: `DERIVATIVE_GEOMETRY_TRANSPORT_INTERFACE`.

Normalized scoped failures:

- `F-NS-B1a3b-DIRECTION-TRANSPORT-NONCLOSURE`;
- `F-NS-B1a3b-HIGH-VORTICITY-DIRECTION-INSTABILITY`.

Prior far-field/profile/unique-continuation failures are neither erased nor relabelled as the cause of this child.

## Seven saturation axes / retained semantic novelty

Internal retained novelty, pending independent semantic audit:

- `KNOWLEDGE = 1` — Lei–Ren–Tian's source-bound local double-cone / great-circle singularity interface is newly bound into this Navier–Stokes lane; repository search found no prior `2501.08976`/Lei–Ren–Tian record.
- `OPERATOR = 0` — no reusable operator is promoted.
- `EXPERIENCE_PATTERN = 0` — one scoped episode is insufficient to promote a general pattern.
- `OBSTRUCTION = 1` — the nonconvex vorticity-direction transport interface is retained as one scoped obstruction candidate.
- `RELATION = 1` — one new exact relation is retained between the AB producer topology and the LRT geometric consumer, including the missing closure certificate.
- `PATH = 1` — the route is split into source-native flux/branch compactness versus direct pre-limit geometry, replacing the undifferentiated “try vorticity coherence” path.
- `META_METHOD = 0` — the framework-improvement idea below is not validated or promoted.

Axes reopened: `KNOWLEDGE`, `OBSTRUCTION`, `RELATION`, `PATH`.  
Axes flattened this cycle: none.  
Explicit zero axes: `OPERATOR`, `EXPERIENCE_PATTERN`, `META_METHOD`.

Raw files, commits, prose, issue/PR count, and source count are not included in these semantic novelty numbers.

## Novelty class

`UNRESOLVED_ROOT_WITH_SCOPED_SEMANTIC_DELTA`.

This is not a new Navier–Stokes theorem. The explicit calibration is elementary functional analysis used to falsify a proposed glue. The source-bound obstruction/relation/path claims are internal RAKL_math research-state updates pending independent novelty review.

## Framework-improvement hypothesis

**Candidate only, no `META_METHOD` credit:** `contextual_theory_gluing` could add a registered **consumer-closure probe** before connecting a producer topology to a nonlinear/derivative-defined consumer. The probe would ask whether the consumer admissible set is closed under the producer convergence; for nonconvex sets it would automatically trigger a branch-mixing/Young-measure adversary. This cycle supplies one motivating case, not sufficient evidence for a RAKL method upgrade.

## Authority / root gate

Same-context expert review passed only as same-context review. No isolated mathematical reviewer inspected the result. No formal proof of a root theorem exists. The exact Clay root contract therefore blocks all root promotion.

**Root status:** `OPEN_NO_SOLUTION_CERTIFICATE`.  
**Root authority:** `NONE`.
