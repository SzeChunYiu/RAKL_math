# RAKL_METHOD_CASE_STUDY — NS-B1a3b1b C001 R1

Authority: `PROPOSAL_SHADOW_METHOD_EVIDENCE / NO_ROOT_AUTHORITY / NO_FRAMEWORK_PROMOTION`.

## Atom / signature / frozen fibre

Active atom: `NS-B1a3b1b`, controlled by application issue #145 under Navier–Stokes root #4. The exact problem signature was the producer/consumer interface

`Albritton–Barker finite-I Type-I class -> Grujić-v2 one-center critical-point morphology`,

with weak-`L^{3/2}` vorticity amplitude, multiple-core translation/dichotomy, global far-field compatibility and the pre-singularity/ancient state-space distinction held as separate coordinates. Frozen context hash: `sha256:f86f9db826ceb8a6e44cbbe1d63052a0ee7261f709a61d19ab2a5453f7567da8`. Frozen fibre snapshot hash: `sha256:620fb4f52e69f364506e519ec6e85208dcc29d4813dfa0ccf0a346c04c97cfce`.

The two-core hostile idea arose during scoping before a durable current-v3 receipt. That idea therefore receives no prospective discovery credit. Issue #145 plus receipt `600be22b...` prospectively froze the exact source-bound verification target and allowed outcomes before the verification/serialization stage.

## Research method used

The cycle used current RAKL v3 as a **search-control and evidence-typing** framework, not as proof authority. Canonical `method_specs.py` surfaces operationally invoked were:

`decomposition`, `memory`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `equivalence_similarity`, `gap_discovery`, `experiment_query_selection`, `contradiction_diagnosis`, `contextual_theory_gluing`, `synthesis`, `review`, `saturation_stopping`, and an `authority_promotion` audit that explicitly returned no promotion.

The chosen operator was counterexample-first adversarial scaling: test whether a critical-space amplitude representation actually carries the one-center morphology demanded by the selected consumer before spending a cycle on a PDE-specific proof attempt.

## Prior experience actually used

Selected canonical experience/memory: `EP-NS-B1a3b-C001-20260811`, `F-NS-B1a3b-GEOMETRY-NOT-INHERITED-BY-TOPOLOGY`, `EP-NS-B1a3-C001-20260811`, `F-NS-B1a3-PROFILE-LEAKAGE`, `F-NS-B1a3-LOCAL-GLOBAL-INTERFACE-MISMATCH`, and `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`.

These changed routing qualitatively by blocking another direction-modulus/topology transfer and by elevating the independent multiple-core/source-family coordinate. However, causal attribution that “RAKL changed the action” is `CANNOT_MEASURE`: no pre-memory ranking was prospectively frozen.

Retrieved but rejected/deferred: pending PR #131's `NS-B1a3b1a` episode (noncanonical and non-mergeable at retrieval time), the scale-neutral local-energy failure, the finite-energy core-count failure as a direct theorem input, and the equation-change mismatch as first discriminator. They affected priority/boundaries only and carried no authority.

Missed prior experience is `CANNOT_MEASURE` because no exhaustive relevance-labelled retrieval universe was frozen.

## What worked

The primary-source type check was decisive. Grujić arXiv:2607.08866v2 Definition 2.1 supplies a one-center `Phi|x|^-2` profile and the high-vorticity radius bound; Theorem 4.1 assumes conformity to that profile and says **in particular** that vorticity is uniformly weak-`L^{3/2}`. That wording makes amplitude a consequence of the source family, not its definition.

The predeclared hostile family then closed the bare reverse implication exactly. Two fixed separated copies of a critically scaled smooth compactly supported divergence-free template have distribution function

`mu_r(lambda)=2 r^3 mu_0(lambda r^2)`,

so the weak-`L^{3/2}` distribution quasi-norm is independent of `r`. At `lambda_r=(1/2)r^-2`, the super-level set contains two balls whose centers remain distance `2L` apart, forcing any enclosing ball to have radius at least `L`, while the consumer profile would require `R=O(r)`. This is an analytic scaling argument; finite computation is only regression calibration.

The v3 episode/diagnosis/failure/obstruction separation also prevented an overclaim. The verified episode is a route-pruning observation; the diagnosis is representation loss; the local failure and gluing failure are distinct; the obstruction is the missing equation-specific one-center producer; the candidate lesson is search guidance only.

## What failed / remains open

The cycle did **not** produce the actual PDE bridge `NSE + finite I -> one-center morphology`. The hostile family is not a Navier–Stokes solution, so it cannot falsify that stronger statement. This is the main local mathematical residual.

The local-to-global/gluing residual is separate: even if a singular-point blow-up selects one core locally, the route still needs translation/dilation no-dichotomy, secondary-profile/tail control, global Lorentz/far-field compatibility, and a same-theory bridge from the ancient-limit producer to the pre-singularity consumer. Pressure localization, weak/strong convergence sufficient for vorticity source-family identification, backward-uniqueness hypotheses, and equation/state-space changes remain audited but unclosed.

No Type-II information was gained.

## Failure taxonomy

Primary cycle success/failure type: `REPRESENTATION / SOURCE_FAMILY_COMPLETENESS` route pruning.

Local mathematical failure: `F-NS-B1a3b1b-AMPLITUDE-DOES-NOT-SELECT-ONE-CORE-MORPHOLOGY` — **supported scoped failure**.

Local-to-global/gluing failure: `F-NS-B1a3b1b-LOCAL-CORE-TO-GLOBAL-CONSUMER-GLUING` — **open residual**, not proved impossible by this cycle.

Retrieval: no decisive retrieval failure; bounded source completeness remains `CANNOT_MEASURE`.

Decomposition: improved by splitting amplitude, direction and morphology/source-family coordinates.

Verification: exact local analytic argument plus same-context role-separated review; independent mathematical review remains `0/3`; no formal proof assistant run.

Tooling: repository/application CI was not yet known at this case-study freeze and must be read as a separate gate.

Meta-policy: chronology was partially retrospective because the hypothesis preceded the durable receipt; this is preserved rather than repaired post hoc.

## Saturation audit

- `KNOWLEDGE`: **REOPENED** — current consumer source revealed a separately typed morphology coordinate.
- `OPERATOR`: **FLATTENED/PRODUCTIVE** — counterexample-first scaling worked, but no new operator was invented.
- `EXPERIENCE_PATTERN`: **REOPENED** — multi-core/source-family leakage now links prior profile-leakage and geometry-inheritance failures.
- `OBSTRUCTION`: **REOPENED** — `O-NS-B1a3b1b-ONE-CENTER-MORPHOLOGY-PRODUCER` is explicit.
- `RELATION`: **REOPENED** — `critical profile -> weak-L3/2` is not reversible on amplitude information alone.
- `PATH`: **REOPENED** — next route is equation-specific one-core/no-dichotomy or a consumer replacement compatible with multi-profile ancient limits.
- `META_METHOD`: **FLATTENED** — no method change or promotion is warranted from one application case.

## Novelty classification

Scoped solved subproblem: the bare amplitude-to-one-center-morphology representation implication is closed negatively. Shadow RAKL novelty class: `REPRESENTATION_NOVEL`. This means the application representation/interface was newly separated and falsified under the frozen packet; it is **not** a claim of new mathematical novelty in the literature and mints no protected novelty certificate.

Conservative retained semantic deltas for the proposal/shadow application memory are `KNOWLEDGE=1, OPERATOR=0, EXPERIENCE_PATTERN=1, OBSTRUCTION=1, RELATION=1, PATH=1, META_METHOD=0`. Raw files, commits, prose and tests are not counted as learning.

## v3 feature assessment

Helpful v3 features: immutable TaskEpisode storage admission, explicit pre-action receipt, authority-labelled retrievals, target-conditioned fibre, separate FailureExperience/obstruction/lesson semantics, saturation routing, and method metrology. In particular, the source-signature type check plus local-vs-gluing distinction stopped weak-Lorentz membership from being silently upgraded to a morphology theorem.

Missing/awkward feature: the current general scope tuple has object/fibre identity, QoI/consumer, population/scale/regime/time, observation model and assumptions, but this case repeatedly needed a first-class **source-family / morphology / state-space** coordinate within a theorem-consumer signature. Framework-improvement hypothesis: test an optional typed `source_family_geometry` field in mathematical problem fibres and bridge audits, with DifferenceWitness requirements, to see whether it reduces norm-to-morphology and ancient-to-pre-singularity conflations across problems. One case is insufficient to open or promote a framework change, so no RAKL issue is created here.

## Next action

Attack `NS-B1a3b1b1`: source-bind or derive an equation-specific unique-core/no-dichotomy certificate for the actual finite-`I` Type-I blow-up class with the same pre-singularity/global source family required by the consumer. Run translation/dilation leakage and moving-center adversaries first. If that interface cannot be sourced, rotate away from Grujić-v2 rather than weakening its hypotheses.

Root remains `OPEN_NO_SOLUTION_CERTIFICATE`; independent review `0/3`; root promotion authority `NONE`.
