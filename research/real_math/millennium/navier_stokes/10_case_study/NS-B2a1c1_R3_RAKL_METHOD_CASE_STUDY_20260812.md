# RAKL_METHOD_CASE_STUDY — NS-B2a1c1 R3

Authority is proposal/shadow only. The strict-discovery record for this cycle is R3; same-cycle C001/R2 are retained as negative process provenance because they did not materialize all current-v3 gates before candidate identity.

**Atom/signature.** `NS-B2a1c1`: decide whether the 2026 generalized power-log producer `F(a)=a^(alpha-1)`, `alpha<=3/2`, can satisfy Section-4 cutoff condition (4.4) under `3/s1+2/l1=4`, `l1<=s1`, and whether only the exponent mechanism transfers from current arXiv:2402.13229v3 under `alpha=2-m`.

**Actual fibre.** `01_frontier/NS-B2a1c1_CONTEXT_FIBER_R3_20260812.json`, hash `sha256:0bb23767bf43d6d478cd702045f3697241e01d7394e3da9b800fa2ef048c6b64`. Load-bearing contents were the generalized `F`, nontriviality exponent, Section-4 exponent/cutoff relation, pure-power coordinate, exact equation change to Euler, F=1 predecessor failure, current-v3 conservation analogue, and explicit theorem-transfer disanalogies.

**Expert cell.** Seven same-context roles were separated: Type-II scaling/PDE, axisymmetric vorticity, exponent/functional analysis, primary-source provenance, adversarial endpoint falsification, local-to-global gluing, and RAKL/metrology. Findings were cross-challenged before the R3 candidate. This cell is not independent mathematical review.

**Methods/operators/motifs.** Current `method_specs.py` surfaces used: `decomposition`, `routing`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `memory`, `review`, `saturation_stopping`. Gate-C selected `SEARCH` using `OTEP-NS-SEREGIN-PUREPOWER-CUTOFF-R3`; the structural mapping is exponent-only and records the DifferenceWitness. JUMP/GLUE/LIFT were not needed after SEARCH survived. Reused motif: `MOTIF-CONSUMER-ASYMPTOTIC-COMPATIBILITY-FIRST`.

**Prior experience.** Selected: `F-NS-B2a1c-F1-AXIAL-44-EXPONENT-INCOMPATIBLE`, `F-NS-B2a-F1-ABSOLUTE-CUTOFF-FLUX-NONDECAY`, `F-NS-B2a1-DOUBLE-LIMIT-TAIL-INHERITANCE`, predecessor `TE-NS-B2a1c-R2-20260812`, and the compatibility-first motif. Retrieved but rejected: `OTEP-LIONS-LOCAL-TO-GLOBAL-TIGHTNESS-SPLIT` for the local exponent atom; Type-I finite-I geometry/compactness family by scaling/equation DifferenceWitness; Hill/asymptotic-density route after its recent falsifier. Cross-Millennium memory was not queried because exact same-domain SEARCH dominated; no completeness claim is made. Missed experience is `CANNOT_MEASURE` without a governed exhaustive coverage receipt.

**Decision policy.** Before memory/gates, the plausible preference was another F=1 conservation attempt or broad Euler-rigidity search. RAKL changed the action to a generalized parameter-cone audit. Experience changed priority only and granted no truth authority.

**Falsifier/verification.** Exact algebra gives `1<l1<=5/4` and cutoff exponent `E=2-3l1/2-(alpha-1)(l1+1)/2`. Condition (4.4) is equivalent to `alpha>(5-2l1)/(l1+1)`. Equality gives `E=0`; `alpha=1`/F=1 fails; the interval intersects `alpha<=3/2` nontrivially for every `l1>1`; at `l1=5/4` the threshold is `10/9`. Under `alpha=2-m`, the same inequality is exactly current arXiv:2402.13229v3 (2.11). Primary provenance is arXiv:2606.29468v1 and arXiv:2402.13229v3.

**Outcome/residual.** `PARTIAL_SUCCESS / SCOPED_PARAMETER_COMPATIBILITY_VERIFIED`. The local F=1 failure is parameter-specific rather than a global blacklist of the cutoff-conservation operator. The separate remaining obstruction is `O-NS-B2a1c1-FINITE-G-TERMINAL-RIGIDITY-GLUE`: the generalized producer does not yet supply the finite `g(t0)` premise plus a same-theory terminal-zero/other rigidity trigger. Old Proposition 2.3 carries an unmatched ancestor `ess sup_t L^q` premise. Next atom: `NS-B2a1c1a — GENERALIZED_F_TERMINAL_TRACE_RIGIDITY`.

**Episode -> diagnosis -> obstruction/lesson.** Episode `TE-NS-B2a1c1-R3-20260812` records the attempt and observations. Diagnosis `D-NS-B2a1c1-F1-FAILURE-WAS-PARAMETER-SPECIFIC` interprets the local predecessor failure. Obstruction `O-NS-B2a1c1-FINITE-G-TERMINAL-RIGIDITY-GLUE` names the remaining interface. Lesson candidate `L-NS-B2a1c1-PARAMETER-CONE-BEFORE-OPERATOR-BLACKLIST` remains proposal/shadow and receives zero EXPERIENCE_PATTERN learning credit.

**Failure categories.** Local mathematics: none in the verified window; F=1/equality are falsified subcases. Retrieval/representation: wholesale old Proposition-2.3 transfer rejected. Gluing: finite-`g` plus terminal rigidity unproved. Meta-policy/tooling: `F-PROC-NS-B2a1c1-V3-GATE-MATERIALIZATION-MISSED` records that C001/R2 did not bind all current executable gates before candidate identity; strict credit was restarted at R3 rather than backfilled. The repeated mathematical failure link to `F-NS-B2a1-DOUBLE-LIMIT-TAIL-INHERITANCE` is producer-output/consumer-input mismatch with a DifferenceWitness separating tail quantifiers from vorticity/terminal trace.

**Seven saturation axes.** Reopened: `OBSTRUCTION`, `RELATION`, `PATH`. Flattened/no retained novelty: `KNOWLEDGE`, `OPERATOR`, `EXPERIENCE_PATTERN`, `META_METHOD`. The F=1 specialized family remains flattened, and the bare asymptotic-density family remains saturated. Defensible novelty class: primary `compositional`, secondary `representation`.

**Framework observation.** v3's exact MathContextFiber, dual memory, source-verified SEARCH mapping, DifferenceWitness, scoped TaskEpisode and gluing separation prevented two false moves: global blacklisting after an F=1 failure and wholesale theorem transfer from the pure-power source. The process miss suggests one concrete hypothesis: candidate-write surfaces should require a machine-produced receipt that all current Gate A/B/C/D schemas have been instantiated and audited at the exact pre-candidate commit. This is a one-cycle improvement hypothesis, not a framework-defect verdict; no RAKL issue/PR is opened from it.