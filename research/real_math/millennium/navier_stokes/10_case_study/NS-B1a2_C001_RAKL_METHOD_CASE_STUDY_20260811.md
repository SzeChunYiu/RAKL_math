# RAKL_METHOD_CASE_STUDY — NS-B1a2-C001

**Scope:** proposal/shadow v3 research episode. No Navier–Stokes theorem, novelty, independent-review, Type-I-exclusion or Clay-root authority.

## Research method used

The cycle read current RAKL main `bd1a2768f0f474ff44ffa25243241f94bfaf6466`, including the merged v3 protected-authority hardening, while recording that RAKL_math's framework pin remains stale at `15f1c3affe5bf85ba41ff0ab65b25ba19e0d28a3`. The research path used strict context-first chronology, dual success/failure memory, a DifferenceWitness-backed root-bridge audit, a seven-role same-context expert cell, and a counterexample-first packing falsifier.

The active atom asked whether finite global kinetic energy could be the scale-symmetry breaker missing after `NS-B1a1`: could it quantize `L^3`-critical concentration cores and thereby support global tail tightness? The route was deliberately strengthened with the favorable pointwise bound `||u(t)||_infinity<=M/lambda` before being tested.

## What worked

The exact critical-scaling calculation produced a clean discriminator. If each disjoint core has local `L^3` norm at least `gamma`, then the pointwise bound forces at least `gamma^3 lambda/M` kinetic energy in each core. Finite total energy `E0` therefore yields only

`N(lambda) <= M E0/(gamma^3 lambda)`.

The bound diverges at the blow-up scale. A smooth divergence-free packed-snapshot calibration with `N~lambda^-1` scaled bumps has bounded global `L^2` energy while global `L^3~lambda^-1/3`, showing that the exponent is compatible with the instantaneous constraints.

RAKL memory changed routing materially. `F-NS-B1a1-SCALE-NEUTRAL-LOCAL-ENERGY-LEDGER` prevented another version of shell-budget summation. `F-NS-B1a-C001-PRESSURE-SUMMABILITY` prevented returning to the raw pressure-divergence route. `SOURCE-GAP-2606.07875-TERMINAL-SCALE` prevented using smaller scale as a terminal rank. The cross-problem root-bridge audit forced the surrogate/root question to be asked before launching a compactness theorem.

## What failed

The route

`finite kinetic energy + favorable Type-I amplitude + local critical concentration -> O(1) critical-core multiplicity -> global L3 tail tightness`

failed at its first bridge. Kinetic energy is supercritical relative to the `L^3` concentration scale: one critical core costs only `O(lambda)` energy.

This is an **instantaneous representation/assembly failure**, not a local Navier–Stokes estimate failure. The packed fields are not PDE solutions, and no dynamics-specific compactness, vorticity-coherence, pressure-time cancellation or no-recrossing mechanism was tested or refuted.

## Failure taxonomy

- mathematics-local: `NO` — no source-valid Navier–Stokes estimate was refuted;
- retrieval: `NO` — prior failures and the reusable audit tool were found;
- decomposition: `NO` — `NS-B1a2` isolated one candidate scale currency;
- representation: `YES` — finite kinetic energy was tested as a critical-profile quantization coordinate and found scale-misaligned;
- gluing/local-to-global: `YES` — local critical concentration plus finite physical budget does not assemble to global critical compactness;
- verification: `NO` — scaling, disjoint energy sum and divergence-free packed-snapshot identities were checked;
- tooling/framework: `PARTLY` — the application framework pin is stale although current RAKL main was read directly;
- meta-policy: `NO` — no authority gate was weakened.

## Seven-axis saturation update

Retained proposal/shadow novelty:

- `KNOWLEDGE +1`: explicit `O(lambda)` kinetic-energy cost of one `L^3`-critical core and `O(lambda^-1)` count bound;
- `OPERATOR +0`: no new problem-solving primitive;
- `EXPERIENCE_PATTERN +1`: another local/physical budget that fails to become a root-critical compactness coordinate because of scaling;
- `OBSTRUCTION +1`: finite `L^2` energy is not a scale-independent critical-core quantizer;
- `RELATION +1`: explicit disanalogy to energy-critical bubbling and link to prior scale-neutral assembly failures;
- `PATH +1`: route rotates toward dynamics-specific compactness/coherence/no-recrossing rather than more budget accounting;
- `META_METHOD +0`: no framework method established.

Reopened axes: `OBSTRUCTION`, `RELATION`, `PATH`.

## Solved-subproblem novelty class

`TRANSFER_NOVEL`, structural rank `0`, for the route-pruning subproblem only. The cycle reused a previously registered root-bridge stability audit and a structurally witnessed energy-critical bubbling comparison; it did not invent a new operator.

## Next action

Open `NS-B1a3` only through a fresh pre-candidate gate. The child should search for a dynamics-specific coordinate that rules out the multi-core/tail-escape pattern which finite energy permits. Two prioritized but unresolved families remain:

1. minimal/critical-element compactness in an appropriate genuinely critical global topology;
2. vorticity/pressure/no-recrossing coherence that dynamically suppresses repeated or spatially escaping critical cores.

The expert cell intentionally did not collapse these alternatives.

## Framework-improvement hypothesis

The cycle reinforces the existing framework-freshness telemetry gap rather than identifying a new framework operator. RAKL_math can faithfully record current-main adoption in shadow artifacts, but a canonical machine-readable episode-start receipt should bind current RAKL main, application pin, v3 authority surfaces and execution mode automatically. This remains framework issue work in `SzeChunYiu/RAKL`; no new duplicate issue is warranted from this episode.

## Attribution boundary

RAKL clearly changed the documented route by applying stored failures before action selection, but no matched model-only or pre-memory control was frozen for this exact task. The episode therefore records `OBSERVED_ROUTING_CHANGE_NOT_CAUSAL_ATTRIBUTION`, not a claim that RAKL caused improved mathematical performance.