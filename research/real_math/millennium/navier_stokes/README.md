# RAKL Verified Discovery — Navier–Stokes existence and smoothness

Persistent root issue: `RAKL_math#4`.

Root status: `OPEN_NO_SOLUTION_CERTIFICATE`.

Two parallel lanes attack the same exact Clay root without conflating their intermediate authority:

- `NS-R001` — critical-control/global-regularity route;
- `NS-B1` — Type-I blow-up classification and ancient-solution rigidity route, with Type-II retained separately as `NS-B2`.

The blow-up lane has isolated `NS-B1a`, the dynamics-specific trace/tail bridge. A parent functional calibration showed that finite local Type-I quantities do not imply a globally `L^3` time slice without using the Navier–Stokes equation. PR #19's historical identity `NS-B1a-C001` contains a retrospective analytic shell calculation: critical Morrey energy growth makes the **instantaneous far-field quadratic pressure contribution summable**. Exact Git history shows that the identity, calculation, and interpreted result first appeared together, so this material is `RETROSPECTIVE_ANALYTIC_CALIBRATION / SEARCH_CONTROL_ONLY`, not preregistered candidate evidence. It changes search priority only and creates no Navier–Stokes root evidence.

The next child is `NS-B1a1`: pressure-aware **temporal shell-flux anti-replication**. It requires a fresh context, memory review, same-context expert cell and hash-chained pre-candidate trace before any candidate is generated.

Historical RAKL issues/branches remain migration provenance only. Current framework authority is always `SzeChunYiu/RAKL@main`; application state belongs here.

The PR #19 source blobs and chronology failure are preserved under `04_candidates/negative_history/`. Later event timestamps and later tests do not backfill an immutable evaluator/candidate freeze.

Same-context expert roles are not independent review. Computation cannot promote to theorem authority. Excluding Type-I behavior would still leave `NS-B2` Type-II and therefore would not close the Clay root.
