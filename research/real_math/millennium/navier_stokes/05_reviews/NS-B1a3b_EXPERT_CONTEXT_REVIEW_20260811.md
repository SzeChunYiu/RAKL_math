# NS-B1a3b expert-context review — pre-candidate

Authority: `SAME_CONTEXT_PRE_CANDIDATE_REVIEW / NOT_INDEPENDENT / ROOT_AUTHORITY_NONE`.

The frozen context is `NS-B1a3b_CONTEXT_FIBER_20260811.json` with fibre hash `sha256:5abea6153d4a3801a8f6637e6dc0e5aafeecefc438981052c5c9b082136818e4`. Six specialists reviewed the same source packet before any candidate result was allowed.

## Type-I / ancient-limit specialist

Delegation: bind the selected geometric theorem to the exact whole-space Type-I mild class and compare it with the existing Albritton–Barker finite-`I` ancient object.

Finding: Giga–Miura Theorem 1.1 is already stated for a whole-space Type-I mild solution and therefore fits the blow-up-classification lane without a global finite-energy hypothesis. This does **not** mean every Albritton–Barker finite-`I` ancient solution automatically carries Giga–Miura continuous alignment. The route must remain conditional unless that inheritance is separately proved.

## Vorticity-geometry / depletion specialist

Delegation: separate genuine direction coherence from the already-falsified local strain/vorticity alignment shortcut.

Finding: the selected quantities are a spatial modulus of `zeta=omega/|omega|` on high-vorticity sets and, quantitatively, `integral ||grad zeta||_infinity^2 dt`. They are not the pointwise positive-eigenframe alignment tested in `NS-R001b`. The latter therefore cannot be reused as a refutation. Giga–Miura explicitly marks the `L_t^2 L_x^infinity` gradient condition as scaling invariant.

## Blow-up compactness / pressure specialist

Delegation: audit convergence, pressure normalization, far field, and noncompact symmetries.

Finding: the source normalizes at spatial maxima, obtains bounded rescaled mild solutions plus derivative bounds, and extracts locally uniform convergence of velocity and vorticity. Mildness passes using weak-star convergence of `u_k tensor u_k` in `L^infinity`. In the whole-space theorem the pressure is bound through the mild formulation rather than a local pressure decomposition. No finite-energy or spatial-decay hypothesis is required, so far-field loss is not an unlisted theorem input. Translation of the centers and dilation are part of the normalization, not a global-tightness claim.

## 2D Liouville / rigidity specialist

Delegation: check the exact rigidity interface after direction collapse.

Finding: constant spatial vorticity direction on the nonzero set yields, after rotation at a time slice, `omega=(0,0,omega_3)`. Then `(curl omega)_3=0` and boundedness gives harmonic `u_3` spatially constant; `omega_1=omega_2=0` gives `x_3`-independence of the horizontal velocity; mild uniqueness propagates the two-dimensional structure. The bounded ancient 2D vorticity equation is then killed by the source's maximum-principle/Liouville lemma. No stationary Leray equation is introduced.

## Adversarial scaling / interface specialist

Delegation: try to break the high-vorticity-set and direction-limit passage.

Finding: direction is undefined at `omega=0`, so all passage statements must be localized to compact subsets of `{omega != 0}`. On such a compact set the limiting vorticity has a positive lower bound and local-uniform convergence makes the rescaled vorticity eventually exceed the transported threshold `lambda_k^2 d`; the source performs this check explicitly. The theorem is therefore not entitled to a direction statement on the zero-vorticity set. The contrapositive must quantify correctly: a Type-I blow-up forces failure of the selected criterion for every fixed `d>0`, not merely for one preferred threshold.

## RAKL formal-methods / provenance specialist

Delegation: enforce theorem-signature typing, root limits, episode/diagnosis separation, and prospective falsification.

Finding: the strongest admissible pre-candidate discriminator is: either (a) the Giga–Miura criterion supplies a fully typed conditional rigidity interface but finite-`I` does not itself furnish the geometry, giving a classification residual; (b) an exact source hypothesis fails to bind and the route is blocked; or (c) a separately proved inheritance bridge is found. Type-II is untouched. Same-context agreement is not independent review and cannot promote root authority.

## Joint decision

Proceed with one source-signature audit centered on `GM-COR2.6` and the supporting Giga–Miura blow-up proof. Treat the old local-alignment construction as a disanalogy. Do not invoke global `L^3`, backward uniqueness, stationary self-similar Liouville, or pressure-tail summability unless the selected route unexpectedly requires them.
