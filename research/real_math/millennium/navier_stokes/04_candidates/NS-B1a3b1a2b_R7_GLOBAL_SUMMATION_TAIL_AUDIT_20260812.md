# NS-B1a3b1a2b-R7 — global Lorentz summation/tail audit

**Authority:** proposal/shadow routing-and-gluing evidence only. This is not a theorem candidate, proof receipt, novelty certificate, Type-I exclusion, or Clay-root promotion.

## Frozen producer and consumer

Let `u` be in the registered whole-space mild bounded ancient Navier–Stokes finite-`I` source class and let `omega=curl u`. The preceding R6 cycle source-bound a local ancient-smoothing consequence and derived the fixed-time packing envelope

`m_lambda(R) := | B_R intersect { |omega(t)| > lambda } | <= C I M^4 R lambda^(-3)`

for fixed `lambda>0` and sufficiently large `R`, with `M=||u||_infinity`.

The present atom does **not** strengthen that local estimate. It audits the exact infinite-exhaustion interface required by the proposed global weak-`L^(3/2)` consumer.

## Exact exhaustion contract

For fixed `t` and `lambda>0`, `m_lambda(R)` is nondecreasing in `R`, and `B_R` increases to `R^3`. Hence countable additivity / monotone convergence gives, allowing `+infinity`,

`| { |omega(t)| > lambda } | = lim_(R->infinity) m_lambda(R)`.

Thus a global weak-`L^(3/2)` distribution bound requires a radius-uniform estimate of the form

`sup_R m_lambda(R) <= C lambda^(-3/2)`

with the appropriate uniformity in time if the consumer needs it. Equivalently, for dyadic annuli `A_j=B_(2^(j+1))\B_(2^j)`, the nonnegative annular masses

`m_j(lambda,t)=|A_j intersect {|omega(t)|>lambda}|`

must be summable with total `<= C lambda^(-3/2)`.

## Counterexample-first discriminator: unbounded envelopes do not identify the consumer

Suppose the only cumulative information available were

`m_lambda(R) <= C(lambda) Phi(R)`

for some `Phi(R)->infinity`. This is insufficient *by itself* to imply finite global superlevel mass, regardless of how slowly `Phi` diverges. An abstract hostile configuration can place equal-threshold compact smooth divergence-free packets at an increasingly sparse sequence of centers so that the number of packets in `B_R` grows no faster than the prescribed unbounded envelope while infinitely many packets remain globally. The global equal-threshold superlevel measure is then infinite.

This hostile family is a **representation falsifier only**. It is not asserted to solve Navier–Stokes, satisfy finite `I`, obey the suitable local-energy inequality, or arise from the Albritton–Barker blow-up extraction. It therefore does not refute an equation-specific tail, ancestry, no-incoming-flux, pressure-cancellation, or rigidity mechanism.

The R6 phrase “contract the `O(R)` packet capacity” is consequently sharpened: merely proving `O(R^alpha)` for `0<alpha<1`, `O(log R)`, or any other still-unbounded cumulative envelope cannot close this particular global Lorentz interface. The target is true tail summability / bounded exhaustion, not just an improved positive growth exponent.

## Source-family completeness and DifferenceWitnesses

### Same-source finite-`I` lane

Albritton–Barker define `I` through scale-critical local quantities `A,C,D,E` over parabolic subcylinders and use finiteness of this ledger to characterize the Type-I ancient lane. Their discussion also records stronger global critical controls, such as weak-`L^3` velocity control, as sufficient routes to finite `I`. The finite-`I` producer was deliberately retained here without importing the converse global tail or global critical norm.

**DifferenceWitness:** a theorem whose premise is a globally finite critical norm cannot be applied to the finite-`I` source merely because that global norm would imply finite `I`.

### Critical-element/profile analogue

Gallagher–Koch–Planchon-style critical-element/profile methods handle translation/dilation noncompactness in a globally controlled critical state space and exploit profile orthogonality/decoupling.

**DifferenceWitness:** the present finite-`I` fibre does not contain the globally finite critical norm/profile-decoupling hypothesis required to start that compactness engine. The analogue identifies the missing coordinate but does not transfer theorem authority.

### Ancient linear/heat rigidity analogue

Ancientness can eliminate some backward-growing modes for scalar linear parabolic equations.

**DifferenceWitness:** 3D Navier–Stokes vorticity has vortex stretching and nonlocal velocity/pressure coupling; no scalar maximum-principle tail theorem was transported.

## Pressure/nonlocality and other audits

- `D` is retained as local pressure-oscillation information in the finite-`I` ledger. Pressure remains a possible equation-specific rescue coordinate; no source-valid implication from `D` to summable annular vorticity mass is claimed here.
- The consumer and `I` are scale-critical in their registered forms; an observation-radius envelope cannot be erased by dimensional bookkeeping.
- No endpoint interpolation, epsilon-regularity threshold, derivative-gain bootstrap, or circular “global norm from local norm from global norm” step is used.
- No moving-radius compactness limit is exchanged with the physical/ancient limit in this cycle.
- Amplitude production remains separate from the Grujic morphology/vorticity-direction coordinates.

## Scoped result

`GLOBAL_LORENTZ_REQUIRES_BOUNDED_EXHAUSTION_OR_SUMMABLE_ANNULAR_TAIL`.

The failure category is `LOCAL_TO_GLOBAL_SUMMATION_GLUING_FAILURE`: the current local magnitude ledger and R6 packing estimate do not identify the global Lorentz consumer through any still-unbounded radial envelope. Local ancient smoothing remains a scoped success.

## Residual

`RES-NS-B1a3b1a2b-R7-SOURCE_VALID_TAIL_SUMMABILITY_OR_ALTERNATE_CONSUMER`:

Search the actual finite-`I` ancient Navier–Stokes dynamics for a source-valid radius-uniform vorticity distribution theorem, summable annular tail, no-incoming/tightness/ancestry mechanism, or pressure/local-energy cancellation that is strong enough to imply the required exhaustion bound. If no such source-valid bridge survives bounded search, rotate to a terminal consumer compatible with the genuinely available Morrey-type information rather than repeatedly optimizing an unbounded growth exponent.

## Root status

`OPEN_NO_SOLUTION_CERTIFICATE`; proof DAG open; genuinely isolated mathematical review `0/3`; no promotion attempted.
