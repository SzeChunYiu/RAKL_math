# RAKL_METHOD_CASE_STUDY — P-vs-NP global cover-incidence audit

**Atom:** `O9d12a2a1a1b`  
**Authority:** proposal/shadow research-method evidence only.  
**Framework semantics read:** `SzeChunYiu/RAKL@bd1a2768f0f474ff44ffa25243241f94bfaf6466`.

## Research method used

The intended method sequence was

`source object -> exact semantics-preserving quotient -> known easy/logarithmic calibration -> multiplexing test -> only then scalar invention`.

The mathematical action was candidate-free. It used the complete legal-pair neighborhood of every relevant semi-filter as an exact row semantics, proved that this neighborhood is injective on `G_NEQ`, and then attacked row richness with the cheapest source-native column-capacity control.

## Prior experience that changed routing

The recorded memory review carried:

- `T-PNP-UPPER-FIRST` as the selected search-control tool;
- C025 as a canonical known-answer comparator rather than a final representation;
- `F-C010-MULTIPLEXING`, which required testing whether one legal pair can discharge many apparent obligations;
- `F-C023-SCALAR-COLLAPSE`, which blocked immediate row-count/entropy scoring;
- `F-C024-FRACTIONAL-INTEGRALITY-GAP`, which warns against repairing multiplexing by silently reverting to a fractional packing relaxation;
- the parent activation-congruence failure, which moved the object from witness-local recurrence to full global incidence.

This memory materially shaped the **recorded rationale** for the action. A clean causal counterfactual versus a pre-memory model preference was not frozen, so no causal `RAKL changed the action` credit is claimed.

## What worked mathematically

Two exact source-derived local lemmas sharply narrowed the route:

1. complete pair-cover neighborhoods are injective on all relevant `G_NEQ` semi-filters, so global noncanonical row distinctions genuinely survive the witness-local congruence quotient;
2. for any fixed unequal edge, the single pair of its singleton diagonal traces covers every semi-filter above that edge, while there are at least `2^(N-2)` distinct exact rows in that family.

Thus the global source representation is richer, but the richness is free under legal-pair multiplexing. For `N=2^n`, this coexists with the source theorem `rho(G_NEQ)=n`.

The finite regression at `N=2,3,4` found exact full/quotient row counts `1/1`, `4/4`, and `17/17`, with `1,2,5` rows above a fixed edge, all covered by the same singleton pair.

## What failed

The failed research hypothesis is:

`exact global row richness -> bounded accumulating cover-cost coordinate`.

The source incidence distinguishes many rows, but a legal column can cover exponentially many of them. Raw quotient-class count, antichain count, row entropy, or an additive row-wise potential therefore has no valid per-pair budget.

Failure category:

`REPRESENTATION -> MULTIPLEXING / ROOT-COORDINATE ACCOUNTING FAILURE`.

This is not a mathematical contradiction, not a refutation of the cover/fusion framework, and not an impossibility theorem for every global incidence invariant.

## Gluing status

Local source semantics are clear, but the root-facing bridge remains open:

`exact incidence distinction != charged integral lower-bound progress`.

The missing interface is now more precise. A future object must prove that one legal pair/fusion cannot cheaply discharge an unbounded amount of the proposed quantity, while also remaining genuinely integral rather than reducing to the C024 fractional relaxation.

## RAKL process failure observed in the same run

The run also exposed a non-mathematical process defect. The pre-action context artifact looked complete in prose but did not conform to the current executable `MathContextFiber` contract: it used an invalid analogy status and omitted two required retained-analogy fields. Because `plan_math_research` was not actually executed before the audit, this was discovered only after the candidate-free action.

Consequences:

- no strict context-first credit is claimed;
- no candidate was generated;
- the frozen defective packet is preserved rather than rewritten;
- the result trace is explicitly retrospective;
- the next child requires an executable current-schema gate receipt before any candidate.

This failure is `TOOLING / META-POLICY EXECUTION`, not mathematics.

## v3 features that helped

- failure memory turned multiplexing into a first-class falsifier rather than an afterthought;
- exact source/problem scope prevented generic Set Cover/Horn hierarchy claims from receiving lower-bound authority;
- residual typing separated representation richness from the missing integral accounting bridge;
- negative history allows the route to advance without pretending previous representation work succeeded;
- vector metrology can retain `OBSTRUCTION`, `RELATION`, and `PATH` change without calling the P-vs-NP root solved or the whole operator space saturated.

## v3/process feature missing in practice

The framework already contains `audit_math_context_fiber` and `plan_math_research`; the failure was that the application action did not emit a machine-observed gate receipt before proceeding. Artifact presence was implicitly treated as gate execution.

### Framework/process improvement hypothesis

Require every consequential mathematical action runner to consume a content-bound `PreCandidateGateReceipt` generated by the current framework executable, not a hand-authored declaration that the packet is frozen. The receipt should bind exact framework SHA, context/memory/trace artifact hashes, gate verdicts and `candidate_generation_allowed`.

This is a hypothesis only. One incident is insufficient to justify a framework mutation or duplicate issue.

### Expected benefit

- prevent schema-complete-looking but executable-invalid packets from receiving procedural credit;
- make Paper-5 gate-compliance metrics machine-countable;
- distinguish `artifact_written` from `gate_executed`.

### Possible regression

A mandatory executable receipt can create coupling to framework versions and can block useful candidate-free exploratory work if its scope is too broad. The receipt should therefore govern authority/strict-discovery credit, not prohibit all exploratory calculation.

### Cheapest prospective discriminator

Across matched research atoms, compare current artifact-presence checking against mandatory executable gate receipts. Measure late gate-defect discovery, invalid candidate starts, repair cost and false blocking of candidate-free exploratory actions.

## Saturation and novelty disposition

Retained internal metrology for this episode is conservatively:

- `KNOWLEDGE=0`;
- `OPERATOR=0`;
- `EXPERIENCE_PATTERN=0` because the method motif already existed in nearby v3/Paper-5 work;
- `OBSTRUCTION=1` for exact global-row multiplexing;
- `RELATION=1` for the injective row-neighborhood relation plus its failure to control cost;
- `PATH=1` for pruning raw global row-richness scoring and opening a narrower integral-accounting child;
- `META_METHOD=0` because the gate-receipt idea is only a proposal from one process incident.

The root problem remains `UNRESOLVED`. No mathematical novelty claim is made.

## Next action

`O9d12a2a1a1c — MULTIPLEXING_RESISTANT_GLOBAL_HITTING_STRUCTURE` remains `OPEN_CONTEXT_REQUIRED`.

Before any candidate, freeze a fresh current-schema packet and **execute** the current `plan_math_research` gate. If the only surviving repair is a fractional/capacity normalization already covered by C024, rotate the method basis rather than renaming the same obstruction.
