# Same-context expert review — O9d12a2a1a1d

Authority: `SAME_CONTEXT_ROLE_SEPARATION_ONLY`. This is not independent mathematical review and earns zero independent-review credit.

## Roles and checks

1. **General circuit lower bounds / barriers** — background in nonuniform complexity, explicit-function lower bounds, and barrier discipline. Checked that a zero-cost graph-space counterexample to a candidate statistic does not imply any circuit lower bound or root separation. Recommendation: retain only route-pruning authority.

2. **Fusion / cyclic discrete complexity** — background in set-theoretic fusion, cyclic constructions, and graph cover complexity. Checked the source binding: `D^cyc_cap` charges only intersection operations and `A=R_1` is a generator, so the counted cost is zero; Theorem 3 / Theorem 30 supplies `rho=D^cyc_cap` for non-trivial targets. Recommendation: source-exact within the registered scope.

3. **Extremal set systems / VC dimension** — background in shattering, traces, and set-family complexity. Checked `T_A=P(A)`: for every `S subseteq R_1`, the union of exactly the columns indexed by points of `S` restricts to `S` on `R_1`. Recommendation: `|T_A|=2^N` and `VCdim(T_A)=N` follow immediately.

4. **Transfer / analogy control** — background in structure-preserving reductions and DifferenceWitnesses. Checked that the falsifier stays in the exact `G_{N,N}` graph generator family rather than using an arbitrary rich generator family. Recommendation: reject generic Horn/VC transfer authority; keep only the same-domain construction.

5. **Adversarial construction** — background in counterexample search and edge-case testing. Checked `N>=2`, non-triviality of `R_1`, the empty subset and full subset of the target, and that column unions require no hidden intersections. Recommendation: no local counterexample found to the stated lemma.

6. **Formal/provenance assurance** — background in statement binding, dependency identity, and verifier boundaries. Checked current root/success contracts, current RAKL main `38a530a52d863513db16052474b85e63fbb488cd`, method version 3.0.0, stale application framework pin warning, and primary-source date/title. Recommendation: proposal/shadow only until exact CI/schema checks and higher gates are available.

7. **Local-to-global gluing** — background in proof-DAG interfaces and dependency coverage. Checked that the local candidate failure does not close the parent route: a relative/incremental statistic could still exist. Recommendation: open child atom `O9d12a2a1a1e`; root remains OPEN.

## Consensus and disagreement

Consensus: the t=0 shattering construction is an exact, high-information hostile control against absolute base-trace cardinality/entropy/VC-dimension candidates. Consensus also holds that it is not a lower bound and not independent review.

Open disagreement/uncertainty: there is not yet a justified choice of normalization for an incremental trace potential, nor a theorem bounding its marginal increase under one counted intersection after arbitrary free unions. This uncertainty is the next atom rather than a reason to broaden the current claim.
