# P versus NP — RAKL flagship 001

**Root target:** prove either `P = NP` or `P != NP` under standard deterministic/nondeterministic Turing-machine definitions, with the current working hypothesis focusing on `P != NP`.

**Current authority:** OPEN_PROBLEM / NO_SOLUTION_CERTIFICATE.

## Active research cell

These are analytical roles, not claims of independent human reviewers.

1. **Complexity theorist lens**. Owns reductions, circuit/proof complexity, class containments, and meaningful bridge theorems.
2. **Barrier specialist lens**. Attempts to classify every proposed route against relativization, natural-proofs, algebrization, and known restricted-model gaps.
3. **Computational mathematician lens**. Builds exact small-instance oracles and counterexample-first tests.
4. **Formal-methods lens**. Owns exact statement binding, proof DAGs, dependency audits, and machine-checkable lemmas.
5. **Adversarial novelty lens**. Searches for neighboring known results, direction errors, hidden uniformity changes, and false claims of progress.

## Research strategy

We do not repeatedly ask a language model to write a complete proof. We maintain a persistent DAG and attack the smallest uncertified obstruction.

Current route tournament:

- `R001` monotone CLIQUE lower bounds -> isolate the negation gap;
- `R002` meta-complexity / MCSP -> seek hardness-magnification bridge obligations that admit exact finite tests;
- `R003` circuit-state dual potentials -> synthesize lower-bound invariants from exact circuit state graphs, then test whether any compressed invariant can scale without immediately falling into a known barrier.

`R003` is the current discovery engine. It is not itself a claimed path to P != NP.

## Immediate milestones

1. Freeze the canonical problem and sufficient bridge statements.
2. Refresh primary-source frontier and barrier metadata before any novelty claim.
3. Validate the exact small-circuit oracle on calibration targets.
4. Generate compressed potential candidates and attempt to falsify their gate-Lipschitz inequalities cheaply.
5. Apply surviving candidates to structured monotone targets, then NP-complete function families.
6. Formalize any nontrivial surviving lemma and run isolated review.

Every rejected route remains under `04_candidates/negative_history/`.

## O9d12a2a1a migration-integrity boundary

The migrated `O9d12a2a1a` strict packet remains pre-candidate and has no root
authority. Its source trace preserved a valid stored link sequence but six event
hashes did not equal the canonical hashes of their exact payloads. The original
file is retained as negative history. Executable gates use the separately
identified migration-repaired trace, which changes only the trace identity and
hash-chain fields.

Machine receipt:
`migration/O9d12a2a1a_TRACE_INTEGRITY_REPAIR_RECEIPT_20260811.json`.

## C025 joint-signature checkpoint

`C025` completed the cheapest gate for child atom `O9d12a2a1a` under frozen chronology. In normalized `G_NEQ` cut families, simultaneous full-semi-filter coverage is exactly injectivity of the joint binary generator signatures, giving `ceil(log2 N)` coordinates. Exact source-definition enumeration for `N<=4` found no mismatch, and registered constructions through `N=17` passed.

The same result is negative at the route level: a **cardinality-only** first-order binary-signature argument on `M` distinguished traces is exhausted at `ceil(log2 M)`. This does not rule out realizability-sensitive bounds or higher-order semi-filter closure state. The next child `O9d12a2a1a1` is pre-candidate and must receive a fresh strict context before work continues.

Authority remains `OPEN_PROBLEM / NO_SOLUTION_CERTIFICATE`. The C025 review is same-context, not independent review.
