# R004 C025 route delta — compatibility repaired, representation reopened

C025 executed the exact next action frozen in the strict `O9d12a2a1a` packet.

## What survived

On the canonical `G_NEQ` witness family, Claim 41 and Lemmas 42–43 reduce a common integral covering family to binary partitions of the diagonal complement. Joint witness signatures must be injective, so `k >= log_2 N`, and standard bit partitions attain equality.

This confirms that the C024 fractional collapse genuinely erased simultaneous compatibility.

## What failed

The repaired state is still only **canonical/original-generator separation**. It does not encode arbitrary noncanonical semi-filters or their higher-order closure relations. The existing C009 checkpoint already gives a universal `O(log N)` ceiling for canonical cover complexity.

Therefore the first-order joint-signature route is retained as a calibration tool but is not a route to the required super-logarithmic full-cover lower bound.

## Explicitly rejected overclaim

C025 does **not** prove that every object computed from polynomially many binary signatures has logarithmic value. Such a sentence has no content until the score, legal pair realization, bounded fusion law and bridge to full cover complexity are specified. Higher-order tuple/closure states remain open.

## Next child

`O9d12a2a1a1` — freeze a higher-order noncanonical semi-filter/closure-state context with:

- a precise state object beyond original-generator separation;
- a proved legal-fusion update/budget interface;
- a DifferenceWitness against C009/C025 canonical compression;
- immediate C010 multiplexing and C021 cheap-target hostile tests;
- no candidate generation before a fresh RAKL context/memory/trace gate passes.

Root remains `OPEN_NO_SOLUTION_CERTIFICATE`.

## Synthesized executable assurance

A separately commit-frozen registration and self-hashing receipt strengthen the regression without changing the negative-history verdict. Conditional on a fixed normalized partition family on `G_NEQ`, the source preservation definition was enumerated over all `4`, `18`, and `166` full semi-filters for `N=2,3,4` and all registered `5`, `73`, and `273` ordered cut families. There were zero discrepancies between direct full-semi-filter coverage and signature injectivity. Constructive and one-bit-too-small capacity cases also passed for `N=2,3,4,5,8,9,16,17`.

This does **not** extend the source normalization lemmas from canonical coverage to arbitrary full-cover pairs. It only confirms that, after the normalized family is fixed in the source calibration, the compact signature implementation agrees with direct semi-filter preservation. C009-based canonical route pruning remains the controlling negative conclusion.

The successful assurance is assimilated into `T-PNP-GNEQ-JOINT-SIGNATURE-CALIBRATION`; the failure remains `F-C025-FIRST-ORDER-CANONICAL-COLLAPSE`. Parallel provisional capacity IDs are not minted.
