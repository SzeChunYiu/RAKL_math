# RAKL_METHOD_CASE_STUDY — O9d12a2a1a1d

Framework source of truth: `SzeChunYiu/RAKL@9027cc6beab7e935d714bbdf8e902b89b50caaa8` (`rakl` package `0.1.0`, core skill manifest `0.6.6`). Application base: `SzeChunYiu/RAKL_math@111a3f95c72b0a418f968708bd3eda77ef98bccf`.

## Research method used

The cycle used current RAKL v3 as an operating method rather than a label. It first read the v3 experience substrate, metrology contract, canonical method surfaces, and the new two-phase `PreActionFibreReceipt` implementation. On the mathematical lane it froze a child atom around the smallest representation ambiguity left by the parent fixed-Lambda factorization result: before inventing another scalar on the shared `t`-intersection system, determine whether all uncounted union-node topology can be removed exactly.

Routing came from dual experience memory. The parent episode and fixed-Lambda obstruction, plus C010 multiplexing, C023 scalar collapse and C025 canonical-collapse history, changed the pre-memory preference from “propose a quotient-respecting `Phi` now” to “eliminate zero-cost structure first.” Draft PR #113 was retrieved because its free-union quotient points in the same direction, but it was kept at pending/search-priority authority and did not bear mathematical authority. Current range-avoidance work, generic Horn lower-bound transfer, generic Set Cover hierarchy transfer and cross-Millennium import were rejected because no applicability/DifferenceWitness connects them to this exact cyclic normalization atom.

A seven-role same-context expert cell separated source semantics, order/least-fixed-point structure, adversarial feedback cases, complexity transfer, formal/provenance trust, novelty/value and local-to-global gluing. The frozen falsifier was a finite source-valid OR/AND feedback system whose least fixed point changes under union-node elimination. Verification then used a direct least-fixed-point proof plus a bounded exhaustive calibration over 126,042 finite systems; the computation had zero mismatches but was explicitly treated as support, not proof.

## What worked

The strongest methodological success was **routing discipline**. Prior failure episodes prevented another score-first cycle and exposed a smaller exact question. The source-native proof then showed that, for fixed counted intersection-node values, every pure-union subsystem is just the reachability union of generators and counted outputs. Eliminating these variables gives an exact `k`-meet normal form and cleanly narrows the next search representation.

RAKL v3’s episode/diagnosis/lesson separation was useful: the immutable-style TaskEpisode records what was attempted and observed; the diagnosis identifies uncounted join topology as presentation slack; the reusable lesson remains an unpromoted candidate saying future `D^cyc_cap` lower-bound coordinates should be defined after this elimination. No file, commit, proposed lesson or local proof is counted as retained lattice learning without a protected retention gate.

The v3 two-phase receipt also helped by **failing closed**. A pre-action receipt was genuinely emitted before the verification action, but its allowed result labels were descriptive rather than exact `EpisodeOutcome` values. Current `audit_pre_action_binding` therefore derives `RETROSPECTIVE_BINDING_REFUTED` for the resulting `PARTIAL_SUCCESS` episode. The receipt was not rewritten after the result; the downgrade is preserved as evidence.

## What failed

There were two distinct process failures and one mathematical/gluing residual.

First, strict discovery chronology failed: the representation idea was already conceived before a v3 receipt existed, so the mathematical candidate is retrospective for strict RAKL-discovery credit. Second, the attempted prospective binding of the **verification** action also failed because `allowed_outcome_branches` was not typed to the exact `EpisodeOutcome` enum. This is a meta-policy/tooling-schema interface failure, not a mathematical refutation. A narrow framework issue, `SzeChunYiu/RAKL#175`, records the generic defect without embedding P-vs-NP evidence.

The mathematical local verification itself did not expose a counterexample. The remaining failure is local-to-global gluing: the normal form supplies no useful invariant `Phi`, no theorem `Phi <= f(k)`, no explicit graph with `Phi = omega(log N)`, no general circuit lower bound and no root bridge. Root status therefore remains `OPEN_NO_SOLUTION_CERTIFICATE`.

No retrieval/source failure materially blocked this cycle. No independent mathematical review, theorem prover, verifier/dependency/axiom audit, barrier audit or bounded novelty search was completed for this local representation lemma. Same-context review is not independent review, and the bounded enumeration is not proof.

## Failure classification

- mathematical/local: `SUCCESS_WITH_SAME_CONTEXT_PROOF_ONLY`; no counterexample found to the join-elimination statement;
- mathematical/gluing: `OPEN` — no `Phi <= f(k)` plus explicit super-log target and no root bridge;
- retrieval/decomposition: `NO_MATERIAL_FAILURE`; decomposition improved routing;
- representation: `PARTIAL_SUCCESS` — representation slack removed, lower-bound coordinate still missing;
- source/verification: primary-source semantics were rechecked; formal/independent verification remains absent;
- tooling/meta-policy: `FAIL` — v3 outcome-branch typing mismatch refuted prospective receipt binding;
- chronology: `FAIL` for strict discovery, and `RETROSPECTIVE_ONLY` after the binding audit;
- novelty: not audited; shadow class `representation` only, with zero protected retained novelty.

## Repeated-process linkage

This cycle belongs to the same broad chronology-risk family as earlier retrospective P-vs-NP activation/congruence and XM004-style post-result records: useful evidence may still route future search, but retrospective status cannot be upgraded by backfilling. The new subfailure is more specific: even a genuinely pre-action receipt can fail later if its result taxonomy is not exactly compatible with the episode outcome type. That distinction is now preserved rather than collapsed into a generic chronology warning.

## v3 feature assessment

Helpful features were the immutable TaskEpisode substrate, explicit selected/rejected retrieval authority, the separation of search-priority admissibility from theorem authority, local-versus-global obstruction discipline, seven-axis metrology, and especially the derived chronology audit in `pre_action_receipt.py`.

The missing feature exposed here is prospective **type safety** at receipt construction. `PreActionFibreReceipt` structurally accepts arbitrary non-empty `allowed_outcome_branches`, while the later audit compares them exactly to `EpisodeOutcome.value`. It also remains proposal-only and not automatically wired into the mathematical runtime, so application agents must manually construct and audit the pair.

## Concrete framework-improvement hypothesis

Make pre-action outcome branches typed or validated against `EpisodeOutcome` at receipt construction/schema-validation time, or separate custom discriminator-result labels from the episode outcome enum. Then have the canonical mathematical planning/runtime surface emit the receipt automatically before consequential execution and attach its audit result to run-level metrology, without granting any proof, lesson, novelty, gluing or promotion authority. A hidden-world regression should demonstrate that unknown branches fail **before** action execution and that retrospective episodes remain usable for routing/failure learning.

## Next action

Open a fresh child context on the exact join-eliminated `k`-meet representation. Before proposing a `Phi`, freeze its invariance under the normal form/free-union quotient, prove or falsify a universal per-`k` accounting law, and run the existing C010/C021/C023/C024/C025 hostile controls. Use exact `EpisodeOutcome` values in any future pre-action receipt. Root remains open.
