# RAKL_METHOD_CASE_STUDY — O9d12a2a1a1d

## Research method

The cycle used RAKL v3 counterexample-first mathematical research with a frozen graph-space MathContextFiber, dual success/failure memory review, exact-source transfer, a seven-role same-context expert cell, hash-chained public trace, immutable TaskEpisode evidence, and the newer separate `method-telemetry-v1` interpretation record. Search was deliberately routed to the smallest exact zero-cost control before any hard-target scoring.

The pre-candidate fibre was frozen against RAKL main `38a530a52d863513db16052474b85e63fbb488cd` (method 3.0.0, package 0.1.0). During the cycle RAKL main advanced to `8118b9c96cd426439a7e3c4b737d279a047e37f2`; the later commit is explicitly proposal-only and wired into no gate. The frozen pre-candidate chronology was not rewritten after observing that drift. Cycle-close metrology records both SHAs.

## What worked

The accumulated experience substrate changed the *routing pressure* without granting authority. `F-C021-CHEAP-ADJACENCY` made a zero-cost control mandatory; `F-C023-SCALAR-COLLAPSE` blocked score-first scalar intuition; the `O9d12a2a1a1b` failure/episode moved attention from later closure dynamics to the base/shared system; and open PR #113 was used only as an unmerged shadow warning that a candidate should survive free-union normalization.

That routing exposed a very cheap exact falsifier in the root-facing graph context. For `A=R_1`, unions of columns realize every trace subset of `A`, so the free-base trace family is `P(A)` although the counted cyclic intersection cost is zero. This simultaneously kills absolute trace cardinality/entropy and VC-dimension candidates without spending a cycle scoring a purported hard graph.

The v3 separation `episode -> diagnosis -> lesson/obstruction` was useful. The observation is the full-powerset trace at t=0; the diagnosis is that the statistic measures zero-cost base expressivity; the proposal-only reusable lesson is to normalize/condition on the free base before using richness. None of those objects is conflated with retained learning or proof authority.

## What failed

The candidate family failed for a **representation** reason with an exact mathematical counterexample: quotient invariance alone does not remove richness already present in uncharged unions. This is a local candidate failure, not a failure of the graph-cover route as a whole.

The local-to-global **gluing** failure remains separate: there is still no target-relative/incremental `Phi`, no universal theorem bounding its marginal growth per counted intersection, and no explicit graph with a super-logarithmic certified value. Root status therefore remains OPEN.

There was also a **tooling/verification limitation**. The application repository pins an older RAKL commit (`fe47a12c...`), while current main is newer, and the execution container could not clone GitHub because DNS resolution was unavailable. Current framework semantics were therefore read through the GitHub connector and applied manually, but the exact current-framework runtime/schema suite could not be executed locally. This is recorded as `CANNOT_CHECK`, not silently upgraded to a pass.

## Retrieved, rejected, and missed experience

Selected routing evidence: `T-PNP-GNEQ-JOINT-SIGNATURE-CALIBRATION`, `F-C021-CHEAP-ADJACENCY`, `F-C023-SCALAR-COLLAPSE`, `F-C025-FIRST-ORDER-CANONICAL-COLLAPSE`, `F-O9D12A2A1A1B-FIXED-LAMBDA-GLOBAL-STATE-FACTORIZATION`, episode `PNP-O9d12a2a1a1b-GLOBAL-FACTORIZATION-R2-20260811`, and unmerged shadow PR #113.

Rejected: generic Horn lower-bound transfer, generic VC lower-bound transfer, current 2026 circuit-lower-bound results lacking an applicability map, and cross-Millennium import without a DifferenceWitness. Complete retrieval misses are `CANNOT_MEASURE` because neither repository nor literature search was exhaustive.

## Saturation and novelty

Qualitative investigative saturation reopened `OBSTRUCTION`, `RELATION`, and `PATH`: the obstruction sharpened from presentation sensitivity to zero-cost base expressivity; the relation sought is now an incremental per-intersection accounting law; and the path rotated from absolute base scores to normalized/conditional potentials. The absolute base-richness family is flattened for this exact t=0 failure scope.

The locally solved subproblem is defensibly classified **compositional** in shadow novelty taxonomy: it combines the exact source definition with an elementary row/column shattering construction. No novelty certificate was run. All seven retained semantic novelty counts are zero because no protected retention gate ran; the proposed obstruction/lesson is not counted as learning.

## v3 feature assessment

Helpful: immutable TaskEpisode evidence, experience-conditioned routing without authority leakage, explicit local-versus-gluing status, seven-axis metrology, and the newly landed separate `method-telemetry-v1` interpretation object.

Missing/awkward in this run: `method-telemetry-v1` requires numeric before/after values for saturation-axis deltas. When a lane has only a qualitative axis-reopen determination and no materialized canonical numeric saturation vector, the typed record cannot faithfully say `CANNOT_MEASURE` for those deltas. The telemetry object therefore leaves quantitative deltas empty while the metrology receipt records qualitative reopen status.

**Framework-improvement hypothesis:** allow a typed unknown/unmaterialized saturation-delta state (with a mandatory reason) in method telemetry, or permit a qualitative reopen record separately from numeric deltas. This would preserve fail-closed metrology without inventing numbers. This is a hypothesis only and does not mutate framework authority.

## Next action

Open `O9d12a2a1a1e`: define only a target-relative or incremental trace potential that vanishes on the free-union base, and run the cheapest one-intersection hostile controls before any explicit graph scoring. The admission test is a proved marginal accounting inequality under one counted intersection plus arbitrary free unions.
