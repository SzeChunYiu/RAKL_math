# RAKL_METHOD_CASE_STUDY — PNP C041A odd-slice copy

**Cycle:** `PNP-O9d12a2a1b-C041A-ODD-SLICE-COPY-20260811`  
**Authority:** proposal/shadow method evidence only  
**Root:** `OPEN_NO_SOLUTION_CERTIFICATE`

## Research method used

The cycle used current RAKL v3 as an experience-conditioned search controller, not as a theorem authority. The active parent C041 packet already separated six obligations: relevance persistence, lifted dual feasibility, positive augmentation, recurrence/divergence, rate, and root transfer. Current-work binding rejected two live duplicate routes (Wig95 source recovery in PR #179 and restricted/full polarity in PR #162). The same-context failure memory then made `F-C037-ARBITRARY-EXTENSION-NONMONOTONE` decisive: an arbitrary extension was not allowed to inherit old relevance by assumption.

A five-role same-context expert cell was used: fusion/cover complexity, LP duality, adversarial combinatorics, circuit transfer, and formal assurance/metrology. The search policy selected the structurally simplest rule that could isolate the persistence question: embed the complete parent complement on one parity slice and add no new complement points. Counterexample-first verification then asked whether the supposedly helpful persistence actually created fresh capacity.

## What worked

The v3 fibre and failure lattice changed the mathematical decomposition. Instead of trying to optimize a finite LP value or speculate about a recurrence, the cycle first proved the exact interface behavior of one rule. The C037 failure forced explicit relevance transport; the C024 scoped tool supplied the exact dual-load language; the frozen C041 discriminator made `ZERO_AUGMENTATION` a legitimate terminal failure rather than something to patch after observing the result.

This produced a clean local negative theorem: the child cover graph for the pure odd-slice copy rule is isomorphic to the parent cover graph, so integral and fractional cover complexities are unchanged. The same proof isolates why: every off-slice graph edge has an empty generator trace and hence supports no relevant semi-filter. The cycle then converted that failure into a sharper residual: after old dual mass is transported, one-coordinate fresh augmentation is possible exactly when a new relevant semi-filter avoids every saturated lifted-pair neighborhood.

## What failed

The selected operator failed mathematically, not procedurally: it has exact zero augmentation. The failure is local and operator-scoped. It is not evidence that all uniform complement extensions fail, and it is not an upper bound on the parent atom's optimum or recurrence potential.

No local-to-global gluing was attempted beyond auditing the interface. The gluing residual remains open separately: a positive graph-realizable augmentation must be iterated uniformly, quantitatively lower-bounded, shown to diverge at a root-relevant rate, transferred through the source graph/Boolean interface, and connected to NP by a verifier or a lower-bound-preserving reduction.

The concrete child rule was derived before the child fibre/trace artifact was persisted. The pre-existing parent C041 no-rule discriminator was genuinely frozen, but strict child result-blind discovery chronology is therefore not claimed. This is a provenance/chronology limitation, not a mathematical defect in the local proof.

## Failure typing

Primary category: `MATHEMATICAL_LOCAL`.

Not primary: retrieval, decomposition, representation identity, local-to-global gluing, source, verification, tooling, or meta-policy. A chronology limitation is recorded under provenance/verification authority and prevents strict discovery credit, but does not change the mathematical verdict.

Repeated-failure links:

- `F-C037-ARBITRARY-EXTENSION-NONMONOTONE` -> C041A DifferenceWitness: relevance can fail under arbitrary extension, whereas exact copy preserves relevance but gives zero augmentation.
- parent C041 warning `persistence creates no new mass` -> C041A exact operator-level witness.
- `F-C024-FRACTIONAL-INTEGRALITY-GAP` remains active as a separate root-strength warning; C041A does not repair it and does not count a different explicit family as a DifferenceWitness.

## Saturation and novelty

No protected RAKL saturation state is mutated. Retained semantic novelty is zero on all seven axes: `KNOWLEDGE=0`, `OPERATOR=0`, `EXPERIENCE_PATTERN=0`, `OBSTRUCTION=0`, `RELATION=0`, `PATH=0`, `META_METHOD=0`.

At proposal/shadow level, `OBSTRUCTION` and `PATH` are marked reopened because the saturated-neighborhood criterion creates a sharper next child. The pure-copy/relabelling operator family is locally flattened for amplification. Shadow novelty classification is `REPRESENTATION_NOVEL` for the explicit slice-isomorphism obstruction and `COMPOSITIONAL` for the LP residual-slack reduction. Neither classification grants novelty authority.

## What v3 helped with

The strongest v3 contribution was not a proof primitive; it was routing discipline. `TaskEpisode` separation prevented the observed failure from becoming a promoted lesson automatically. The problem fibre made selected/rejected current work explicit. Experience-conditioned routing used C037 to avoid a known bad family. The seven-axis saturation model allowed a new residual to reopen only obstruction/path at shadow level without pretending repository growth was learning. The local/global distinction prevented an exact cover-graph theorem from being mistaken for a root bridge.

## What v3 was missing or could improve

No gate weakness was found and no framework issue is warranted from one episode. A concrete framework-improvement hypothesis is nevertheless testable:

`H-C041A-RESIDUAL-SLACK-COMPILER`: for finite packing/cover amplification atoms, add a proposal-only operator that compiles a verified transported certificate into its saturated-constraint neighborhood and exposes the cheapest admissible fresh-witness discriminator to the next fibre. It must remain search-priority only and may not promote a lesson, theorem, or obstruction without ordinary v3 evidence gates.

The hypothesis is motivated by this episode because the useful next action came only after manually converting transport success plus augmentation failure into a residual-slack object. It should be evaluated against matched tasks before any framework promotion.

## Next action

Open fresh child `O9d12a2a1b-C041B` only after a new context freeze. Seek one non-copy, source-compatible extension that adds off-slice complement structure while preserving old dual feasibility. Before any recurrence calculation, require an exact graph-realizable new relevant semi-filter outside every saturated lifted-pair neighborhood. If that atomic condition fails, rotate again rather than optimizing finite values.