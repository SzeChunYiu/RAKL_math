# RAKL_METHOD_CASE_STUDY — O9d12a2a1a1c

**Episode:** `PNP-O9d12a2a1a1c-FREE-UNION-QUOTIENT-R3-20260811`  
**Authority:** proposal/shadow only.

## Research method

The cycle used current RAKL v3 as an operational search controller rather than as theorem authority. The active residual from O9d12a2a1a1b was atomized one level further: before inventing a scalar on the shared `t`-rule system, identify transformations that cost zero in the exact source complexity measure. Bound failure memory then changed routing from direct scalar design to representation normalization. The source definitions were attacked counterexample-first, followed by an exact replacement proof and a same-context seven-role adversarial review.

Current canonical `method_specs.py` surfaces used operationally were `decomposition`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `equivalence_similarity`, `gap_discovery`, `experiment_query_selection`, `memory`, `review`, `saturation_stopping`, and `prompting_context_policy`.

## What worked

The cost-zero-operation question was both smaller and more discriminating than another target score. Section 2.5 of Cavalar–Oliveira makes unions free for `D^cyc_cap`; replacing any generator from `U(B)` by an acyclic union tree proves `D^cyc_cap(A|B)=D^cyc_cap(A|U(B))`. Theorem 30 then transports the equality exactly to `rho`. This produces a reusable admission test: presentation-sensitive scores must factor through, be uniform over, or be canonicalized over the free-union equivalence class.

The most consequential reuse was not a stored theorem but routing pressure from `F-C010-MULTIPLEXING`, `F-C021-CHEAP-ADJACENCY`, `F-C023-SCALAR-COLLAPSE`, `F-C025-FIRST-ORDER-CANONICAL-COLLAPSE`, and the immediately prior fixed-Lambda factorization episode. Together they prevented a score-first repeat.

## What failed / remains open

No quotient-respecting invariant `Phi` with a proved `Phi<=f(t)` bound and explicit `omega(log N)` target value was found. The local mathematical obstruction closed in this cycle is only presentation inflation under free unions. The root-facing failure is therefore a **gluing failure**, not a failure of the local quotient lemma.

Failure taxonomy:
- mathematical: local lemma succeeded; root lower bound remains absent;
- retrieval: bounded, non-exhaustive; no missed-item rate can be measured;
- decomposition: improved by separating cost-zero normalization from score construction;
- representation: one route pruned (free-union-sensitive presentations);
- gluing: still open from quotient-respecting invariant to explicit target and then unrestricted circuit consequence;
- source: primary-source signature verified;
- verification: same-context only, no isolated-review credit;
- tooling: aggregate token/tool counters unavailable;
- meta-policy: prior failure memory successfully altered action selection.

## Episode -> diagnosis -> reusable obstruction/lesson

- **Episode:** attempted to determine whether candidate shared-rule scores may be defined directly on the current generator presentation.
- **Diagnosis:** the target measure ignores arbitrary union-gate count, so generator presentations related by finite union closure have identical counted complexity.
- **Reusable obstruction/lesson proposal:** `MOTIF-NORMALIZE-FREE-OPERATIONS-BEFORE-POTENTIAL` — before defining a complexity potential, quotient or uniformly control every transformation generated by operations that the target measure counts as zero. This remains proposal/shadow; no protected lesson-promotion gate ran.

This links to the repeated process-failure chain `F-C010 -> F-C023 -> F-O9D12A2A1A1B-FIXED-LAMBDA-GLOBAL-STATE-FACTORIZATION`: each warns against charging semantic/presentation growth without an exact resource-accounting bridge.

## Saturation / novelty

Shadow classification: **representation** (the same complexity is exposed through a coarser free-operation quotient). Retained semantic novelty is zero on all seven v3 axes because no protected retention/promotion gate ran. The **RELATION** and **PATH** axes are reopened at proposal level: the relation `B ~ U(B)` is now the normalization constraint, and the search path is restricted to quotient-respecting invariants. No retained learning is claimed from repository growth.

## v3 feature assessment

Helpful: typed failure memory, explicit routing separation from authority, DifferenceWitness/applicability discipline, saturation reopening, and same-context-vs-independent-review separation. Missing on current `main`: a canonical pre-action/TaskEpisode method-telemetry producer that mechanically records which retrieved item changed routing; the open framework proposal is not treated as current authority.

**Framework-improvement hypothesis:** a proposal-only `cost_zero_equivalence` field in method telemetry could record the exact transformations that must be quotiented before candidate scoring. A matched benchmark would be required before promoting such a framework change; this cycle does not create a framework issue because one application instance is insufficient evidence of a systemic defect.

## Next action

Stay on the cover/cyclic lane. Enumerate a very small basis of invariants defined on the free-union quotient, reject any that fail multiplexing or cheap-target controls, and only then attempt an explicit graph lower bound. Root remains `OPEN_NO_SOLUTION_CERTIFICATE`.
