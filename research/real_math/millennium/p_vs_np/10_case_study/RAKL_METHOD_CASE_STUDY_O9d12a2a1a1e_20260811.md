# RAKL_METHOD_CASE_STUDY — O9d12a2a1a1e

## Method used

RAKL v3 was used operationally as an experience-conditioned research controller: current `TaskEpisode` semantics were read first; the parent failure episode changed routing from “score a hard graph” to “prove the smallest accounting lemma, then run hostile gluing controls.” The cycle froze a new MathContextFiber, bound primary-source cyclic semantics, separated local proof from transport/gluing, and preserved a hash-chained public trace plus proposal-only episode evidence.

## What worked

The base-normalized trace entropy representation repaired the parent's zero-cost blow-up **locally**. The union-closure doubling operator proved an exact `≤1` marginal for one immutable added set. Counterexample-first review then found the terminal-target invisibility control before any hard-target search. The v3 separation `Episode -> Diagnosis -> Lesson` prevented the local success from being silently upgraded into a reusable theorem or root bridge.

## What failed

No local mathematical failure remains for the one-added-set lemma. Two distinct failures remain:
- **gluing/representation:** target membership does not force positive trace entropy when `Γ` is already free-union reachable;
- **context-translation/gluing:** immutable set addition cannot be identified with a cyclic fixed-point intersection gate without a separate transport theorem.

This was not a retrieval, source, verification, or tooling failure. Same-context role separation is not independent review.

## Prior experience effect

Selected failures `F-O9D12A2A1A1D-ZERO-COST-BASE-RICHNESS-SHATTERING` and `F-C023-SCALAR-COLLAPSE` changed action ordering. `GENERIC-VC-LOWER-BOUND-TRANSFER`, cross-Millennium import, and direct cyclic-gate/add-set identification were retrieved or considered and rejected for explicit DifferenceWitness reasons. Complete retrieval recall cannot be measured.

## Saturation / novelty

The `OBSTRUCTION` and `PATH` axes reopened: a paired Lipschitz-plus-target-forcing requirement is now the active discriminator, while the absolute-richness family remains flattened. Shadow novelty class for the solved local atom is `compositional`; retained novelty is zero absent protected retention authority.

## v3 feature assessment

Helpful: immutable episode evidence, experience-conditioned routing without authority, explicit local-versus-gluing separation, DifferenceWitness discipline, and seven-axis saturation accounting.

Missing/awkward: the current substrate has no first-class typed field for **two-stage mathematical success** where an operator is locally valid but its theorem-to-target transport fails independently.

## Framework-improvement hypothesis

Add a proposal-only `BridgeObligation` view keyed by `(local_claim_id, source_context, target_context)` with separate `local_verdict`, `transport_verdict`, `target_forcing_verdict`, and `DifferenceWitness`. This would reduce repeated process failures where a valid local inequality is accidentally treated as a target lower-bound coordinate. No framework change is proposed or promoted in this application cycle.
