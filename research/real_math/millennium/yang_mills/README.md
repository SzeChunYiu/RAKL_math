# RAKL Verified Discovery — Yang–Mills existence and mass gap

This directory is the persistent strict-assurance workspace for the Yang–Mills Millennium problem.

## Root status

`OPEN_NO_SOLUTION_CERTIFICATE`

The authoritative target is the Jaffe–Witten Clay problem description: for every compact simple gauge group, construct a nontrivial quantum Yang–Mills theory on `R^4` satisfying axioms at least as strong as the cited Wightman/Osterwalder–Schrader framework and prove a finite positive mass gap above the vacuum.

Fixed-cutoff lattice control, strong-coupling convergence, Wilson area law, stochastic functional inequalities, numerical glueball spectra, or a transfer matrix at one lattice spacing do not by themselves close the root problem.

## Active lanes

- **constructive-continuum:** Euclidean measure, continuum limit, reflection positivity, OS/Wightman reconstruction, nontriviality, correlation decay.
- **spectral/lattice-RG:** transfer matrices/Hamiltonians, physical-unit spectral gaps, confinement-versus-spectrum separation, weak/strong coupling, multiscale RG transport, thermodynamic and continuum uniformity.

Persistent control issue: `SzeChunYiu/RAKL_math#5`. Historical `SzeChunYiu/RAKL#85` and duplicate `#87` are migration provenance only.

## Strict discovery rule

For every materially new candidate, use current `SzeChunYiu/RAKL@main` and satisfy the exact pre-candidate order

`ATOMIZED -> CONTEXT_FROZEN -> ANALOGY_SCAN -> METHOD_TRANSFER_REVIEW -> EXPERT_CONTEXT_REVIEW -> EXPERIENCE_MEMORY_REVIEW -> NEXT_STEP_PROPOSED`

with a frozen `MathContextFiber`, dual success/failure `ResearchMemoryReview`, same-context expert review, and hash-chained `MathResearchTrace`.

Breakthrough-learning modes and cross-domain analogies are proposal-only. Same-context expert roles are not independent review. Computation is not proof.

## Constructive-continuum state

Parent atom `YM-E1a` asks for observable-level cutoff removal for pure 4D Yang–Mills: obtain cutoff-uniform control of renormalized gauge-invariant observable expectations strong enough to extract an OS-compatible, non-trivial compact continuum limit.

Its non-candidate observable-interface calibration is complete. The retained architecture is:

- a **source-marked generating-functional wrapper** whose derivatives expose observable insertions and mixing to the RG;
- **Wilson-loop / loop-cylinder observables** as the fixed-cutoff reflection-positive gauge-invariant core probe, with continuum loop renormalization still explicit;
- **positive-flow-time gauge-invariant composites** only as UV-regular diagnostic/non-triviality probes until separate reflection-support and `t -> 0` locality bridges are proved.

### Active constructive atom — `YM-E1a1`

`YM-E1a1` asks whether one exact Balaban-style 4D pure-gauge RG blocking step can carry a deliberately small positive-half-space gauge-invariant source family through a marked/source sector with explicit mixing, bounded support enlargement and a source-derivative norm loss that does not already depend catastrophically on total ultraviolet depth.

A fresh strict pre-candidate packet is frozen on the current research branch. It imports the sibling failure `F-YM-S1A-RESTRICTED-SOURCE-HIDDEN-STATE` only as a scope warning: a finite source family may calibrate one-step closure but cannot be promoted to state/spectral completeness without a separate density/cyclicity theorem.

The registered next action is to freeze a **marked one-step closure contract** with five outputs:

1. exact finite-cutoff source-derivative transport identity;
2. generated operator/source labels;
3. support enlargement relative to a buffered reflection half-space;
4. mixing matrix plus typed remainder;
5. one-step marked norm-loss coordinate.

Only after that contract is frozen may the lane generate the narrowest closure-or-leakage candidate. Exact finite closure, graded/quasi-local closure with controlled remainder, and a negative proliferation result remain distinct branches.

## Current spectral atom

`YM-S1` asks for the exact quantitative bridge obligations needed to transport finite-lattice transfer-matrix/correlation information to a nonzero finite **physical** continuum mass gap.

The spectral lane separately tracks reflection-positive transfer matrices, fixed-cutoff estimates, thermodynamic uniformity, source-family spectral completeness, RG transport, physical-unit lattice-spacing scaling, continuum convergence and OS/Wightman reconstruction. Its source-visibility failure must not be silently generalized beyond its registered scope.

## Authority

No continuum measure, OS reconstruction, non-trivial 4D Yang–Mills theory, correlation-decay theorem, positive mass gap, novelty claim, or root solution certificate is currently authorized.
