# RAKL_METHOD_CASE_STUDY — YM-E4a / Faizal–Shabir Proposition 8.8

**Framework source of truth:** `SzeChunYiu/RAKL@bd1a2768f0f474ff44ffa25243241f94bfaf6466` current `main` at cycle start.  
**Application base:** `SzeChunYiu/RAKL_math@24194bba4c88cc4be19dad03d59bfa79599d5ee9`.  
**Atom:** `YM-E4a`.  
**Fibre snapshot:** `sha256:f948cce69ea6296ca3e4ebc7113c26e3789fa61ffb19585e9ca36b6ad91457aa`.  
**Authority:** `V3_METHOD_CASE_EVIDENCE / PROPOSAL_SHADOW / ROOT_AUTHORITY_NONE`.

## Research method

The cycle used failure-guided source verification. Current v3 memory exposed the pre-existing `G4` source-family spectral-visibility obstruction and the locally derived `YM-S1a1-C001` repair interface. That changed routing from a broad scan of a 593-page claimed constructive solution to an early audit of every point where sourcewise Euclidean clustering was upgraded to a full Hamiltonian gap.

Canonical `method_specs.py` surfaces used operationally were:

`decomposition`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `equivalence_similarity`, `mathematical_context_translation`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `experiment_query_selection`, `memory`, `review`, `synthesis`, `saturation_stopping`, and `prompting_context_policy`.

No Python import/execution of `method_specs.py` occurred in the connector runtime; the names above identify the canonical process surfaces used by the research workflow.

## What worked

The prior hidden-state failure changed the action **before accepting the current source claim**. Proposition 8.8 was tested against the stored DifferenceWitness: a source can decay rapidly while a lower-energy state remains orthogonal to its cyclic subspace. The exact three-level model falsifies the proposition's fixed-source global inference.

Cross-checking the same preprint found Theorem E.4, which uses the stronger and structurally correct shape: one common exponential bound for every centered gauge-invariant local observable plus density of the corresponding vectors. That matches the stored `YM-S1a1-C001` interface. This is successful reuse, not a new mathematical operator or obstruction.

## What failed / remains open

The audited Proposition 8.8 does not by itself support its global-gap conclusion. Theorem 8.9's common-gap sentence depends on Proposition 8.8 and is therefore locally blocked until a family-complete repair is supplied.

The cycle does not establish whether the source's stronger Theorem E.4 is fully valid in the claimed Yang–Mills construction. In particular, the gauge-invariant source algebra must be dense/cyclic in the **physical** OS excited space of the same theory, and the common exponent must remain uniform under the parameter, volume, regulator and continuum operations actually used.

## Failure classification

Primary: `SOURCE / VERIFICATION`.  
Structural: `LOCAL_TO_GLOBAL_GLUE / SOURCE_COMPLETENESS_INTERFACE`.  
Not classified as: new abstract spectral-mathematics failure, new obstruction class, or root refutation.

## Episode -> diagnosis -> obstruction/lesson separation

- Episode: `EP-YM-E4A-FS-PROP88-20260811`.
- Diagnosis: `DIAG-YM-E4A-FS-PROP88-SOURCE-TO-GLOBAL-20260811`.
- New scoped failure record: `FAIL-YM-E4A-FS-PROP88-SOURCE-COMPLETENESS-20260811`.
- Reusable obstruction: **none new**; reuse `G4` / `F-YM-S1A-RESTRICTED-SOURCE-HIDDEN-STATE`.
- Reusable lesson: **none new**; reuse `YM-S1a1-C001`.
- Successful reuse: `REUSE-YM-G4-FS-PROP88-20260811`, `REUSE-YM-S1A1-C001-FS-E4-20260811`.

## Saturation and novelty

Retained semantic novelty, conservatively:

- `KNOWLEDGE = 1`: new source-scoped proof-interface diagnosis for Proposition 8.8.
- `OPERATOR = 0`.
- `EXPERIENCE_PATTERN = 0`: existing hidden-state pattern reused.
- `OBSTRUCTION = 0`: G4 already canonical.
- `RELATION = 1`: new applicability relation from the current source claim to G4 / the dense-source repair interface.
- `PATH = 0`: successful reuse is recorded separately; no new strategy composition is minted.
- `META_METHOD = 0`.

Reopened axes: `KNOWLEDGE`, `RELATION`. Flattened/unchanged: `OPERATOR`, `EXPERIENCE_PATTERN`, `OBSTRUCTION`, `PATH`, `META_METHOD`.

Defensible RAKL novelty class: `TRANSFER` — a stored obstruction/repair interface was transferred to a current primary-source claim. This is not a theorem-novelty claim.

## Expert cell

The six same-context roles separately covered OS reconstruction, gauge-invariant physical source algebra, spectral theory, RG/continuum uniformity, adversarial source verification, and v3 assurance/metrology. Their consensus supports only the local source-interface diagnosis. They are not independent reviewers.

## Falsifier / verification

Falsifier: `H=diag(0,epsilon,M)` with `0<epsilon<m_*<=M` and `A Omega=e_high`. The tested correlator decays with rate at least `m_*`; the full gap is `epsilon`.

Verification: exact primary-source selectors, PDF screenshot of Proposition 8.8, analytic hostile model, comparison to stored dense-source lemma, source-internal comparison to Theorem E.4, and same-context expert review.

## Framework-improvement hypothesis

No new framework defect is opened. The relevant v3 feature worked: prior failure retrieval changed routing and prevented duplicate obstruction minting. A future metrology improvement remains to quantify **successful obstruction reuse that yields a new source applicability relation** separately from both novelty gain and raw retrieval count. This run records that distinction explicitly rather than treating reuse as new `EXPERIENCE_PATTERN`, `OBSTRUCTION`, or `PATH` novelty.

## Next action

Issue `#109` freezes the prospective repair. It must bind a family-complete gauge-invariant source theorem or a full vacuum-orthogonal semigroup bound in the same reconstructed theory, with parameter/volume/continuum uniformity, before downstream gap claims are reconsidered.

Root remains `OPEN_NO_SOLUTION_CERTIFICATE`.
