# RAKL_METHOD_CASE_STUDY — Yang–Mills `YM-E4b` R1

**Cycle date:** 2026-08-11  
**RAKL authority read first:** `SzeChunYiu/RAKL@7a42ed03d5062851b706871f1b33cacc13753ab1`, incumbent method `3.0.0`  
**Application base:** `SzeChunYiu/RAKL_math@c48293364c6bdf7e0c5d93f5e01d889008c9eb61`  
**Active atom:** `YM-E4b` / `YM-E4b-FS-OS-CONTINUUM-IDENTIFICATION-STRONG-CONVERGENCE`  
**Fibre:** `sha256:56093cb6547bca98774743a05caa0143aee2255359e3805cb44ba8a3ed5d7832`  
**Authority:** `V3_METHOD_CASE_EVIDENCE / PROPOSAL_SHADOW / NO_FRAMEWORK_PROMOTION / ROOT_AUTHORITY_NONE`

## Research method

The cycle used a source-internal proof-repair strategy rather than another broad Yang–Mills construction search:

`memory-first route selection -> exact primary-source claim extraction -> hostile local falsifier -> source-internal near-solved analogue -> independent local derivation -> changing-space/gluing audit -> same-context expert cell -> saturation update`.

Canonical `method_specs.py` surfaces used where applicable:

`memory`, `routing`, `source_selection_reliability`, `claim_extraction`, `contradiction_diagnosis`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `experiment_query_selection`, `review`, `saturation_stopping`.

`authority_promotion` was inspected conceptually but not invoked: the evidence does not satisfy promotion or root gates.

## Atom / actual fibre / decision policy

The pre-existing issue `RAKL_math#126` supplied the prospective atom and hostile controls before this run. The actual fibre then included root issue `#5`, exact root contract, `#126`, source-family boundary `#109`, same-theory OS gluing `#88`, dense-source local result `PR#79/#62`, current Faizal–Shabir audit history `PR#112/#97`, saturated source-acquisition history `PR#103/#116`, RAKL `#124`, current v3 architecture/metrology/method specs, the exact Faizal–Shabir PDF pages, and Kuwae–Shioya only as a structural changing-space analogue.

Decision policy: attack the smallest interface whose resolution could change the status of the claimed continuum spectral bridge. Because content-bearing source text was available, do not revisit the saturated Bałaban metadata/source-acquisition family. Because `#109` already owns source completeness, do not mint another hidden-state obstruction.

## Methods/operators/motifs tried

- **Source extraction:** compare the main-text Theorems 8.3/8.4 with Appendix E.1/E.2.
- **Counterexample-first falsification:** test the claimed isometry against `T(t)=exp(-tH)` and positive spectrum; test the finite-net step against infinite-dimensional Hilbert geometry.
- **Structural analogue:** compare the target interface to changing-Hilbert-space spectral convergence à la Kuwae–Shioya with an explicit DifferenceWitness.
- **Source-internal near-solved analogue:** use the main text's `t/2t` polarization argument as a candidate repair of Appendix E's weak-to-strong step.
- **Root-bridge audit motif:** reuse `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` only as research-control search policy. It transferred no literal mathematics and no theorem authority.

## Prior experience retrieved, selected, rejected and missed

**Selected:** `#126`, `#109` / `F-YM-S1A-RESTRICTED-SOURCE-HIDDEN-STATE`, `#88`, `PR#79/#62`, `PR#112`, `PR#97`, and the conditionally reusable root-bridge audit as search control.

These changed routing by separating three interfaces that might otherwise be conflated: source completeness, local semigroup convergence, and regulator-to-continuum Hilbert-space gluing.

**Retrieved but rejected:** `YM-E1a1a0` source-acquisition cycles because this atom has content-bearing primary text; `YM-E2a/#73`, `YM-E2b/#92`, `YM-E3a/#93` because their quantitative RG/gap defects are different active coordinates; `XM008` because source substitution is not the current bottleneck; candidate expertise chunk `E-XM-ROOT-CRITICAL-COORDINATE-AUDIT-001` because its retrieval-maturity gate was previously blocked.

**Missed prior experience:** `CANNOT_MEASURE`. There is no closed coverage-certified retrieval universe for all current proposal/draft/canonical application memory. Bounded repository search found no additional exact E4b episode; no completeness claim is made.

## What worked

The strongest success was **source-internal repair by decomposition**. The Appendix E proof fails at isometry/finite-net steps, but the main text already contains a mathematically sound local mechanism: asymptotic inner-product preservation plus `t/2t` semigroup matrix elements lets polarization prove strong convergence on a suitable dense domain.

RAKL v3 helped by keeping local mathematical validity separate from global solution authority. `contextual_theory_gluing` prevents the valid local operator lemma from being counted as continuum-gap closure. The experience/failure boundary also prevented the already-known source-completeness failure from being duplicated as new learning.

## What failed

1. **Local mathematical/source proof failure:** Appendix E's proof-level isometry claim is too strong; if true globally for `e^-tH`, it forces `H=0`. Its finite-net weak-to-strong argument also fails in infinite dimension.
2. **Representation/gluing failure:** the source moves from asymptotic maps on a dense domain to exact isometries/common Hilbert-space identifications without a supplied bridge. This is the live blocker after the local repair.
3. **Verification/tooling failure:** PDF screenshot rendering failed twice with a cache miss. Parsed primary-source text with exact page selectors remained available; no image-dependent statement was used.
4. **Not a retrieval failure:** the exact primary technical source was available.
5. **Not a source-family-completeness discovery:** that obstruction was already stored and was deliberately not recounted as novelty.

## Falsifier / verification

- spectral-theorem proof of `isometry(e^-tH) => H=0`;
- infinite-dimensional no-finite-net control;
- direct algebraic verification of the `t/2t` polarization lemma;
- exact source cross-check of the main/appendix interfaces;
- seven same-context role-separated reviews;
- root-coordinate audit against the official application contract.

No numerical evidence is used as proof.

## Outcome and residual transformation

**Outcome:** `PARTIAL_LOCAL_RESULT__REPRESENTATION_GLUING_BLOCKED`.

**Residual before:** repair the OS contraction semigroup and show a valid varying-Hilbert-space regulator-to-continuum convergence path carrying the physical spectral gap.

**Residual after:** the local strong-semigroup implication is no longer the smallest mathematical unknown: it has a valid conditional lemma. The active residual is now the target-specific representation/gluing certificate:
- coherent/translation-compatible approximation maps;
- exact regulator-dependent OS null-space/quotient handling;
- a legitimate changing-space convergence notion;
- global/resolvent extension without invented isometries;
- vacuum and spectral-projection/gap transport in the same physical theory;
- volume/lattice-spacing/regulator/gauge uniformity.

This is explicitly a **local-to-global/gluing residual**, separate from local mathematics.

## Saturation and novelty

Retained internal semantic novelty, before any independent novelty audit:

- `KNOWLEDGE = 1`: exact local fact that global isometry of the positive self-adjoint OS semigroup forces zero Hamiltonian, plus the verified conditional repair lemma treated as one scoped retained result packet.
- `OPERATOR = 0`.
- `EXPERIENCE_PATTERN = 0`: the local-success/global-gluing pattern already existed.
- `OBSTRUCTION = 0`: `#126` already froze this obstruction class.
- `RELATION = 1`: the source's main-text polarization argument is identified as a valid local repair of the appendix weak-to-strong step while the later exact-isometry bridge remains unlicensed.
- `PATH = 1`: route switches from “repair generic weak-to-strong/common-space proof” to “prove changing-space OS identification hypotheses and then reuse the validated dense-domain semigroup lemma.”
- `META_METHOD = 0`.

Reopened axes: `KNOWLEDGE`, `RELATION`, `PATH`. Flattened: none. No single same-route cycle can certify flatness.

**RAKL novelty class for the solved local subproblem:** `compositional`. The proof combines standard self-adjoint semigroup calculus and polarization; no new representation/operator/ontology authority is claimed.

## Episode -> diagnosis -> obstruction/lesson

- **Episode:** `YM-E4b-EP-OS-VARYING-HILBERT-20260811-R1`.
- **Diagnosis:** local semigroup convergence has a valid conditional proof, but the source lacks the changing-space representation witness needed to instantiate it and carry gap information globally.
- **Reusable obstruction:** none new; link to existing `#126` and the broader preservation/gluing process family under RAKL `#124`.
- **Reusable lesson/tool:** none promoted. Experience continues to guide search only.

## Framework feature assessment

Helpful v3 features:
- immutable TaskEpisode evidence and explicit observation/diagnosis separation;
- problem fibre with selected/rejected memory;
- `contextual_theory_gluing` local/global boundary;
- experience-conditioned routing without epistemic promotion;
- seven-axis saturation/metrology;
- same-context review explicitly barred from independent-review credit.

Potentially missing high-leverage representation: a typed proposal-only **ChangingSpaceIdentificationCertificate** specialization of `mathematical_context_translation` + `contextual_theory_gluing`, with fields for source/target Hilbert spaces, quotient/null spaces, map direction/domain, asymptotic norm/inner-product control, semigroup/form intertwining, vacuum identification, spectral-coordinate preservation, uniformity and failure witnesses.

**Framework-improvement hypothesis:** benchmark such a certificate prospectively before proposing a new primitive. This cycle alone does not establish a framework defect, and RAKL `#124` already owns the broader root-coordinate-preservation hypothesis, so no duplicate framework issue is opened.

## Repeated process-failure linkage

This episode links to the already observed RAKL `#124` pattern: a locally meaningful result does not advance the root until a preservation/gluing edge is proved. Within Yang–Mills specifically, `PR#112` showed fixed-source decay cannot be promoted to a full spectral gap; this cycle shows dense-domain semigroup convergence cannot be promoted to a continuum Hamiltonian comparison without a changing-space identification witness. The mathematics differs; the reusable process pattern is only local-to-root interface discipline.

## Root/gate status

`OPEN_NO_SOLUTION_CERTIFICATE`.

No proof-DAG closure, formal verifier, isolated recheck, bounded novelty closure, or three isolated mathematical reviews are present. No protected lesson/tool/framework/root promotion is attempted.
