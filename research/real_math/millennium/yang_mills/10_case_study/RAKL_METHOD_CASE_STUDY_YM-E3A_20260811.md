# RAKL_METHOD_CASE_STUDY — YM-E3a nonlinear recurrence repair

Authority: `MEASUREMENT / PROPOSAL-SHADOW EXPERIENCE ONLY`. This is a method case study, not evidence that the Yang–Mills root is solved.

## Atom and signature

Active atom: `YM-E3a`, issue #93.

Problem signature: repair and source-bind the nonlinear recurrence used in Faizal–Shabir Appendix A / Theorem A.9 while respecting the source's local polymer-norm domain; keep source-level and continuum/OS/mass-gap gluing obligations explicit and separate.

Frozen fibre: `YM-E3A_FROZEN_FIBRE_20260811.json`, `sha256:97f8475b86180fb0e3eb747b519e385053863baffc5ce46bb19472609f24f24a`.

The freeze is shadow chronology only: issue #93 already contained the invariant-ball repair direction, and same-run source inspection occurred before repository serialization. No preregistered/pre-insight novelty credit is claimed.

## Method and decision policy

The cycle used current RAKL v3 at `fe47a12c4bad8253658baaf37e1300cab15d0823`, method version `3.0.0`, in proposal/shadow mode. The application repository's stored framework pin remained older and was not silently upgraded.

Decision policy: review the exact root and active failure lattice; prefer the smallest source-bound obstruction with a cheap hostile falsifier; rotate away from a family whose recent source-acquisition attempts were already saturated; treat prior episodes as routing evidence only; stop immediately at any source or local-to-global gluing boundary not verified in the same context.

Canonical `method_specs.py` process surfaces actually invoked: `decomposition`, `routing`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `contradiction_diagnosis`, `gap_discovery`, `experiment_query_selection`, `synthesis`, `memory`, `review`, `contextual_theory_gluing`, `authority_promotion`, `saturation_stopping`, and `prompting_context_policy`.

## Expert cell and delegated checks

A same-context seven-role cell was used: nonlinear discrete dynamics; constructive RG/polymer norms; constructive QFT/OS; asymptotic freedom; adversarial proof audit; formal assurance; source/novelty audit. The cell delegated recurrence algebra, source-domain transfer, continuum/OS gluing, AF interpretation, counterexample search, executable checks, and provenance/novelty discipline. It receives **zero independent-review credit**.

## Retrieved, selected, rejected, and missed experience

Selected experience:

- `ISSUE-93-YM-E3a`: canonical prospective success contract.
- `YM-E2E3-EP-FS-PROOF-AUDIT-R2-20260811T1304Z`: proposal-only prior episode; it changed routing by exposing the A.10 defect and opening this child atom.
- `ISSUE-5-YM-ROOT`: exact root and closure gate.

Retrieved but rejected for this atom:

- `YM-E1a1a0-family`: saturated primary-source acquisition blocker; another pass had low expected information and did not address the recurrence.
- `YM-S1a2-family`: spectral/OS transfer gluing, a different downstream context.
- `YM-E2a-family`: defect-budget sibling, not needed for the current falsifier.
- `YM-E2b-family`: variable-scale/fixed-time gap transport sibling, not needed for the current falsifier.

Missed prior experience cannot be enumerated because no complete memory-coverage receipt/enumerator was exposed in this run. Cross-Millennium memory was not queried: an exact same-problem failure episode was already available, so no transfer claim or DifferenceWitness was needed.

## Operators and motifs tried

Operators/motifs: counterexample-first falsification; invariant-ball induction; discrete variation of constants; strict zero-floor contraction; source-bound transfer assumptions; local/global gluing firewall; same-context adversarial review; executable deterministic checks.

## Falsifier and verification

The literal no-smallness scalar lemma was tested against equality recurrence `rho=1/2`, `C=1`, `r=x_0=2`. It gives `x_1=5`, `x_2=27.5`, while the claimed A.10-form bound at `n=2` is `8.5`. The literal statement is therefore false without an invariant-domain hypothesis.

The repaired local proposition was verified algebraically: with `q=rho+C r_*<=1`, `[0,r_*]` is invariant; with `q<1`, `x_n<=q^n x_0`. The sufficient choice `r_*<=(1-rho)/(2C)` yields `q<=(1+rho)/2<1`. An executable test preserves the counterexample and representative strict-invariant cases.

Primary-source parsed text was available. Visual PDF screenshot verification was attempted but failed with a transient web PDF cache miss; that is recorded as a tooling verification failure rather than erased.

## Outcome and residual

Outcome: `LOCAL_REPAIR_VERIFIED_CONDITIONALLY_SOURCE_BOUND_ROOT_OPEN`.

Local mathematical residual after the cycle: source-bind the actual FRD/polymer `rho,C,r_0`; prove the weak-coupling/bare-coupling entry reaches `r_*` without circularly assuming contraction; propagate the corrected estimate through exact Theorem A.9 and any asymptotic-freedom/universality identification.

Local-to-global/gluing residual, recorded separately: nontrivial continuum Euclidean measure; gauge invariance/fixing compatibility; reflection positivity under limits; finite/infinite-volume and continuum-limit uniformity/interchange; OS reconstruction; correlation decay to the physical Hamiltonian spectrum in the same limiting theory; strictly positive mass gap; all compact simple gauge groups.

## Failure classification

- `MATHEMATICAL_LOCAL`: the literal scalar Lemma A.10 lacks a necessary invariant/smallness condition and has an explicit counterexample.
- `SOURCE`: the repaired scalar statement is not yet source-bound to all exact RG constants/hypotheses needed downstream.
- `GLUING`: recurrence repair does not establish continuum, OS, nontriviality, or physical-gap obligations.
- `VERIFICATION_TOOLING`: PDF screenshot cache miss prevented visual-page verification in this run, although parsed primary text was available.
- `RETRIEVAL/DECOMPOSITION/REPRESENTATION`: no new failure observed; v3 routing improved on the prior cycle by avoiding another saturated source-acquisition pass.
- `META_POLICY`: no gate weakening; chronology caveat blocks prospective discovery credit.

## Seven saturation axes

Before this cycle, the selected `YM-E3a` atom was fresh, while `YM-E1a1a0` source acquisition was flattened/saturated. This cycle reopens only scoped semantic axes for `YM-E3a`:

- `KNOWLEDGE`: reopened — literal counterexample and strict invariant-ball consequence were retained as scoped facts.
- `OPERATOR`: flattened/no new retained operator — invariant-ball induction and variation-of-constants are standard operators, merely applied here.
- `EXPERIENCE_PATTERN`: flattened/no new retained pattern — no new general cross-episode pattern is promoted from one case.
- `OBSTRUCTION`: flattened for retained learning — a proposed local failure object is recorded but not counted as learned obstruction merely because it was created.
- `RELATION`: reopened — explicit relation between source local domain, invariant radius, and strict zero-floor decay; separate relation that the A.59-form positive floor does not imply decay.
- `PATH`: reopened — conditional repair path `r_* <= min(r_0,(1-rho)/(2C))` under source-bound constants and noncircular entry.
- `META_METHOD`: flattened — no retained new method claim.

The old `YM-E1a1a0` source-retrieval family remains flattened; it was not reopened by this cycle.

## Novelty class

At most `COMPOSITIONAL`: issue #93 already stored the invariant-ball repair direction. The retained contribution is an explicit literal counterexample, complete scoped invariant-ball proof, and separation of the positive-floor bound from genuine geometric decay. This is not claimed as publication-level mathematical novelty and gives no root authority.

## What worked / what failed

Worked: v3 episode-guided routing changed the chosen atom away from a saturated external-source blocker; the exact active issue made the falsifier cheap; counterexample-first testing strengthened the diagnosis from “printed induction invalid” to “literal no-smallness statement false”; the gluing firewall prevented a scalar recurrence repair from being inflated into an asymptotic-freedom, continuum, OS, or mass-gap result.

Failed/blocked: exact source-level constants/domain and noncircular entry remain unverified; visual PDF screenshot verification failed transiently; no full memory-coverage receipt exists for proving what relevant experience may have been missed.

## Framework-improvement hypothesis

The current v3 experience substrate materially helped by separating episode -> diagnosis -> reusable obstruction/lesson and by making saturation/routing explicit. A still-useful improvement would be a **mandatory pre-action retrieval-coverage receipt** that records the enumerated candidate memory set, selected/rejected IDs, coverage gaps, and the action preference before and after retrieval. That would make “RAKL changed the route” attribution and missed-memory rates directly measurable instead of partially reconstructive. This hypothesis overlaps the current v3 open memory-coverage/metrology gap and therefore does not justify a duplicate framework defect issue in this cycle.

## Authority

`PROPOSAL_SHADOW_ONLY / SAME_CONTEXT_REVIEW_ONLY / ROOT_OPEN_NO_SOLUTION_CERTIFICATE`.
