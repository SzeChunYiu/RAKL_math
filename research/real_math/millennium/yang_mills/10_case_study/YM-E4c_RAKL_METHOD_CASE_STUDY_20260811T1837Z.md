# RAKL_METHOD_CASE_STUDY — YM-E4c O(4) restoration repair

**Authority:** `V3_PROPOSAL_SHADOW_METHOD_EVIDENCE / NO_THEOREM / NO_ROOT_AUTHORITY`  
**Framework read directly:** `SzeChunYiu/RAKL@3299072b410ac9136548dfd103e846fc7656c31e`, method `3.0.0`, package `0.1.0`.  
**Application base:** `SzeChunYiu/RAKL_math@812addd25a7f34d3c6272143e21d5d7db34539aa`.  
**Atom:** `YM-E4c / YM-E4c-FS-O4-RIEMANN-SUM-NORMALIZATION`.  
**Fibre:** `sha256:29ce4b6423fd90604b6effc5d686ac9aaf7dcc6399455a0efe17c9bd41cd1de1`.

## Research method used

The cycle used current-v3 memory/routing and saturation control to rotate away from a repeatedly exercised zero-novelty source-acquisition family and choose a fresher same-theory root-facing OS1 repair. It then combined four bounded operators: exact primary-equation dependency composition, configuration-space domain type-checking, counterexample-first generic-rotation falsification, and same-theory gluing audit.

The durable prospective contract was already frozen in issue #141 before the source probe. The cycle did not manufacture an earlier file-level pre-action receipt after the fact.

Canonical current `method_specs.py` process surfaces actually invoked were `memory`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `contextual_theory_gluing`, `gap_discovery`, `review`, and `saturation_stopping`.

## Actual fibre and experience routing

The fibre was intentionally narrow: the exact Clay-strength root contract; issue #141's O(4)-repair success contract; current Faizal-Shabir arXiv v1 pages/equations around Proposition 4.6; the finite-lattice configuration-space definition; and prior application memories needed to avoid duplicative routing.

Prior experience that changed routing:

- `YM-E1a1a0-EP-SOURCE-20260811T1424Z`, PR #103 and PR #116 showed repeated content-blocked metadata/source acquisition with zero retained semantic novelty. These changed the preferred action away from another Bałaban metadata search.
- The current issue #141 became the selected same-theory repair target because it was fresh, root-facing, and had a cheap exact falsifier.

Retrieved but rejected:

- `F-YM-S1A-RESTRICTED-SOURCE-HIDDEN-STATE`: source-family spectral visibility, not Euclidean symmetry.
- issue #126: OS contraction/varying-Hilbert interface, downstream and different.
- issue #133 and draft PR #140: OS quotient/relative-form domination, a different gluing mechanism.
- issue #92: physical-time spectral normalization, different coordinate.
- issue #93: weak-coupling recurrence, different mechanism.
- PR #85 was redundant with the later zero-novelty source-acquisition episodes and did not supply additional routing authority.

Missed prior experience remains `CANNOT_MEASURE`: current RAKL itself lists cross-problem memory coverage as an open upgrade gap, and this connector run had no governed completeness receipt for the entire application-memory universe.

No cross-Millennium mathematical transfer was selected; therefore no cross-problem applicability claim was made from incidental search results.

## What worked

The arithmetic falsifier was decisive and cheap. Reading the literal factors in equations (4.47) and (4.49) preserves the already-frozen `a_k^{-3}` residual and blocks the jump to equation (4.50) unless a missing normalization/cancellation is supplied.

More importantly, the representation-domain check exposed a second, independent source-proof obstruction. The source defines gauge variables on coordinate bonds of a hypercubic lattice, but Proposition 4.6 invokes `g^{-1}·A` and exact finite-lattice `O(4)` invariance for arbitrary `g in O(4)` without source-binding an interpolation or approximate action. A generic rotation outside the finite hypercubic point group is an immediate hostile control.

This check improved the residual from one scaling question to two jointly necessary coordinates:

1. normalized defect-density scaling; and
2. a typed arbitrary-rotation comparison map on the same lattice gauge/observable theory.

The expert cell agreed that these are local source-proof failures whose root-facing consequence is a separate local-to-global OS1 gluing failure. No member treated the same-context cell as independent review.

## What failed

### Mathematical/source proof

- The source's displayed Riemann-sum chain does not produce the claimed vanishing rate as written.
- The arbitrary `O(4)` action used in the finite-lattice comparison is not source-bound on the stated link-field configuration space.

### Gluing

Even a local repair has not yet been shown to live in the same regulator/gauge/observable family and the same continuum subsequence used for OS reconstruction. Therefore the continuum OS1 bridge remains open.

### Retrieval/source

No primary-source repair for either missing coordinate was located in the bounded full-text scan. This is a bounded negative search result, not an open-world nonexistence claim.

### Verification/tooling

Mandatory screenshots of the relevant PDF pages were attempted, but the PDF screenshot backend returned `Cache miss` on all three pages. Parsed primary-PDF text was available. Similar screenshot-cache misses were already recorded in the recent Yang-Mills OS-source audits around draft PRs #135/#140, so this is a repeated tooling failure across runs, not a mathematical failure.

### Decomposition/representation/meta-policy

Decomposition succeeded in separating scaling from action-domain and from downstream OS1 gluing. Representation failed in the source proof, not in the RAKL bookkeeping. No meta-policy failure was observed.

## Episode -> diagnosis -> obstruction/lesson separation

- **Episode:** `YM-E4c-EP-O4-20260811T1837Z` records the trajectory and observations.
- **Diagnosis:** `PROPOSITION_4_6_REQUIRES_BOTH_NORMALIZATION_REPAIR_AND_TYPED_ARBITRARY_ROTATION_COMPARISON` is the bounded interpretation of that episode.
- **FailureExperience:** `F-YM-E4C-ARBITRARY-O4-LATTICE-ACTION-UNBOUND` records the scoped failed proof route.
- **Obstruction shadow:** `O-YM-E4C-O4-ACTION-DOMAIN-GLUING` records the reusable-looking obligation, but remains unpromoted proposal/shadow evidence.
- **Lesson:** none is minted from this single mathematical failure. The repeated screenshot tooling failure is logged longitudinally but is not mathematical learning.

## Failure category

Primary classification: `SOURCE + REPRESENTATION + GLUING_INTERFACE`, with a separate already-known `SCALING` failure. Screenshot failures are `TOOLING/VERIFICATION`. There is no numerical-to-proof substitution.

## Saturation and route rotation

The repeated `YM-E1a1a0` metadata-only acquisition family was treated as flattened for this action because several prior cycles returned zero retained semantic novelty and the exact same content blocker. `saturation_stopping` therefore changed the route rather than retrying the same channel.

The `OBSTRUCTION`, `RELATION`, and `PATH` search axes were reopened by the fresh `YM-E4c` representation/gluing residual. Reopening is a routing state, not automatically retained semantic novelty.

Retained novelty for this cycle is conservatively:

- `KNOWLEDGE = 1`: source-bound observation that the written arbitrary-rotation step lacks a same-domain lattice action under the paper's own field definition;
- `OPERATOR = 0`;
- `EXPERIENCE_PATTERN = 0`;
- `OBSTRUCTION = 1`: scoped proposal/shadow action-domain obstruction;
- `RELATION = 1`: explicit DifferenceWitness between hypercubic automorphisms and arbitrary continuum rotations;
- `PATH = 1`: repair path must close action-domain and normalization jointly before OS1 gluing;
- `META_METHOD = 0`.

These axes overlap and are not an additive intelligence score. Raw commits/files/issues/prose are not learning. No solved-subproblem RAKL novelty class is assigned because no mathematical subproblem was solved.

## What v3 feature helped

Three v3 features materially improved the process:

1. semantic saturation/stopping prevented another identical zero-yield acquisition loop;
2. explicit experience provenance made it possible to say which prior episodes changed routing without promoting them to theorem evidence;
3. current `method_specs` separation of `mathematical_context_translation` and `contextual_theory_gluing` encouraged a domain/codomain check before accepting a continuum-symmetry transfer.

The protected proposal/shadow boundary also prevented the new obstruction from being treated as promoted authority.

## What was missing

A governed memory-universe coverage receipt was unavailable, so missed relevant experience cannot be measured. Current RAKL already lists cross-problem memory coverage as an open upgrade gap; this cycle adds no new framework-authority claim to that known issue.

The current process surfaces also do not appear to require a first-class **transformation-domain signature** when a proof transports a symmetry/operator between discretized and continuum representations. That omission did not cause a gate violation here because the manual type-check caught it, but it is a concrete method-improvement target.

## Framework-improvement hypothesis

**Hypothesis (proposal only):** add a `TRANSFORMATION_DOMAIN_WITNESS` field/operator to mathematical context translation and gluing receipts. Whenever a candidate applies a group action/operator from one representation to another, require explicit source/target domains, invariance/approximation mode, and a DifferenceWitness for non-identical domains before the gluing step can pass. Test this prospectively on YM-E4c and one structurally different discretization-to-continuum case before considering any reusable framework promotion.

Separately, the repeated PDF screenshot cache-miss pattern suggests a tooling improvement: preserve a standardized retry/fallback receipt that distinguishes `parsed-primary-text verified` from `visual-page verification unavailable`. This is tooling/provenance, not mathematical experience.

## Next action

Do not search for another general statement of rotational invariance. Build or source-bind the smallest explicit approximate-rotation map `R_{k,g}` for a fixed generic non-hypercubic `g`, with gauge-invariant observable compatibility and a quantitative error bound, and simultaneously restore the missing lattice-density normalization. Run the irrational-angle rotation and `a_k^{-4}` scaling falsifiers first. Only then attempt same-subsequence OS1 gluing.

Root #5 remains `OPEN_NO_SOLUTION_CERTIFICATE`; independent mathematical reviews remain `0`.
