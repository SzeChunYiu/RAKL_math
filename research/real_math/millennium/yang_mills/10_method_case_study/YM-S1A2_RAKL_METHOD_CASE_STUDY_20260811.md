# RAKL_METHOD_CASE_STUDY — YM-S1a2 source-to-transfer gluing

**Episode:** `EP-YM-S1A2-SOURCE-BINDING-20260811-R1`  
**Outcome:** `PARTIAL_SUCCESS / SOURCE_BRIDGE_NARROWED / NO_CANDIDATE_GENERATED`  
**Authority:** `OBSERVATIONAL_METHOD_CASE / PROPOSAL_SHADOW_ONLY`

## Research method actually used

The cycle did not search for another “mass gap” theorem. Current RAKL gluing/context rules forced a change of research object from a headline claim to three exact interfaces that must make an already-valid local operator lemma applicable to the same lattice theory:

1. `S1a2-RP` — finite-volume Wilson reflection positivity and one-step shift must survive into the unique SZZ infinite-volume measure;
2. `S1a2-DENSE` — SZZ-controlled gauge-invariant smooth cylinders must be dense in the same OS physical Hilbert completion, with centered images dense in `Omega^perp`;
3. `S1a2-TIME` — centered translated covariance must be exactly the `T^n` moment and source support distance must be `n+O_F(1)`.

The process surfaces used from current `method_specs.py` were `source_selection_reliability`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `gap_discovery`, `memory`, `review`, and `saturation_stopping`.

## What worked

- **Primary-source separation.** SZZ's Euclidean covariance theorem was retained while their Langevin/Markov generator gap was explicitly excluded from physical-Hamiltonian evidence.
- **Same-model narrowing.** SZZ's finite-volume object is the Wilson plaquette Gibbs measure, its full sequence converges to a unique infinite-volume measure in the stated strong-coupling regime, and Lüscher's Wilson-lattice OS construction provides the physical positive-time completion/shift template. This reduces the former vague “are these the same theory?” concern to an exact reflection-positivity inheritance contract.
- **Density reframing.** Lüscher's construction makes positive-time observable density structural to the OS completion. The unresolved density problem is therefore narrower: whether the *smooth gauge-invariant cylinder subclass controlled by SZZ* is dense in that same OS norm after gauge quotient/centering.
- **Negative-history use.** `F-YM-S1A-RESTRICTED-SOURCE-HIDDEN-STATE` directly prevented a return to restricted-source decay as a full-gap certificate. `F-YM-S1A1-C001-QZERO-LOGARITHM-DOMAIN` was preserved as downstream statement/chronology warning without being misused as a blacklist.
- **Chronology.** Source audit, context, same-context expert review, dual memory, and a hash-chained trace were frozen with zero `CANDIDATE_PROPOSED` events.

## What remains unresolved

No new mathematical proof candidate was generated because this connector-only run could not execute `plan_math_research` against a runtime-capable checkout. The next candidate must wait for that gate.

The mathematical residual is still the conjunction `S1a2-RP ∧ S1a2-DENSE ∧ S1a2-TIME`. Even complete closure would yield only a fixed-cutoff/infinite-volume/strong-coupling transfer spectral statement. `G5` RG transport, `G6` physical lattice-spacing scaling, `G7` continuum spectral identification, continuum existence/nontriviality, and all root assurance remain open.

## Failure / progress category

- local mathematical failure: `NO`;
- retrieval failure: `NO_OBSERVED_BOUND_UNIVERSE_MISS`;
- representation failure: `NO_NEW_FAILURE`;
- gluing/interface residual: `YES — PRIMARY`;
- source/provenance failure: `PARTIALLY_RESOLVED`;
- verification/gate failure: `RUNTIME_PLAN_GATE_UNAVAILABLE`, operational rather than mathematical;
- tooling/CI failure: `CANNOT_MEASURE`;
- meta-policy failure: `NO`.

## Saturation / novelty

This cycle does **not** count files, context packets, reviews, or a narrower narrative as lattice learning. Retained semantic novelty is conservatively recorded as zero on all seven v3 axes pending consolidation/novelty audit. The cycle has high information value because it localizes the remaining interfaces, but information gain is not automatically retained novelty.

The current axis interpretation is: `KNOWLEDGE` not claimed new; `OPERATOR` 0; `EXPERIENCE_PATTERN` 0; `OBSTRUCTION` 0 retained (three residual labels refine the active obstruction but are not promoted); `RELATION` 0 retained; `PATH` 0; `META_METHOD` 0. The **active residual axis is RELATION/GLUING**, not proof-invention.

## Did RAKL change the action?

`CANNOT_MEASURE`. The run did not freeze a pre-memory/pre-gate action preference before reading the current RAKL state, so assigning causal credit to RAKL would be retrospective. Mechanistically, the framework gates constrained the executed path, but this episode supplies no matched counterfactual attribution.

## Framework-improvement hypothesis

**Hypothesis (not yet promoted to a framework issue):** recurring hard-problem gluing failures may benefit from a typed `PhysicalSpaceApplicabilityWitness` attached to `contextual_theory_gluing`, with explicit coordinates for (i) theory/measure identity, (ii) state-space/quotient identity, (iii) norm/completion identity, (iv) operator/semigroup identity, (v) dense source/observable class, and (vi) limit/uniformity handoff. A local theorem could not satisfy a global edge merely by matching names such as “mass gap” or “spectral gap.”

**Expected benefit:** earlier rejection of wrong-generator, wrong-Hilbert-space, wrong-norm and incomplete-source transfers.

**Possible regression:** excessive ceremony for ordinary same-space applications and false blocking if equivalence is established implicitly by a standard theorem.

**Cheapest discriminating benchmark:** a four-case planted suite: same Hilbert/same operator (pass), same covariance but Langevin generator (reject), dense in ordinary `L2` but not witnessed in target completion (cannot-check), restricted source hiding a lower mode (reject). Evaluate whether the witness catches all unsafe transfers without blocking the valid same-space case.

One Yang–Mills episode is insufficient to assert a new framework defect by itself. Cross-problem recurrence should be checked before opening a canonical RAKL framework issue.
