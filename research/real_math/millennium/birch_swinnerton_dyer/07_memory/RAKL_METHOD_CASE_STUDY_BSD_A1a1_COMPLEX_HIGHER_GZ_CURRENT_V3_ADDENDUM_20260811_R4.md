# RAKL_METHOD_CASE_STUDY — R4 current-v3 adoption addendum

This addendum preserves the already-frozen R4 research chronology while binding the cycle to the later current `RAKL/main` state `3299072b410ac9136548dfd103e846fc7656c31e` (method `3.0.0`). The framework advanced after the targeted mathematical source search, so those changes receive no prospective-discovery credit. They are used only for current proposal/shadow evidence handling and verification.

The important v3 change is explicit separation of TaskEpisode **storage** from canonical **admission**. Current `task-episode.schema.json` requires `storage_admission`, and current `episode_admission.py` makes `PROPOSAL_SHADOW_STORED` retainable for search/failure learning while refusing it at protected consumers. R4 therefore now has an exact current-schema TaskEpisode companion plus a content-bound shadow admission receipt. Neither grants theorem, lesson/tool, proof, canonical-inventory, independent-review, or root authority.

A verification/tooling failure was observed on the stale pinned application suite: workflow `31522995303` at head `9dbbaca7561b7841623145009db67c23d714fb9d` had `1 failed, 458 passed`, with the sole failure caused by the historical frozen-inventory test treating every `research/*.json` top-level `episode_id` as an inventory member. This is recorded separately as `F-BSD-A1A1-LEGACY-EPISODE-INVENTORY-SCANNER-COLLISION-R4`; it is not a mathematical failure. The repair does not weaken the frozen gate: non-TaskEpisode JSON records no longer claim the TaskEpisode key, while the exact current TaskEpisode and admission receipt remain content/hash/status-bound proposal-shadow evidence. Their non-`.json` compatibility containers are not an authority mechanism; current content and admission status are.

This process failure links to the prior R3 stale-execution-pin warning but is not promoted into a reusable experience pattern from one occurrence. Accordingly `EXPERIENCE_PATTERN=0` and `META_METHOD=0` remain unchanged. The current v3 feature directly helped by making the intended authority boundary explicit; the missing application capability is pin synchronization, which belongs to the dedicated application-pin workflow rather than this BSD research branch.

The research-method result is unchanged: ontology normalization separated higher complex derivative order, higher weight/cycle codimension, and higher p-adic deformation derivative order. The target remains `number field + weight-two elliptic curve + complex s-derivative order two + root-faithful arithmetic output`; no theorem in that exact cell was located in the bounded source set, and no nonexistence claim is made. Local source failure and later Mordell–Weil/regulator/Sha/Tamagawa/torsion gluing remain separate.

Framework-improvement hypothesis remains proposal-only: a typed coordinate-disambiguation matrix emitted by `search_query_generation` / `ontology_terminology_normalization` should be benchmarked for reduced false semantic merges. No new framework issue is warranted from the legacy inventory collision because current RAKL already contains the storage/admission separation that addresses the defect class.

Root state remains `OPEN_NO_SOLUTION_CERTIFICATE`.
