# RAKL_METHOD_CASE_STUDY — BSD A1a1 R6 plectic applicability

**Cycle:** `BSD-A1a1-PLECTIC-APPLICABILITY-20260811-R6`  
**Episode:** `EP-BSD-A1A1-PLECTIC-APPLICABILITY-20260811-R6`  
**Fibre:** `sha256:5ba3279d73bed3a4dfbb4d7cd6f10a9f5e6680473890d9d93311fb194feb1dae`  
**Framework:** `RAKL 3.0.0 @ bf94d16847971069912501e9a63f0e97a1e3e159`.

## Episode

The active problem signature was exact complex analytic rank two for arbitrary `E/Q`, transported into the current `F=Q` plectic/mock-plectic representation without changing the complex `s` coordinate. The actually consulted fibre contained R3 plectic-representation evidence, R4 exact-coordinate warnings, the R5 base-change episode/failure, the conditionally reusable root-bridge audit, issue #7/root obligations, current primary plectic sources, and the seven-role same-context expert cell.

Operators/process surfaces used were `memory`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `ontology_terminology_normalization`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `experiment_query_selection`, `synthesis`, `review`, and `saturation_stopping`. The reusable tool `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` was used only as search-control guidance.

Prior experience materially changed the route. R3 said the plectic representation was richer but nonvanishing was open; R4 blocked p-adic/Hida coordinate substitution; R5 showed that a broad auxiliary-`K` search still mixed intrinsic and selectable hypotheses. Those records caused the policy to reject another undifferentiated `K` optimization and type-check the theorem-entry interface first. Retrieved-but-rejected items included the sibling Kurihara/Taylor lane, function-field Yun–Zhang theorem transfer, repeat scalar-theta scans, and prior CI as mathematical verification.

## Diagnosis

Primary-source falsification showed that for Fornea's `F=Q` construction the sign `+1` branch forces `S={p}`, while `S=empty` is the classical-Heegner/sign-`-1` branch. The `S={p}` mock-plectic comparison is tied to split/non-split multiplicative reduction, and Fornea–Gehrmann directly assumes multiplicative reduction at `p`.

The diagnosis is therefore not that R5's local complex lemma failed. The failure is a **representation/transfer precondition plus local-to-global route-gluing failure**: an auxiliary field can select splitting/inertness but cannot supply a reduction-type property intrinsic to the fixed curve.

## Obstruction / lesson status

A scoped obstruction shadow `O-BSD-A1A1-R6-INTRINSIC-PLECTIC-ENTRY-INTERFACE` is recorded separately from the episode and diagnosis. It is not protected/promoted and is not counted as retained `OBSTRUCTION` learning. The reusable lesson remains proposal-only: partition theorem-entry hypotheses into intrinsic-object and auxiliary-choice coordinates before optimizing auxiliary data.

No new tool, motif, lesson, obstruction, or meta-method receives protected promotion. No same-context expert role is counted as independent review.

## Outcome and novelty class

Outcome: `PARTIAL_SUCCESS / SOURCE_BOUND_ROUTE_RESTRICTION / NO_ROOT_CANDIDATE`.

Defensible novelty classes: `representation` and `transfer`. The mathematical content itself is mainly stored/source-derived plus compositional sign typing, not a new deep theorem.

The residual changes from a mixed `auxiliary K admissibility` question to two cleaner obligations: first select an arithmetic carrier whose intrinsic entry interface is compatible with the arbitrary-`E` root contract; then prove an exact complex-rank-two -> nonzero arithmetic-class/regulator arrow. Full Mordell–Weil/Sha/regulator/local-factor gluing remains separate.

## Seven-axis saturation

`KNOWLEDGE` and `RELATION` reopen and advance because the current source family is more sharply scoped. `PATH` reopens because the arbitrary-root path through this mock-plectic branch is pruned/scoped. `OBSTRUCTION` reopens as a proposal shadow but receives zero retained/protected novelty. `OPERATOR`, `EXPERIENCE_PATTERN`, and `META_METHOD` remain flat for retained learning.

## What worked / failed

What worked: current-v3 fibre conditioning, explicit DifferenceWitness-style entry typing, counterexample-first `S=empty` falsification, and separation of local proof from gluing. What failed: the R5 plan implicitly treated all remaining plectic admissibility as auxiliary-field selectable. This was a decomposition/representation/gluing failure, not a refutation of the local mathematics or of BSD.

A nearby good-ordinary generalized-Kato representation survives as a successor candidate, but the source still leaves its nonvanishing arrow open; therefore no root-strength conclusion is imported.

## Framework-improvement hypothesis

A concrete hypothesis for future RAKL evaluation is to type every route precondition as `OBJECT_INTRINSIC`, `AUXILIARY_SELECTABLE`, `REPRESENTATION_INTRINSIC`, or `DOWNSTREAM`, and have `contextual_theory_gluing` reject a composition when a required intrinsic coordinate is not supplied by the root contract. No framework issue is opened in this cycle because current v3 equivalence/context/gluing surfaces can already encode the distinction; this is a measurement/ergonomics hypothesis, not a demonstrated framework defect.

Repeated process-failure linkage: R5's protected-artifact mutation attempt remains a separate tooling/provenance failure and is not recast as a mathematical failure. No new recurrence of that process failure occurred in R6.
