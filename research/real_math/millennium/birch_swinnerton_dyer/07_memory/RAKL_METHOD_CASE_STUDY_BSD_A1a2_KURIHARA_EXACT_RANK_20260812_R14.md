# RAKL_METHOD_CASE_STUDY — BSD A1a2 R14

Authority: `PROPOSAL_SHADOW_ONLY`. Root state: `OPEN_NO_SOLUTION_CERTIFICATE`. Same-context expert review earns `0/3` independent-review credit.

## Atom / signature / frozen fibre

Active atom: `BSD-A1a2-EXACT-SELMER-DIMENSION-KURIHARA-ORDER-BRIDGE`, under `BSD-A1a1-THETA-ORDER-COMPARISON`. Frozen fibre SHA-256: `81ac2b39d9bc10d33dd77c8ffe237bef4ea625820ef323c9608a318d3cc67168`.

Residual before: under the stored Zhang R9 theorem cell plus R12 coefficient binding, exact complex analytic rank two gives `dim_Qp H^1_f(Q,V_pE) >= 2` under all Zhang hypotheses. Missing are (i) the exact upper bound/equality `=2`, (ii) transverse nonzero p-local localization, and (iii) the full Mordell-Weil/Sha/regulator/Tamagawa/torsion/period/complex-leading-term glue.

Actual fibre consulted: current RAKL v3 architecture/facade, `AGENTS.md`, core skill/manifest/workflow, mathematical-research workflow, current `method_specs.py`, `pre_action_receipt.py`, current `TaskEpisode` contract; RAKL_math BSD root issue and merged R9/R12 state; prior R11/R8 failure memory; pending R13 only as non-authority routing context; Kim-Pollack arXiv:2505.09121 primary PDF; and cross-Millennium XM018 only for a DifferenceWitness rejection.

## Expert cell and delegation

The same-context cell had seven roles: complex L-function/modularity, arithmetic-geometry/Selmer-Euler-system, p-adic-Iwasawa/Kurihara, local-global/gluing, adversarial falsification, formal/specification, and novelty/research-value. The analytic lead enforced exact complex Taylor order and rejected low-rank extrapolation. The arithmetic and p-adic leads bound the discrete exact-rank theorem and its hypotheses. The gluing lead kept coefficient, localization and root-leading-term interfaces separate. The adversarial lead prioritized parity, premise-reimport, low-rank and numerical-witness falsifiers. The formal lead required the exact two-arrow DAG. The novelty lead required a stronger same-Q theorem cell than R11 before retaining semantic credit.

## Methods / operators / motifs tried

Canonical process surfaces used: `decomposition`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `memory`, `synthesis`, `review`, and `saturation_stopping`. Search policy was invention-last same-domain `SEARCH`; no protected `LIFT` was used. Counterexample-first theorem-direction audit was the decisive scoped falsifier.

Structural motif: two rank-coordinate detectors can only be composed after a calibration theorem. The cross-domain instrument-calibration analogy was retained proposal-only and never used as authority. Cross-Millennium XM018 (`INTERSECTION_IS_NOT_GENERATION`) was retrieved but rejected for this micro-atom by DifferenceWitness: it constrains generation of a transverse localization direction, whereas R14 tests equality between two rank coordinates. It remains relevant to the separate localization residual.

## Retrieved / selected / rejected / missed experience

Selected because it changed routing: `COR-BSD-R12-ZHANG-R9-TO-VP-LOWER` fixed the lower-bound starting state; `LEM-BSD-R12-PINFINITY-VP-CORANK-DIM` prevented duplicate coefficient work; `BSD-R11-KURIHARA-ORDER-RESIDUAL` prevented falsely claiming the complex/discrete mismatch as new and redirected search to a stronger direct same-Q theorem cell; `F-BSD-A1A1-R8-SOURCE-FAMILY-REIMPORTS-ARITHMETIC-ENTRY` enforced the premise-strength test.

Retrieved but rejected: `EP-BSD-A1A1-R7-GENERALIZED-KATO` because it addresses transverse localization rather than exact-rank upper bounds; XM018 because its nonexpansion DifferenceWitness is about localization generation, not rank-coordinate equality; the plectic R6 carrier because it adds base-change/local-reduction interfaces orthogonal to this same-Q discriminator; pending R13 because nonmerged shadow evidence may guide search but bears no canonical authority.

Missed experience is `CANNOT_MEASURE`: current retrieval does not prove completeness of the repository/search universe. The bounded repository query found no existing `2505.09121` source packet; that is evidence of a local application-memory gap, not a claim that no semantically equivalent record exists anywhere.

## Primary-source result and falsification

Kim-Pollack Theorem 1.1 proves a direct exact downstream theorem: nonzero Kurihara data determine the Bloch-Kato Selmer module and `cork Sel = ord(delta)`. Theorem 1.2's ordinary branch supplies nonvanishing under good ordinary, p-distinguished, large-image conditions. Corollary 1.5 therefore proves the discrete equality `ord(delta)=cork Sel` and only parity between complex analytic order and Selmer corank. The exact equality `ord_{s=k/2}L(f,s)=ord(delta)` is explicitly stated as Conjecture 1.6. Corollary 1.10 proves that equality only in the analytic-rank-`<=1` weight-two cell. Corollary 1.11 gives a computational upper bound from an explicit nonzero discrete witness, not from analytic rank two.

The counterexample-first audit therefore rejects four tempting shortcuts: parity is not equality; rank `<=1` is not rank two; numerical Kurihara witnesses do not prove a root-general bridge; and the paper's Taylor/Kurihara analogy is not a theorem because the exact calibration is separately conjectured.

## Outcome / residual / diagnosis

Outcome: `PARTIAL_SUCCESS / MATERIAL_SCOPED_RELATION_MILESTONE_NO_BSD_CERTIFICATE`.

Residual after:

`ord_{s=1}L(E,s)=2 -> ? ord(Kurihara)=2 ->[Kim-Pollack] exact Selmer corank/dimension 2`, with the first arrow identified as the weight-two specialization of Kim-Pollack Conjecture 1.6. Transverse `loc_p != 0` remains open separately. Mordell-Weil rank, Sha finiteness/order, regulator, Tamagawa/local terms, torsion, period normalization and complex leading-term gluing remain open separately.

Episode -> diagnosis -> obstruction/lesson are intentionally separate. The episode records the attempted search and observations. Diagnosis: the discrete representation has enough downstream exactness; the missing edge is complex-to-discrete calibration. No new obstruction is promoted because R11 already contained that obstruction family. No new lesson is promoted because the calibration-theorem discipline is existing RAKL theorem-direction methodology.

Failure category: no local theorem failure. Root-strength failure is `REPRESENTATION + GLUING` (complex Taylor coordinate to discrete Kurihara coordinate). Retrieval incompleteness is separately `CANNOT_MEASURE`; source verification succeeded; tooling did not alter the mathematics.

## Saturation / novelty

Reopened axes: `KNOWLEDGE`, `RELATION`, `PATH`. Flattened axes: `OPERATOR`, `EXPERIENCE_PATTERN`, `OBSTRUCTION`, `META_METHOD`.

Retained semantic novelty lower bound for this application state: `KNOWLEDGE=1`, `OPERATOR=0`, `EXPERIENCE_PATTERN=0`, `OBSTRUCTION=0`, `RELATION=1`, `PATH=1`, `META_METHOD=0`. This counts the source-bound direct same-Q theorem cell and its scoped composition path, not repository growth, literature novelty, proof novelty or framework learning. Defensible novelty class: `representation`, secondary `compositional`/`stored`.

## Assurance process-failure linkage

The R13 PR #346 exact-head application run failed on a protected TaskEpisode-inventory assertion because a research `*.json` object with top-level `episode_id` entered the repository-wide episode-discovery namespace without an inventory/classification extension. That was a process/provenance failure, not a mathematical falsification. During R14 closeout, inspection of the same protected test found the same collision shape in the non-TaskEpisode obstruction/lesson-status shadow before merge. R14 repaired only the reference field from `episode_id` to `episode_reference_id`; the episode, diagnosis, mathematical result, novelty accounting and root state were unchanged. Repeated process-failure signature: `PF-V3-EPISODE-ID-NAMESPACE-COLLISION`, reused for diagnosis only and not promoted as protected memory authority.

Local mathematical failure and local-to-global/gluing failure remain distinct from this assurance defect. The mathematical residual is unchanged; the process defect concerns serialization namespace/registration only. A concrete process-improvement hypothesis is a fail-fast lint that forbids top-level `episode_id` in unregistered research JSON containers (or requires an explicit TaskEpisode classification receipt) before expensive exact-head CI. This hypothesis should be isolated as framework/application-assurance work and must not alter mathematical gates.

## What v3 changed / what was missing

V3 helped by forcing the exact coordinate dictionary and theorem direction before composition, keeping the pending R13 episode non-authoritative, preserving local-versus-gluing failure, and using prior R11/R8 experience to rotate away from another generic Euler-system/nonvanishing scan. The observed action change is that the cycle searched for an explicit coordinate-calibration theorem rather than treating a new exact-Selmer carrier as progress by itself. Causal magnitude relative to a frozen pre-memory counterfactual is `CANNOT_MEASURE` because no independent pre-memory action preference was recorded.

Framework-improvement hypothesis: add a proposal-only transformation-memory facet that distinguishes `DOWNSTREAM_EXACTNESS_THEOREM` from `SOURCE_COORDINATE_CALIBRATION_THEOREM`, and stores a `conjectural_boundary` field when a primary source explicitly names the missing calibration. This should reduce repeated carrier rotations that merely re-encode the unknown root coordinate. Any implementation would belong in a narrowly scoped RAKL framework issue/PR and must not embed BSD evidence or weaken gates.
