# BSD A1a1 R12 prospective same-context expert review

**Authority:** role-separated same-context review only; independent mathematical review credit `0/3`.  
**Atom:** `BSD-A1a1-THETA-ORDER-COMPARISON` / `PINFINITY_SELMER_TO_VP_SELMER_EXACT_COORDINATE_BINDING_OPEN`.  
**Frozen fibre:** `sha256:f1cd498b05c1d2dfca66f7f45b8c4a8cedf9b2c33c21b3001527c9f4a12afd0f`.

## 1. Analytic BSD / automorphic specialist

**Delegation.** Protect the exact analytic input and its scope.  
**Finding.** Only the merged Zhang R9 theorem cell may be used: under all of its technical-prime hypotheses, exact complex analytic rank two yields `corank_Zp Sel_{p^infinity}(E/Q)>=2`. No converse, exact-rank, or arbitrary-prime statement is available from that source.  
**Decision.** PASS for testing a coefficient conversion only; BLOCK any claim that the coefficient conversion supplies the missing Selmer upper bound.

## 2. Selmer / Galois-cohomology specialist

**Delegation.** Check that the torsion and characteristic-zero Selmer groups use matching local conditions.  
**Finding.** Kim defines the usual `E[p^infinity]` Selmer group via Kummer images and the `V_pE` Selmer group by the corresponding `E(Q_v) tensor Q_p` local condition; Skinner explicitly identifies the torsion Kummer subgroup with the image of Bloch--Kato `H_f^1` for `V`.  
**Decision.** PASS for the usual Selmer structures. Strict, signed, relaxed, fine, or modified Selmer structures do not inherit this result without a new comparison witness.

## 3. Iwasawa / Euler-system specialist

**Delegation.** Detect hidden arithmetic entry assumptions.  
**Finding.** The coefficient comparison itself uses `0 -> T -> V -> V/T -> 0` plus the local-condition/maximal-divisible structure. It does not need a main conjecture, generalized-Kato nonvanishing, algebraic rank two, Sha finiteness, or p-adic BSD. Those assumptions would reappear only in stronger exactness/localization claims.  
**Decision.** PASS the local coefficient lemma; keep all stronger arithmetic consequences blocked.

## 4. p-adic representation specialist

**Delegation.** Audit the corank-versus-dimension mechanism.  
**Finding.** For `T=T_pE`, `V=T tensor Q_p`, and `W=V/T=E[p^infinity]`, the natural map from the characteristic-zero Selmer group to the torsion Selmer group has lattice/compact discrepancy, while Skinner identifies its image with the maximal divisible subgroup. A `d`-dimensional `Q_p` source therefore contributes divisible corank `d`, and the residual quotient contributes zero `Z_p`-corank.  
**Decision.** PASS `corank_Zp Sel_{p^infinity}=dim_Qp H_f^1(Q,V_pE)` in the stated usual-Selmer scope.

## 5. Adversarial falsification specialist

**Delegation.** Attack the proposed transfer before composition.  
**Tests.** Searched for: local-condition mismatch; positive-corank quotient after the characteristic-zero image; positive-dimensional kernel that destroys corank/dimension equality; different curve/prime; replacement by a strict Selmer group; hidden Sha/rank/main-conjecture/localization premise.  
**Finding.** No falsifier survives in the stated usual-Selmer coefficient scope. Mandatory visual PDF screenshot attempts for the Kim and Skinner pages returned backend `Cache miss`, so visual verification is `CANNOT_CHECK`; this does not upgrade parsed-text verification into a visual check.  
**Decision.** PASS source-bound result; retain tooling/verification limitation explicitly.

## 6. Local-to-global / gluing specialist

**Delegation.** Determine exactly what closes and what does not.  
**Finding.** The coefficient representation mismatch closes. The R9 lower bound can therefore be restated in the `V_pE` Bloch--Kato coordinate. Nothing here proves exact Selmer dimension two, transverse nonzero localization, Mordell--Weil rank equality, Sha finiteness, regulator nondegeneracy, Tamagawa/torsion accounting, period normalization, or the complex leading term.  
**Decision.** Local mathematical SUCCESS; root-facing gluing OPEN.

## 7. Formal methods / novelty / provenance specialist

**Delegation.** Police authority, novelty, and chronology.  
**Finding.** The mathematical content is stored/source-bound; the application value is compositional/representation-level closure of one explicit residual. The prospective gate packet and eight-event trace were frozen before formal result materialization. Current RAKL main later drifted and added canonical shadow-hash helpers plus a provenance-bound obstruction-transformation corpus; this current-main state may be reconciled append-only but may not rewrite the historical freeze.  
**Decision.** Novelty class `stored` with secondary `compositional` and `representation`; no new-mathematics claim, no formal-proof credit, no independent-review credit, no protected promotion, root OPEN.

## Cell consensus

Proceed with the narrow source-bound coefficient comparison and compose only with Zhang R9. Record a shadow TaskEpisode and separate diagnosis, retain `BSD-A1a2-LOCALIZATION-POSITIVE-RANK-BRIDGE`, mint no new obstruction/lesson/tool/motif, and set the next discriminator to either the missing exact `V_p` Selmer upper bound or transverse localization from exact complex analytic rank two under genuinely weaker assumptions than the target arithmetic statement.
