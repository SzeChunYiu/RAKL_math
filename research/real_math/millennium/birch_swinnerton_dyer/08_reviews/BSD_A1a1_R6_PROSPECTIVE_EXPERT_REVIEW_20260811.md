# BSD A1a1 R6 — prospective same-context expert cell

**Cycle:** `BSD-A1a1-PLECTIC-APPLICABILITY-20260811-R6`  
**Atom:** `BSD-A1a1-THETA-ORDER-COMPARISON`  
**Framework:** `SzeChunYiu/RAKL@f224d91d9fbd2844a89921ca4a30b77a7954ecd2`, method `3.0.0`  
**Working parent:** `RAKL_math@c9dc6a2a4021531970695d3775146bf6fa6605f9` (R5 proposal/shadow lineage; not canonical main).  
**Authority:** same-context role separation only; independent mathematical review credit = `0`.

## Frozen discriminator

Before spending another cycle on auxiliary `K`, verify whether the current `F=Q` plectic/mock-plectic route itself requires an **intrinsic property of `E`**—multiplicative reduction at an inert bad prime `p`—in the even-sign/rank-two setting. Partition every source hypothesis into `E-INTRINSIC`, `K-SELECTABLE`, `p/E-RESIDUAL`, or `DOWNSTREAM`. A route restriction is accepted only if it follows from the exact primary statements after this freeze.

## Role passes

1. **Complex-L / base-change lead.** Preserve the R5 complex-coordinate lemma only as proposal/shadow lineage. The present discriminator does not modify `ord_{s=1}` or infer arithmetic rank. If the plectic entry interface is absent, that is a route-applicability result, not a complex-L theorem.

2. **Plectic / automorphic lead.** Re-read the exact `F=Q` hypotheses in Fornea–Gehrmann and Fornea. The key falsifier is whether the relevant even-sign/mock-plectic specialization can be formed without a multiplicative-reduction prime of `E`.

3. **Arithmetic-geometry / Selmer lead.** Keep `Q_K != 0 -> Selmer` one-way. Construction, localization, nonvanishing, Selmer rank, Mordell–Weil rank and BSD leading terms remain distinct. No rank-two conclusion may be reversed.

4. **Local arithmetic / reduction-type lead.** Reduction type of fixed `E/Q` at `p` is an `E`-intrinsic coordinate; inert/split behavior of `p` in `K` is an auxiliary coordinate. Do not let a twist/nonvanishing theorem be credited with changing the former.

5. **Adversarial falsification lead.** Try to kill the suspected restriction by locating an allowed `F=Q` even-sign plectic path with `p` good/additive, an `S=empty` specialization serving the same role, or theorem text showing multiplicative reduction is nonessential. If any appears, reject the obstruction.

6. **Formal/provenance lead.** Prefreeze scouting receives no prospective credit. Preserve R5's CI-scope defect separately: a passed workflow that omitted the R5 test is not full R5 verification. The test-placement repair is operational only and grants no mathematical authority.

7. **Novelty/routing lead.** If verified, classify the result as a scoped `representation`/`transfer` route restriction, not a BSD theorem. It is valuable only because it changes search routing: an arbitrary-`E` proof must either prove the root contract implies the intrinsic interface (not assumed here) or rotate to a representation that does not require it.

## Consensus / strongest objection

Consensus: run the intrinsic-entry audit first. The strongest objection is that the plectic literature may contain another `F=Q` specialization outside the currently located theorem family, so any restriction must be explicitly scoped to the audited Fornea–Gehrmann/Fornea route and must not become a literature-wide impossibility claim.

## Prospective decision

Selected action: exact primary-source theorem/hypothesis audit with counterexample-first falsifiers.  
Rejected action: another undifferentiated auxiliary-`K` search.  
No mathematical candidate generation is authorized in this cycle.
