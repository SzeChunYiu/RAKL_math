# BSD A1a1 R7 prospective same-context expert cell

**Cycle:** `BSD-A1a1-KATO-LOCALIZATION-REENTRY-20260811-R7`  
**Frozen fibre:** `sha256:788ae31fb787de32f0ab9b4a6e715c0a3092db5332e562dd1481ca29ed324375`  
**Authority:** same-context adversarial planning only; independent-review credit = 0.

This cell is frozen before the targeted post-freeze literature audit. Prefreeze scouting of Castella and Castella–Hsieh is motivation only and receives no prospective discovery credit.

## Delegated roles

1. **Complex analytic/automorphic specialist** — preserve the exact root datum `ord_{s=1}L(E,s)=2`; reject any silent replacement by a Hida, cyclotomic, anticyclotomic, or other p-adic order. Task: identify whether a cited arithmetic nonvanishing theorem is actually driven by the complex second derivative or only by an arithmetic premise.

2. **Generalized-Kato/Iwasawa specialist** — read the exact current theorem for `kappa_p(E)` at good ordinary `p`, separating the proved forward implication from every converse hypothesis. Task: expose residual representation, irreducibility, local, and localization assumptions and mark which are intrinsic/auxiliary/arithmetic.

3. **Selmer/localization specialist** — audit `loc_p : Sel(Q,V_pE) -> E(Q_p) tensor Q_p`, its relation to Selmer dimension, Mordell–Weil rank and Sha. Task: test whether `loc_p != 0` is available from analytic rank two without importing the target arithmetic conclusion.

4. **Gross–Zagier/derived-height/regulator specialist** — inspect rank-two derived-height or p-adic-regulator formulas as possible bridges. Task: distinguish an identity/formula from a nondegeneracy theorem and flag exceptional/trivial-zero or local correction hypotheses.

5. **Adversarial circularity/local-factor specialist** — attack the cheapest closure attempt first. Task: reject proofs that assume positive algebraic rank, Selmer dimension two, p-adic BSD, main-conjecture strength equivalent to the target, nonzero localization, or regulator nondegeneracy unless independently obtained from weaker analytic input; separately track Tamagawa/torsion/Sha/local conditions.

6. **Formal/gluing assurance specialist** — maintain local-versus-global separation. Task: even if `kappa_p(E) != 0` is established at one prime, do not glue it automatically to Mordell–Weil rank, Sha finiteness, real regulator, all local factors, or the complex BSD leading term. Verify theorem-direction and exact statement binding.

7. **RAKL v3/metrology specialist** — keep TaskEpisode -> diagnosis -> obstruction/lesson distinct, proposal/shadow only, preserve retrieved/rejected memory and all seven novelty axes, and refuse to count files/commits/CI as learning. Task: detect whether the successor route is genuinely new or merely re-enters the previously recorded localization child.

## Joint preaction decision

The group selects a narrow theorem-cell audit:

`complex analytic rank 2 -> ? [arithmetic premise] -> kappa_p(E) != 0 -> Selmer information`.

The first discriminator is not another generalized nonvanishing search. It is to determine the exact converse shape and then search for a current primary theorem that supplies its localization/height premise directly from complex analytic rank two. If the premise is algebraic/Selmer/regulator input, classify the route as re-entry into the prior localization obstruction and rotate rather than treating the successor representation as new progress.

## Falsifiers fixed before search

- A theorem `kappa != 0 -> dim Sel=2` does not reverse itself.
- A converse of the form `dim Sel=2 and loc_p !=0 -> kappa !=0` is not an analytic-rank-two bridge unless both arithmetic premises are independently supplied from weaker data.
- A p-adic Gross–Zagier/derived-height formula does not establish nondegeneracy merely by expressing a derivative as a height/regulator.
- A one-prime Selmer conclusion is not the full complex BSD leading-term formula.
- A rank-two special case is not arbitrary-rank BSD.

## Expected outcomes

`SUCCESS` only if a source-bound analytic-only bridge is found and verified at the exact coordinate. `PARTIAL_SUCCESS` if the theorem-cell is narrowed or a route is correctly pruned. `FAILURE/BLOCKED` if the source family does not supply the needed implication. Any gluing failure is recorded separately from a local mathematical failure.
