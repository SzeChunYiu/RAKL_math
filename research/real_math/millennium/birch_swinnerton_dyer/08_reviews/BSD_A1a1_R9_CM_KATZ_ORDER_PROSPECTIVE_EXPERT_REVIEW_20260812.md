# BSD A1a1 R9 prospective same-context expert cell — CM Katz-order theorem cell

**Authority:** proposal/shadow only. This is one seven-role same-context expert cell, not seven isolated reviewers. Independent mathematical review credit is **0/3**.

**Frozen fibre:** `sha256:1169325f3cc17acc2094488809bc13ebba796a54aa51bcbc132690c98e1987c9`.  
**Active atom:** `BSD-A1a1-THETA-ORDER-COMPARISON`.  
**Nested existing obstruction:** `BSD-A1a2-LOCALIZATION-POSITIVE-RANK-BRIDGE`.  
**Search-control input:** `MTS-BSD-A1A2-R8-COMPLEX-TO-ARITHMETIC-ENTRY-20260812` (proposal-only; no mathematical authority).

## Delegated review and cross-check discussion

1. **Complex analytic / Gross–Zagier specialist.** Preserve the root datum as exact `ord_{s=1} L(E,s)=2` for the same `E/Q`; do not replace it by algebraic rank, Selmer rank, a p-adic order, or a twisted/base-changed datum. The Castella theorem cell starts with a positive-even complex-vanishing background but Theorem C's operative entry conditions are exact Katz p-adic orders plus finite p-primary Sha. This role therefore rejects Theorem C as the missing *complex-to-arithmetic* arrow while accepting it as a downstream cell.

2. **CM / Iwasawa / Katz p-adic L-function specialist.** Verify the exact coordinates in the current primary PDF of Castella, arXiv:2204.09608. Theorem C assumes `ord_{s=1} L_p(s)=2` and `ord_{s=1} L_p^*(s)=1`, together with the hypotheses of Theorem B and finiteness of `Sha(E/Q)[p^∞]`, and concludes a two-dimensional Selmer group with explicit basis `Q_p κ_p ⊕ Q_p x_p^(2)`. The proof uses the CM Iwasawa main-conjecture/Perrin–Riou side for the upper bound and Coates–Wiles plus p-parity for the lower bound. The cell is mathematically strong but special to CM/good-ordinary split `p`.

3. **Euler-system / Kolyvagin / Selmer specialist.** Cross-check the transverse-localization geometry. Theorem B says, when the Selmer dimension is two, `κ_p != 0` iff `res_p != 0`; the introduction further records that nonzero `κ_p` spans `ker(res_p)`. Hence `κ_p` cannot be its own transverse witness. Theorem C is useful because the derived elliptic unit `x_p^(2)` supplies the complementary direction and its proof obtains nonzero localization under the stated p-adic-order/Sha assumptions.

4. **p-adic height / regulator specialist.** Adversarially test the tempting reverse direction. Remark 1.3.1 treats the exact p-adic-order/Selmer-rank equivalence as p-adic BSD prediction; known converse inequalities have equality only under p-adic-height nondegeneracy and, on the `L_p^*` side, additional finiteness/nonvanishing conditions. Therefore regulator/height nondegeneracy may not be smuggled into the complex-rank-two bridge.

5. **Local-factor / Tamagawa / torsion / Sha accountant.** Keep every correction coordinate visible. `Sha(E/Q)[p^∞]` finiteness is an explicit Theorem C hypothesis; it is not the full all-prime Sha conclusion of BSD and is not derived in this cycle from exact complex rank two. Tamagawa, torsion, periods, and the global regulator/leading coefficient are untouched downstream obligations. Any future complex-to-Katz-order theorem must expose Euler factors and exceptional/trivial-zero corrections instead of identifying orders by notation.

6. **Formal logic / gluing / scope specialist.** Separate three statements: (i) Castella's local theorem cell is valid in its stated CM scope; (ii) the transfer from exact complex rank two to its p-adic entry premises is open in this bounded audit; (iii) even success in (ii) would not by itself prove the arbitrary-curve/all-rank BSD root or the full leading-term formula. No low-rank or CM special case receives generic authority.

7. **RAKL v3 / metrology / provenance specialist.** Re-read current RAKL method `3.0.0` and route with the R8 missing-transformation specification instead of another saturated carrier rotation. R7 and R8 episodes/failures changed the observed search focus toward the source-to-arithmetic entry border, but no independent pre-memory counterfactual was frozen, so causal magnitude is not measurable. The current result is stored as a proposal/shadow TaskEpisode plus separate diagnosis and failure; no new obstruction, lesson, tool, motif, path, or method promotion is authorized.

The cell then jointly replayed the root-facing falsifiers. A route is rejected if it reverses a one-way theorem, treats p-adic BSD as established, assumes algebraic/Selmer rank two, assumes finite Sha or root-strength height/regulator nondegeneracy as a free consequence, changes the curve/theory without a return witness, silently equates complex/p-adic/augmentation/discrete orders, or promotes the CM cell to arbitrary `E/Q`.

## Joint finding

The cell unanimously records `PARTIAL_SUCCESS_SCOPED_CM_DOWNSTREAM_CELL_IDENTIFIED_NO_ROOT_CANDIDATE`. The important positive result is a **theorem-backed border refinement**:

`{exact Katz p-adic order pair + finite Sha[p^∞] + stated CM/local hypotheses} -> {dim Sel=2 + explicit transverse derived-unit direction}`.

The root-facing missing arrow remains:

`ord_{s=1} L(E,s)=2 -> ? -> {exact Katz p-adic order pair + finite Sha[p^∞] + required local hypotheses}`

in the special CM cell, while generic non-CM/arbitrary-rank arithmetic entry and full BSD leading-term gluing remain separately open. This is not a solved root subproblem, so no RAKL mathematical novelty class is asserted for a solved BSD subproblem. As a search-route refinement only, it is best described as **representation/transfer**.

## Saturation and next action

`KNOWLEDGE` and `RELATION` reopen because a current primary theorem cell makes the transverse target more explicit. `OPERATOR`, `EXPERIENCE_PATTERN`, `OBSTRUCTION`, `PATH`, and `META_METHOD` remain flat: R9 reuses the existing entry-relocation experience pattern and A1a2 obstruction and establishes no successful root path. The next bounded search must target the exact complex-to-Katz coordinate interface with all local/exceptional-zero corrections visible. If near matches require the desired arithmetic conclusions or equivalent p-adic BSD/regulator strength, record negative history and rotate the *intermediate coordinate*, not merely the carrier name.