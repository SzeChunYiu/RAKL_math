# BSD A1a1 R5 — Kolyvagin/derived-Heegner Selmer-carrier bridge audit

Authority: **PROPOSAL/SHADOW ROUTE EVIDENCE ONLY**. No BSD theorem, candidate theorem, lesson/tool promotion, independent review, or root certificate.

Framework semantic subject frozen before the consequential source action: `SzeChunYiu/RAKL@3df0000e71e7f3f8a61925bf9a8176e72d5143de`, method `3.0.0`. Application base: `SzeChunYiu/RAKL_math@812addd25a7f34d3c6272143e21d5d7db34539aa`. Canonical atom remains `BSD-A1a1-THETA-ORDER-COMPARISON`; this cycle does **not** pretend that a Selmer carrier proves the theta-order atom. It is a representation/routing discriminator inside the BSD analytic-to-arithmetic root interface.

## 1. Exact root and predeclared discriminator

The root is still the BSD rank equality for every elliptic curve over `Q`, with regulator/period/Tamagawa/torsion/Sha leading-term obligations separate. The prospectively frozen R5 discriminator asked for a number-field weight-two theorem in the exact complex-`s` derivative-order-two cell, either identifying a plectic/generalized-Kato/derived-height object with the complex second central derivative or proving its nonvanishing from exact complex analytic rank two without importing Mordell–Weil/Selmer rank two, finite Sha, p-adic BSD, or equivalent root-strength arithmetic input.

**Target-cell result:** no such direct theorem was located in the bounded primary-source set. Coverage completeness is `CANNOT_MEASURE`; this is not a literature-wide nonexistence claim.

## 2. High-information route refinement: a weaker arithmetic carrier already exists in scoped Kolyvagin regimes

Jetchev–Lauter–Stein isolate the conditional implication in their Corollary 3.5: assuming Kolyvagin's nontriviality conjecture, an elliptic curve of even nonzero analytic rank has `Z_p`-corank of `Sel_{p^∞}(E/Q)` at least two. Their proof uses an auxiliary imaginary quadratic field and Gross–Zagier/Kolyvagin structure rather than an identity between `L''(E,1)` and a rank-two arithmetic object.

Sweeting later proves nontriviality of the relevant Kolyvagin system in substantial weight-two non-CM regimes under explicit generalized-Heegner, local and residual hypotheses (Theorem A / Corollary 8.3.7 and Theorem A.1.1), extending Wei Zhang's earlier scoped p-indivisibility theorem. On the **overlap** where the auxiliary field/prime selected for the analytic argument also satisfies the nonvanishing theorem's hypotheses, the combined theorem chain therefore supplies a genuine scoped arithmetic carrier:

`exact analytic rank 2`

`--> [auxiliary K + Gross–Zagier/Kolyvagin structure + proved Kolyvagin-system nontriviality in compatible scope]`

`p^∞-Selmer Z_p-corank >= 2`.

This is **not** recorded as a universal theorem for all elliptic curves. The simultaneous auxiliary-`K`/`p` admissibility required to compose the source theorems at root generality has not been audited to closure here; CM and exceptional/residual/local cases remain separate. The result is a scoped route witness, not a BSD rank-two theorem.

## 3. The new earliest root-facing cut is Sha faithfulness, not construction of a plectic object

The same Jetchev–Lauter–Stein introduction states the exact DifferenceWitness needed for the root bridge: the `p^∞`-Selmer corank is at least the Mordell–Weil rank, and equality holds exactly when the `p`-primary Tate–Shafarevich group is finite. Thus a Selmer lower bound cannot be reversed into a Mordell–Weil lower bound without excluding a divisible/infinite `Sha[p^∞]` contribution.

The local theorem chain and the gluing failure must therefore remain separate:

- **local analytic/arithmetic success, scoped:** compatible Kolyvagin nontriviality can carry analytic rank two into a `p`-Selmer lower bound;
- **local-to-root gluing failure:** `corank Sel_{p^∞} >= 2` is not root-faithful for `rank E(Q) >= 2` unless `Sha(E/Q)[p^∞]` is known finite/corank zero;
- **exact-rank failure:** even a lower bound does not give `rank E(Q)=2`; a compatible upper bound is still required;
- **leading-term failure:** regulator, real period, Tamagawa factors, torsion, and the order/finiteness of Sha remain independent obligations.

This changes the routing picture relative to R3/R4. A direct complex-second-derivative-to-plectic formula remains a legitimate route, but it is no longer the only high-information way to leave the complex side. The derived-Heegner/Kolyvagin system can act as an intermediate **Selmer carrier** in proved scopes. The next root-critical discriminator on this branch is whether one can obtain root-faithful Mordell–Weil information without assuming the required Sha finiteness or an equivalent BSD-strength input.

## 4. Current-frontier falsification

Fresh primary-source checks do not remove that cut. Da Ronche's 2025 non-ordinary extension proves versions of Kolyvagin's conjecture using signed Selmer machinery but is driven by rank-one Selmer/converse input, not by the complex second derivative. Kim–Pollack's 2025 refined Tamagawa work gives powerful discrete Kolyvagin-derivative control of Selmer structure under a localized Iwasawa-main-conjecture input; it explicitly uses a discrete coordinate rather than complex or p-adic analytic variation, so it remains a parallel coordinate and was not substituted for the selected complex target. Fornea's 2026 plectic construction and the 7 August 2026 Burungale–Kobayashi–Nakamura–Ota CM anticyclotomic main conjecture strengthen nearby arithmetic machinery but do not state the missing root-faithful complex-second-derivative bridge.

## 5. Expert-cell findings

The **complex analytic specialist** accepted JLS Corollary 3.5 only as an indirect analytic-rank-to-Selmer implication and rejected any claim that it computes `L''(E,1)`. The **Heegner/plectic specialist** verified that Sweeting's nonzero Kolyvagin class is a genuine arithmetic carrier but that construction/nonvanishing has explicit scope. The **Euler-system/Iwasawa specialist** separated Kolyvagin-system nontriviality from discrete Kato/Kurihara and p-adic-Iwasawa coordinates. The **BSD leading-term specialist** identified `Sha[p^∞]` finiteness/corank zero as the first failed faithfulness edge from Selmer to Mordell–Weil and kept regulator/Tamagawa/torsion/period work downstream. The **automorphic/local specialist** kept auxiliary `K`, `p`, Heegner, residual-image, ordinary/nonordinary and CM hypotheses explicit. The **falsification specialist** applied `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` and rejected the invalid inference `Selmer lower bound => Mordell–Weil lower bound`. The **verification/metrology specialist** classified the direct R5 target-cell search as unresolved and the alternate Selmer-carrier relation as scoped/compositional only; same-context review earns no independent-review credit.

## 6. Episode -> diagnosis -> obstruction/lesson discipline

**Episode:** `EP-BSD-A1A1-KOLYVAGIN-SELMER-CARRIER-20260811-R5` records the bounded search, the direct target-cell miss, and the alternate scoped carrier relation.

**Diagnosis:** `D-BSD-A1A1-SELMER-CARRIER-FAITHFULNESS-R5` — previous routing overfocused on producing a rank-two plectic/generalized-Kato object directly from the complex second derivative; a proved Kolyvagin nonvanishing theorem can instead deliver a Selmer lower-bound carrier in compatible scope, but that carrier is not root-faithful.

**Obstruction:** `O-BSD-A1A1-SELMER-TO-MW-SHA-FAITHFULNESS-R5` — the root-facing lift from `p^∞`-Selmer corank to Mordell–Weil rank is blocked by possible `Sha[p^∞]` corank unless finiteness/corank-zero is proved. This is an atom-local obstruction record, not a new theorem about Sha and not a promoted reusable lesson.

No lesson is promoted. A candidate process hypothesis for future framework work is only this: retrieval for bridge problems should search not only for direct target-cell formulas but also for **weaker certified carriers plus their exact faithfulness kernels**, because a carrier may bypass a saturated representation while exposing a sharper gluing obstruction. This remains a case-study hypothesis, not framework authority.

## 7. Next action

Do not repeat broad “higher Gross–Zagier” or scalar theta-order searches. On the Kolyvagin branch, audit the smallest exact question:

> Under a source-complete compatible auxiliary-`K`/`p` regime where analytic rank two implies `p^∞`-Selmer corank at least two, is there any theorem forcing `Sha(E/Q)[p^∞]` to have corank zero (or otherwise extracting two independent global Mordell–Weil points) from strictly weaker input than the desired BSD rank statement?

If that requires BSD-strength Sha finiteness, rotate again. The direct complex-second-derivative carrier branch remains open in parallel. Root state stays `OPEN_NO_SOLUTION_CERTIFICATE`.
