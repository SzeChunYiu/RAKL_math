atom_id: RH-ANA-002
parent_atom_id: RH-ANA-001
root_issue: 3
status: PRE_CANDIDATE_PACKET_FROZEN_PENDING_EXACT_HEAD_CI
root_authority: NONE

question: >-
  Identify the weakest genuinely zeta-specific coefficient/prime-side condition
  that improves a named partial zero-free region while remaining demonstrably
  weaker than RH. Reject any all-n residual-growth target that the source
  implications already make root-strength.

opened_by:
  calibration_id: CAL-RH-LI-PREFIX-001
  failure_id: F-RH-ANA-001-FINITE-LI-PREFIX
  trace_event_id: RH-ANA-001-E10

source_bound_route_pruning:
  lagarias_finite_place_relation: >-
    S_f(n) equals the incomplete Li coefficient through height sqrt(n), up to
    O(sqrt(n) log n), in the zeta specialization of the source theorem.
  root_strength_warning: >-
    Under RH the source gives polynomial/sqrt(n)log(n) control, while under RH
    failure the incomplete Li term is sometimes exponentially large. Therefore
    an all-n polynomial bound on the exact finite-place term is not a cheap
    intermediate target.
  graded_target_sources:
    - Brown finite Li inequalities <-> zero-free-region implications at source scope
    - Freitas parameterized Li-type zero-free half-plane criteria
    - current explicit zeta zero-free regions as partial-control baselines

candidate_generation_gate:
  allowed: false
  reason: >-
    The strict packet is authored on a research branch. Candidate generation
    remains blocked until exact-head application checks confirm that the context,
    dual-memory review and hash-chained trace pass against current RAKL main and
    no blocking review concern exists.

next_discriminators:
  - Freeze one exact partial zero-free target and the current best source bound at matching heights.
  - Derive the weakest Li/finite-place inequality sufficient for that partial target.
  - Prove the target is strictly weaker than RH before treating it as a child lemma.
  - Freeze cutoff, subtraction, regularization and order-of-limits semantics.
  - Run T-RH-LI-PREFIX-QUARTET-CALIBRATION against any finite-prefix surrogate.
  - Run T-XM-ROOT-BRIDGE-STABILITY-AUDIT with the RH-specific DifferenceWitness.

authority: STRICT_PRE_CANDIDATE_ROUTE_CLASSIFICATION / NO_MATHEMATICAL_CANDIDATE / ROOT_AUTHORITY_NONE
