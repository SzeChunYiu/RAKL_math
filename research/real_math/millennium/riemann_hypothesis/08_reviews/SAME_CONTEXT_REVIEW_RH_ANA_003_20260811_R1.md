# RH-ANA-003 same-context expert cell — R1

**Authority:** SAME_CONTEXT_REVIEW_ONLY / NOT_INDEPENDENT / NO_MATHEMATICAL_CANDIDATE / ROOT_AUTHORITY_NONE

**Bound subject:** `RH-ANA-003` at RAKL_math base `49edbefcb3bd4bab24b154ac509ed933e8d817dc`, using RAKL framework `a521d577724dfedb3123e22cdbac457bce4e22f7` and context `sha256:156df74ce368255b5b85065b49775ff6516ff24956388cc6dbf2b4988b4c154b`.

This cell is a set of role-separated analytical passes over the same frozen packet. It does not count as independent peer review.

## Domain / explicit-formula lead

The active target should be the exact finite-place/archimedean decomposition or an equivalent incomplete-Li remainder, not an informal “prime contribution.” The review requires exact regularization and summation order. A bound on separately absolute-valued terms is low-information if the source identity relies on cancellation.

**Recommendation:** extract the strongest primary-source theorem that explicitly binds a truncation/height scale to the Li index before proposing any new inequality.

## Li / zero-free lead

Finite initial Li inequalities and finite-height zero-free information are inherently range-limited unless a theorem gives a quantitative scale transfer. Any assertion that “density near the line is enough” must survive a hostile world containing one off-line zero consistent with the density statement.

**Recommendation:** treat density as a screening coordinate, not a root bridge, until the remaining single-zero contribution is bounded or excluded.

## Asymptotic / transform lead

Voros-type formulas show that positive or gently varying source-side quantities can enter an oscillatory alternating transform. Source positivity therefore cannot be transported termwise. The meaningful all-index alternative is a global growth-class theorem, but its exact logical strength must be checked against RH.

**Recommendation:** retain the growth route only as a contrastive benchmark and avoid silently restating RH as an `O(n log n)` target.

## Adversarial falsification lead

Use the exact Li summand `1-(1-1/rho)^n`. The modulus of `1-1/rho` is the cheapest local stress test for whether one off-line zero can amplify with n. Preserve the functional-equation/conjugation quartet and do not infer a fixed sign from a single term because phases can cancel.

**Recommendation:** the falsifier should target the claim “sparse off-line zeros or finite-height control is enough for all-index positivity,” not RH itself.

## Formal-assurance lead

This run may produce a source-bound dependency audit and an elementary algebraic identity. It must not produce a new positivity candidate before a future prospective candidate packet passes all gates. Primary-source observations, derived identities, and heuristic envelope estimates must be separately labeled.

**Recommendation:** append a hash-chained trace and TaskEpisode-style shadow record; leave `CANDIDATE_PROPOSED` absent.

## Novelty / research-value lead

The expected output is mainly compositional: known source results plus an elementary hostile calculation used to localize the residual. If verified, classify the scoped subproblem as `RAKL_TRIVIAL` rather than mathematical novelty. The value is route pruning and interface localization.

**Recommendation:** report exact prior-source ancestry and avoid novelty language.

## Consensus next action

Execute `RH-ANA-003-INDEX-HEIGHT-CANCELLATION-AUDIT-R1` only after this packet is frozen. Extract the exact index-height relation, test density/truncation claims with one off-line-zero envelope, and stop at route diagnosis. Mollifier/resonance invention remains deferred until an exact residual coordinate is localized.
