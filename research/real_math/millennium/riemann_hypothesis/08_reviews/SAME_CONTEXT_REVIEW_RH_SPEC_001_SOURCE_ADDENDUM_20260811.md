# Same-context expert review — RH-SPEC-001 canonical-system / local-positivity addendum

Date: 2026-08-11

Authority: `SAME_CONTEXT_REVIEW_ONLY / NOT_INDEPENDENT / SOURCE_AUDIT_ONLY / NO_MATHEMATICAL_CANDIDATE / ROOT_AUTHORITY_NONE`.

This review uses the same six-role cell as the frozen RH-SPEC-001 packet. It evaluates the newly inspected Suzuki canonical-system and Connes–Consani/Connes–Moscovici operator sources. It does not revise the frozen strict context packet and therefore cannot authorize a candidate based on these additions.

## 1. Analytic-number-theory lead

**Background:** zeta/L-functions, explicit formulas, zero criteria.

**Finding:** Suzuki's all-`omega` canonical-system condition and the global Weil positivity target must be treated as exact RH criteria, not as evidence for RH. Their value is to expose precise intermediate obligations and proven restricted/local regions.

**Delegated check:** every future use must identify which implication is unconditional and which step is already RH-equivalent.

**Vote:** `RETAIN_AS_CONTEXT / BLOCK_CIRCULAR_PROMOTION`.

## 2. Spectral/operator lead

**Background:** unbounded/self-adjoint operators, canonical systems, spectral theorem.

**Finding:** the new sources force a sharper separation between (a) well-defined operator/canonical system, (b) positivity or self-adjointness, and (c) exact zeta-zero completeness. Connes–Moscovici is a useful control because an actual self-adjoint operator may have zeta-like ultraviolet behavior without furnishing an exact Hilbert–Polya spectrum.

**Delegated check:** keep `SELF_ADJOINTNESS` and `EXACT_ZERO_CORRESPONDENCE` as non-compensatory bridge rows.

**Vote:** `RETAIN / ADD_NEGATIVE_CONTROL`.

## 3. Arithmetic-geometry / trace-formula lead

**Background:** explicit formulas, arithmetic trace analogues, local/global structures.

**Finding:** Connes–Consani's archimedean-place positivity makes `LOCAL_TO_GLOBAL_ASSEMBLY` a distinct bridge obligation. The local positive compression is mathematically more informative than a generic analogy, but the missing semi-local/global arithmetic composition remains load-bearing.

**Delegated check:** require exact finite-place terms, common test-function class, correction terms, and limiting procedure before any global Weil-sign inference.

**Vote:** `RETAIN / OPEN_GLOBALIZATION_ROW`.

## 4. Adversarial functional-analysis lead

**Background:** pathological domains, regularized traces, continuous spectrum, hidden target encoding.

**Finding:** two cheap falsifiers emerge. First, a de Branges/canonical-system proposal fails if the claimed positivity is simply the RH-equivalent innerness condition in new notation. Second, a spectral proposal fails if it offers only ultraviolet/counting/statistical agreement despite a legitimate self-adjoint operator.

**Delegated falsifiers:** `CRITERION_RENAMING_TEST` and `ASYMPTOTIC_NOT_EXACT_TEST`.

**Vote:** `RETAIN / PRIORITIZE_CHEAP_FALSIFIERS`.

## 5. Formal-methods lead

**Background:** exact statement binding and proof-obligation DAGs.

**Finding:** the planned matrix needs one additional row beyond the original six: `GLOBALIZATION_OR_PARAMETER_CONTINUATION`. It distinguishes `omega>1 -> all omega>0` and `archimedean -> semi-local/global` transitions from ordinary domain/self-adjointness checks.

**Delegated artifact requirement:** each row needs a typed state (`PROVED`, `SOURCE_SUPPORTED_RESTRICTED`, `OPEN`, `FAIL`, `CANNOT_CHECK`) plus exact source/evidence and a cheapest falsifier. Rows must be non-compensatory.

**Vote:** `REVISE_MATRIX_SPECIFICATION`.

## 6. Novelty / research-value lead

**Background:** theorem fingerprinting and prior-art normalization.

**Finding:** de Branges/canonical-system and Weil-positivity routes are established programmes. Recasting them in RAKL's matrix is a research-control improvement, not mathematical novelty.

**Delegated check:** any later child lemma must be searched against de Branges, Suzuki, Connes–Consani, Connes–Moscovici and stronger-parent literature before a novelty label is considered.

**Vote:** `NO_NOVELTY_CLAIM`.

## Joint decision

The team agrees on four source-audit findings:

1. **Coverage correction:** de Branges/canonical systems are sufficiently concrete to deserve their own method-transfer row in a future frozen context revision/new atom.
2. **New orthogonal obstruction coordinate:** local/restricted success must be separated from global/all-parameter validity via `GLOBALIZATION_OR_PARAMETER_CONTINUATION`.
3. **Known-answer negative control:** the prolate self-adjoint operator demonstrates that `self-adjoint + zeta-like asymptotics` is insufficient for exact spectral correspondence.
4. **No candidate authorization yet:** because these sources are not part of the currently frozen MathContextFiber/memory/trace identity and exact-head CI remains a separate gate, the current cycle must remain pre-candidate.

The highest-information next move remains the route-independent operator-bridge matrix, now with seven non-compensatory rows:

1. state space/domain;
2. self-adjointness or positivity;
3. trace/distribution/regularization legitimacy;
4. exact prime-power and archimedean matching;
5. complete zero correspondence/multiplicity;
6. non-circularity;
7. globalization or parameter continuation.

The first child atom should be selected only after the governing strict packet is machine-audited, and should be the cheapest row whose resolution partitions the largest set of route families.