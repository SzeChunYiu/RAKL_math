# RAKL_METHOD_CASE_STUDY — RH-ANA-003

**Authority:** proposal/shadow research-process evidence only. No theorem, tool, framework, or RH-root authority.

## Active atom and problem signature

- Atom: `RH-ANA-003`
- Root: Riemann Hypothesis, exact all-n Li positivity route
- Signature: exact finite-place Li arithmetic formula; prime/background cancellation; PNT error `E(x)=psi(x)-x`; growing associated-Laguerre kernel; uniform-in-`n` signed transform.
- Frozen fibre: `RH_ANA_003_CONTEXT_FIBER_20260811.json`, hash `sha256:cf1aba3880a0deef2e0c6f14e5e5a4e51c41cf15db15bce9b4dc3188fa3cbdc9`.

## Research method used

1. Freeze the existing child atom rather than proposing another positivity object.
2. Reconstruct Lagarias' exact `S_f(n)` and `eta_j` conventions from the primary source.
3. Collapse the binomial sum before applying any bound, obtaining the single kernel
   `K_n(t)=L_{n-1}^{(1)}(-t)` and the exact continuous counterterm.
4. Rewrite the finite-place term as a Stieltjes transform of `d(psi-x)` and, for each fixed `n`, as a signed PNT-error transform against `W_n=K_n-K_n'`.
5. Compare the information type of current primary-source PNT estimates with the root obligation: two-sided pointwise magnitude versus one-sided family-uniform signed control.
6. Use a hostile symmetric-envelope alignment only as a proof-strategy stress test. Do not represent it as a possible zeta error function.
7. Freeze five same-context expert roles and dual-memory review before any mathematical candidate.

## Fibre and experience actually consulted

### Retrieved

- Tool: `T-RH-LI-PREFIX-QUARTET-CALIBRATION`.
- Failures: `F-RH-ANA-001-FINITE-LI-PREFIX`, `F-RH-ANA-002-SUZUKI-NORM-NO-WEAKER-BRIDGE`.
- Prior memory: `RH_ANA_002_RESEARCH_MEMORY_REVIEW_20260811.json`.
- Primary sources: Lagarias `arXiv:math/0404394v4`, Johnston `arXiv:2411.13791`, Bellotti `arXiv:2508.02041`.

### Selected

- Exact source-bound binomial collapse / transform localization.
- Finite-prefix tool only as a regression warning against finite-`n` extrapolation.
- Quantitative `M_n(B)=integral B|W_n|` comparison as the next discriminator.

### Rejected or deferred

- Termwise bounding of individual `eta_j`: rejected because it destroys the exact cancellation before the root-relevant combination is formed.
- Finite-prime or finite-`n` positivity evidence: rejected by the existing finite-prefix failure record absent an all-index theorem.
- Immediate mollifier/resonance candidate: deferred until it maps to the exact residual or supplies a DifferenceWitness.
- Broad claim that pointwise PNT bounds can never help: rejected by the adversarial expert; a sufficiently strong quantitative absolute bound could in principle be useful, so the correct next step is a quantitative family audit.

## Decision policy and falsifier

The next action was chosen by smallest-residual / highest-information logic. The exact source algebra exposes a quantitative discriminator before any new theorem invention. The hostile envelope test asks whether a derivation obtains directional information from a symmetric magnitude envelope. It falsifies only that inference pattern; it does not falsify the arithmetic PNT error or explicit-formula research family.

## Outcome and residual

**Outcome:** `PARTIAL_SUCCESS`.

What worked:
- the previously vague prime/archimedean cancellation was localized to an exact signed transform;
- fixed-`n` regularization and boundary behavior are source-bound;
- the distinction between magnitude control and signed/correlation control is explicit.

What did not close:
- no useful uniform-in-`n` one-sided bound was proved;
- no theorem candidate was generated;
- no strict weakening of RH was established;
- novelty of this exact transform representation in the literature was not assessed.

Residual:
`bound the signed PNT-error transform uniformly in n below S_infty(n)-n+1, or expose the weakest additional zeta-specific signed/correlation statistic that does so and prove that statistic is strictly weaker than RH`.

Primary failure category: **representation / analytic-uniformity bridge**, with no local mathematical refutation.

## RAKL process surfaces used

Canonical `method_specs.py` surfaces used in this cycle:

- `decomposition`
- `routing`
- `search_query_generation`
- `source_selection_reliability`
- `claim_extraction`
- `mathematical_context_translation`
- `equivalence_similarity`
- `gap_discovery`
- `experiment_query_selection`
- `memory`
- `review`
- `saturation_stopping`
- `prompting_context_policy`

No `authority_promotion` was attempted.

## Saturation and novelty

Retained semantic novelty inside the RAKL application state, not literature novelty:

- KNOWLEDGE: `0`
- OPERATOR: `0`
- EXPERIENCE_PATTERN: `0`
- OBSTRUCTION: `1` — the exact one-sided uniform signed-transform obligation.
- RELATION: `1` — the source-derived `S_f -> Laguerre-kernel PNT-error transform` relation.
- PATH: `0`
- META_METHOD: `0`

Axes reopened: `RELATION`, `OBSTRUCTION`.  
Axis flatness: `CANNOT_MEASURE` because this application lane does not materialize a canonical `SaturationVectorState` for RH-ANA-003 in the repository.

Solved-subproblem novelty class: `UNRESOLVED`; the cycle localized a representation but did not solve a mathematical subproblem under the v3 novelty classifier.

## Did RAKL change the action?

`CANNOT_MEASURE`: no pre-memory/pre-gate action preference was frozen before the dual-memory and context gates, so a causal action-change counterfactual cannot be claimed. The observable fact is narrower: existing failure records explicitly disallowed finite-prefix promotion and the final trace selected an exact-transform audit instead.

## Framework-improvement hypothesis

**Proposal only:** application lanes still lack a canonical serialized `RAKLV3State` snapshot and first-class cycle-receipt schema on current framework `main`, so true pre/post `state_fingerprint` and causal action-change attribution are often not measurable. Repeated evidence across lanes should be required before opening a framework challenger; this single RH cycle is insufficient for promotion or a framework issue by itself.

## Next discriminator

Quantify or analytically bound the induced sign-blind majorant family under a current primary-source PNT envelope and compare it with the exact archimedean threshold. If it is noncompetitive, open a child residual around the minimal signed/correlation information required, not another generic positivity surrogate.

**Root:** `OPEN_NO_SOLUTION_CERTIFICATE`.
