# RAKL_METHOD_CASE_STUDY — RH-ANA-004a R3

**Authority:** proposal/shadow process evidence only. Experience guides search and does not certify mathematics, novelty, review independence, or root closure.

## Atom / signature

`RH-ANA-004a — PRIME_ERROR_LAGUERRE_ROOT_STRENGTH_AUDIT`.

Signature: RH exact root; all-index Li sensitivity; Coffey `S_2=S_gamma+S_Lambda`; signed von-Mangoldt/PNT-error component; Laguerre transform; explicit-formula truncation; root-strength audit of child success contracts.

## Actual fibre

The cycle consulted current `RAKL@main` v3/method specs, the current-work coverage schema/constructor, exact RH root contract, open RH work #3/#10/#40/#53/#80/#95/#118/#125/#147/#151/#160 under the recorded search surfaces, the #147 R2 cancellation-preservation packet, #118's same-atom density/exception route, #160's current-work diagnosis, Coffey arXiv:`0706.0343v2` and `math-ph/0505052v1`, Johnston--Yang arXiv:`2204.01980`, Johnston--Cully-Hugill arXiv:`2402.04272` current version dated 2026-03-22, and Dunster--Gil--Segura arXiv:`1705.01190`. The exact compact fibre snapshot is `sha256:0a01b3e5c91c98896ec9ec5c336be9f097d15cdcf1ff7a0e72512dc7ecba1dab`.

## Expert cell / delegated checks

The analytic-number-theory lead owned the `A(x)=floor(x)-psi(x)` Abel transform and convergence boundary. The Li/positivity specialist owned the check that proposed global `S_Lambda/S_2Lambda` growth is already RH-sufficient/root-scale. The special-functions lead owned the Laguerre derivative identity, generating-function Laplace transform and fixed-`x` asymptotic limitations. The explicit-formula/zero-density lead owned the current finite-height source interface and distinguished fixed-`T` from selected-`T*` statements. The adversarial proof auditor checked signs, the `floor(x)` term, the modulus boundary `|rho|/|rho-1|`, and prohibited an infinite zero-sum interchange. The v3 provenance/metrology lead owned current-work reconciliation, chronology, authority and telemetry. These are same-context roles and receive zero independent-review credit.

## Operators / motifs tried

Used: current-work binding; root-strength/equivalence audit; finite Abel summation; Laguerre derivative/contiguous identity; Laguerre generating-function Laplace transform; single-zero-mode hostile calibration; source-interface comparison; `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` under DifferenceWitness. Reused motif `M-RH-ANA-CANCELLATION-BEFORE-MAJORANT`. New proposal motif: `M-RH-ANA-ROOT-STRENGTH-BEFORE-GLOBAL-BOUND`.

Rejected/saturated: finite Li-prefix calibration; termwise absolute eta/S_Lambda majorants; zero-density rarity as universal exclusion; fixed-`x` Fejer asymptotics summed over infinite prime support; treating a sharper pointwise selected-`T*` explicit formula as one fixed zero sum under an integral.

## Prior experience retrieved, rejected, and missed

Selected: `EP-RH-ANA-003-DENSITY-EXCEPTION-20260811`, `EP-RH-ANA-003a-MAGNITUDE-TRANSPORT-20260811-R2`, `EP-XM011-CURRENT-WORK-COVERAGE-20260811-R7`, `L-RH-ANA-003a-PRESERVE-SIGNED-TRANSFORM-BEFORE-MAGNITUDE-BOUND`, and `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`.

Rejected: `T-RH-LI-PREFIX-QUARTET-CALIBRATION`, `F-RH-ANA-001-FINITE-LI-PREFIX` as the immediate discriminator, spectral PRs #40/#53, and cross-lane PRs #95/#125/#151 for lack of a target-local analytic bridge.

Prior cycle #147 had missed pre-existing same-atom #118. XM011/#160 later diagnosed that omission. This cycle found and selected both before consequential routing. That prior retrieval failure is linked, not silently erased.

## Decision policy and action change

Observable pre-current-work/pre-memory preference: continue `RH-ANA-003b` directly and search for the strongest unconditional global `S_Lambda` bound.

After current-work binding and memory: reconcile #147's `RH-ANA-003b` under #118's `RH-ANA-004` as `RH-ANA-004a`; first test the strength of the proposed global-bound child contract; preserve signed cancellation; derive the exact prime-error/Laguerre representation; rotate to a finite/truncated `X,T,n` transport problem when the global bound is recognized as root-scale.

RAKL changed the chosen action: **YES**. The current v3 `CurrentWorkCoverageReceipt` was causally material: without it, the run would have repeated the previously diagnosed same-atom coverage failure.

## Falsifiers / verification

Counterexample-first/root-strength falsifier: Coffey's source itself states that the relevant global polynomial-growth condition is sufficient for RH, so it fails the test "strictly weaker child obligation" even though it remains mathematically meaningful.

Representation verification: exact finite Abel summation plus Laguerre identities give the prime-error kernel; a generating-function calculation gives `I_n(s)=n+1-s+s(1-1/s)^(n+1)`; the single zero-mode maps to `n/rho+1-(1-1/rho)^(-n)` with modulus transition exactly at `Re(rho)=1/2`. SymPy checks for `n=1,...,5` were calibration only.

Source-interface verification found and corrected an over-strong shorthand in the first result serialization. The append-only erratum records that the actual remainder `E(x,T*)` must stay inside the integral before any magnitude bound, and that the 2026 selected-`T*` theorem does not automatically provide one common finite zero set over an `x`-integration interval. This is a verification/gluing correction, not a mathematical theorem failure.

## Outcome / residual

Outcome: `PARTIAL_SUCCESS_REPRESENTATION_LOCALIZATION_AND_DECOMPOSITION_REPAIR`.

Retained route-level result: `S_Lambda` has an exact fixed-`n` PNT-error/Laguerre representation, and its single-zero-mode transform displays the critical-line modulus boundary. This is not an RH proof and not a literature-novelty claim.

Residual: `RH-ANA-004b — TRUNCATED_LAGUERRE_EXPLICIT_FORMULA_TRANSPORT`: choose a source interface (fixed `T` or selected `T*`), preserve the actual remainder function, and obtain explicit finite `X,T,n` control without assuming root-scale global growth or silently changing the finite zero set under integration.

## Failure categories

`F-RH-ANA-004a-GLOBAL-POLYNOMIAL-BOUND-AS-CHILD`: **decomposition/meta-policy** — the child contract was root-strength.

`F-RH-ANA-004a-FIXED-X-FEJER-TO-PRIME-SUM`: **gluing/verification** — fixed-`x` special-function asymptotics do not justify the infinite arithmetic sum.

`F-RH-ANA-004a-TSTAR-SELECTION-TO-LAGUERRE-INTEGRAL`: **source-interface/gluing/verification** — an existential pointwise truncation choice is not automatically a common integration parameter.

No source was shown false; no zero-density theorem was refuted; no mathematical counterexample to RH was constructed.

## Seven saturation axes

`KNOWLEDGE=REOPENED`, `OPERATOR=FLAT`, `EXPERIENCE_PATTERN=REOPENED`, `OBSTRUCTION=REOPENED`, `RELATION=REOPENED`, `PATH=REOPENED`, `META_METHOD=FLAT`.

Retained semantic novelty vector for this proposal/shadow cycle: `KNOWLEDGE=1`, `OPERATOR=0`, `EXPERIENCE_PATTERN=1`, `OBSTRUCTION=1`, `RELATION=1`, `PATH=1`, `META_METHOD=0`. Raw files/commits are not counted.

Novelty class: `REPRESENTATION_NOVEL` at application-state scope only; no literature novelty is asserted.

## What v3 helped / what is missing

What helped: `CurrentWorkCoverageReceipt` directly repaired the prior same-atom omission; immutable episode-versus-diagnosis separation prevented treating the route diagnostic as theorem authority; `equivalence_similarity`, `contextual_theory_gluing`, `contradiction_diagnosis`, `gap_discovery`, `review`, and seven-axis saturation made the root-strength and source-interface failures explicit.

Missing/weak surface: the method has no dedicated child-success-contract **root-strength lint** that asks, before theorem search, whether a proposed child condition is already known equivalent or sufficient for the root.

Framework-improvement hypothesis: add a proposal-only `child_contract_strength_audit` process surface that binds known equivalence/sufficiency relations and forces re-atomization when a child contract is root-scale. This hypothesis is not promoted to a framework issue in this run; one case is evidence for design, not enough by itself to mutate framework authority.

## Repeated-process-failure linkage

The prior `F-XM011-RH-CURRENT-WORK-COVERAGE-MISS` / RAKL current-work-coverage defect is **not repeated** here: the current v3 receipt changed routing before mathematics. The new source-interface/gluing failures are linked to prior `F-RH-ANA-003a-MAGNITUDE-TO-LI-SIGN` as the same broader local-to-root bridge-stability family, but they remain mathematically distinct.

**Root:** `OPEN_NO_SOLUTION_CERTIFICATE`.
