# RAKL_METHOD_CASE_STUDY — RH-ANA-003 R1

**Framework:** `SzeChunYiu/RAKL@a521d577724dfedb3123e22cdbac457bce4e22f7`  
**Application base:** `SzeChunYiu/RAKL_math@49edbefcb3bd4bab24b154ac509ed933e8d817dc`  
**Atom:** `RH-ANA-003`  
**Authority:** SELF-RAKL CASE-STUDY EVIDENCE / SEARCH_CONTROL_ONLY / ROOT_AUTHORITY_NONE

## Research method used

The cycle used a **target-residual-first dependency audit** rather than theorem invention. After freezing a fresh v3 context/memory/expert/trace packet, it re-opened the primary Lagarias and Voros sources, extracted the exact finite-place and oscillatory-transform interfaces, and ran a hostile one-off-line-zero calculation against finite-height and density-only extrapolation.

The selected operator/tool was `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` at search-control authority. The operative motif was:

```text
finite or aggregate surrogate
-> identify exact root-critical residual
-> bind source-to-target scale relation
-> stress-test one surviving defect
-> rotate only after interface is explicit
```

No new mathematical candidate was generated.

## Fibre contents actually consulted

The active fibre contained:

- exact child `RH-ANA-003` and its parent `RH-ANA-002`;
- the finite-Li-prefix failure `F-RH-ANA-001-FINITE-LI-PREFIX`;
- the Suzuki faithfulness episode `EP-RH-ANA-002-SUZUKI-FAITHFULNESS-20260811` and source-bounded no-weaker-bridge failure;
- `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` as the selected reusable diagnostic;
- Lagarias' finite-place/incomplete-Li and height-to-index formulas;
- Voros' alternating/binomial `Z(j)` representation and asymptotic dichotomy;
- Bombieri–Lagarias as the arithmetic explicit-formula parent source.

Retrieved but rejected for the current action:

- `T-RH-SPEC-HURWITZ-DOMAIN-ZERO-TRANSPORT`, because it is target-side spectral/entire-function transport rather than source-side arithmetic residual control;
- immediate mollifier/resonance invention, because no applicability witness currently maps those methods to the exact incomplete-Li residual;
- finite-prime positivity, because it repeats the finite-surrogate family absent a uniform all-index tail theorem.

## What worked

1. **Prior failure memory changed the action before invention.** The finite-prefix failure prevented a numerical/finite-height continuation, while the Suzuki episode forced a representation rotation away from another positive surrogate.
2. **Primary-source scale extraction contracted the residual.** Lagarias identifies the finite-place contribution with an incomplete Li coefficient at height about `sqrt(n)` plus `O(sqrt(n) log n)`, and explicitly binds RH verified to height `T` to a bounded index window `n <= T^2/[4(log T)^2]`.
3. **Counterexample-first reasoning exposed the density gap cheaply.** The exact identity `|1-1/rho|^2 = 1+(1-2 Re rho)/|rho|^2` shows that one reflected off-line zero has an amplifying modulus greater than one. This is enough to reject any density-only inference that remains compatible with such a zero, without pretending to prove RH or a sign theorem.
4. **Voros supplied a hostile transform control.** Positive and gently varying `Z(j)` still enter an oscillatory alternating transform, blocking termwise-positive source-coordinate reasoning.

## What failed or was deliberately rejected

No theorem candidate failed because candidate generation was intentionally deferred. Three route families were rejected at search-control level:

- **finite-height / finite-prime extrapolation:** fails at the all-index gluing interface unless a scale-uniform closure theorem is supplied;
- **zero-density as direct closure:** average/sparsity information does not exclude one root-critical off-line zero;
- **mollifier/resonance now:** representation-to-target mapping is not yet explicit, so importing the machinery would be premature.

The cycle did **not** prove a new unconditional bound on the incomplete Li coefficient. That remains the mathematical residual.

## Failure classification

- **Local mathematics:** OPEN. The desired all-index incomplete-Li control remains unproved.
- **Representation:** IMPROVED. The active finite-place residual is sharper than “prime cancellation” in the abstract.
- **Local-to-global / gluing:** PRIMARY DIAGNOSIS. Finite height `T` maps only to a bounded Li-index window; density/finite evidence requires an unbounded closure bridge.
- **Retrieval:** NO OBSERVED MISS IN THIS CYCLE. Relevant parent sources and prior failures were found. Cross-problem coverage remains subject to the separate framework issue `RAKL#119`.
- **Verification:** SOURCE-BOUND, with two PDF pages visually audited and the elementary modulus identity checked directly. No formal theorem proof was attempted.
- **Tooling/CI:** no scientific tooling failure observed during the research action; exact-head repository CI remains a downstream integration check.

## Episode -> diagnosis -> reusable lesson

**Episode:** `EP-RH-ANA-003-INDEX-HEIGHT-20260811-R1`.

**Diagnosis:** the first useful finite-place coordinate is an incomplete Li coefficient whose required height grows with `n`; finite-height/density success can therefore leave a root-critical single-defect residual untouched.

**Reusable lesson proposal:** `L-RH-ANA-003-INDEX-HEIGHT-BOUNDARY-R1`. Before assigning all-index relevance to finite-height, density, or finite-prime evidence, require an explicit index-height/remainder theorem and stress-test one surviving defect. The lesson is `CANDIDATE` / search-priority-only and may be falsified.

This diagnosis is related to, but does not duplicate, the broader cross-lane pattern tracked in `RAKL#124`: a locally meaningful surrogate is not a root-critical coordinate without a preservation/gluing bridge.

## Seven-axis saturation update

- `KNOWLEDGE`: **REOPENED** — source-bound finite-place/index-height relation entered the active fibre.
- `OPERATOR`: **FLAT** — no new operator.
- `EXPERIENCE_PATTERN`: **REOPENED** — finite/aggregate success can coexist with a single-defect all-index residual.
- `OBSTRUCTION`: **REOPENED** — incomplete Li coefficient near `sqrt(n)` sharpened the obstruction.
- `RELATION`: **REOPENED** — explicit `T -> n` window bound.
- `PATH`: **REOPENED** — future methods must map to the incomplete-Li residual before deployment.
- `META_METHOD`: **FLAT** — no new framework mechanism warranted from this episode alone.

The fibre is not saturated.

## Solved-subproblem novelty class

`RAKL_TRIVIAL` / compositional for the scoped route diagnostic. The result composes already-published source results with elementary algebra and existing RAKL operators. It is not claimed as new mathematics.

## v3 feature that helped

The most useful v3 mechanisms were the immutable TaskEpisode-style record, prior-failure routing, explicit selected-versus-rejected memory, scoped lesson proposal, and seven-axis saturation vector. Together they prevented a tempting but low-information move from “positive source coordinates” or “many zeros verified” directly to another positivity candidate.

## v3 feature missing or weak

The current `TaskEpisode.cost` is a scalar, while this cycle naturally needed a vector effort receipt: source families reopened, PDF pages audited, repository-state reads, retrieval calls, and candidate count. The shadow episode therefore stores a `cost_proxy` extension that is **not** claimed as canonical v3 schema.

Separately, cross-problem retrieval coverage still lacks the content-bound coverage receipt already tracked in `RAKL#119`; no duplicate framework issue is opened here.

## Framework-improvement hypothesis

**Hypothesis:** a canonical vector-valued episode metrology receipt, kept non-authoritative and bound to the exact TaskEpisode, would make residual contraction per unit research cost measurable without overloading the existing scalar `cost`. The cheapest benchmark is retrospective parsing of several current Millennium TaskEpisodes followed by a prospective matched run that compares whether the richer cost vector changes routing decisions or merely adds logging overhead.

This is one case-study hypothesis only. It does not justify a framework mutation or a new RAKL version by itself.

## Next action

Keep `RH-ANA-003` open. In the next non-duplicative analytic cycle, search for a primary-source theorem that acts **directly** on the incomplete-Li finite-place term (or an exactly equivalent residual) with all-`n` uniformity. Zero-density/zero-free, mollifier, resonance, or prime-sum machinery should be admitted only with a target-specific applicability map and the cheapest hostile falsifier. If no such map survives, rotate to the Lagarias–Voros growth-class interface rather than repeating finite-surrogate families.
