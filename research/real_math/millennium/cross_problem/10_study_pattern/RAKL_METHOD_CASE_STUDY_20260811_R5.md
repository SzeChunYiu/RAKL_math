# RAKL_METHOD_CASE_STUDY — cross-Millennium observer R5

**Frozen application base:** `RAKL_math@812addd25a7f34d3c6272143e21d5d7db34539aa`  
**Current semantic framework inspected before mathematical work:** `RAKL@3299072b410ac9136548dfd103e846fc7656c31e` (`method_version=3.0.0`)  
**Application execution pin at frozen base:** `RAKL@787c7e00af2a5877ccb715bc807ec14f52974e9c`  
**Active observer atom:** `XM009-HODGE-BSD-JET-FAITHFULNESS`  
**Active fibre:** `sha256:e7a7470e3737aeb020ab7724fd127a4c0384ba75c070093eb08358ddbedbb004`  
**Authority:** `PROPOSAL_SHADOW / METHOD_OBSERVER / SEARCH_CONTROL_ONLY / SAME_CONTEXT_REVIEW / NO_ROOT_OR_FRAMEWORK_AUTHORITY`

## Chronology and observer cell

This round preserves the chronology of earlier cross-problem observations rather than rewriting them into one synthetic history. The relevant retained sequence is: XM005 moving-core/root-critical-coordinate calibration; XM007 state-projection congruence; XM008 root-interface-preserving source substitution; and the present XM009 jet-faithfulness discriminator. Historical cross-Millennium records are used only at their recorded authority.

Eight same-context roles shared the same evidence: P-vs-NP/complexity lead, RH/analytic-number-theory lead, Navier–Stokes/PDE lead, Yang–Mills constructive-QFT lead, Hodge/deformation lead, BSD/arithmetic-geometry lead, adversarial transfer/provenance lead, and RAKL metrology/authority lead. Role separation is an internal error-finding device only and receives no independent-review credit.

## P versus NP — latest consequential lane episode sampled

**Atom/signature.** Open PR #128, `O9d12a2a1a1d`, asks whether absolute free-base trace cardinality/entropy/VC dimension can be a lower-bound coordinate for cyclic intersection complexity on the graph discrete space.

**Decomposition and actual fibre/search universe.** The lane fixes `B=G_{N,N}`, one row `A=R_1`, the cyclic-intersection cost, free union closure, and the exact restriction family on `A`. It also carries the earlier free-union quotient warning only as unmerged search-priority evidence.

**Retrieved/selected/rejected experience.** The free-union quotient warning was selected as a structural warning, not theorem authority. Broad absolute trace-richness candidates were attacked before target-relative/incremental variants. A complete relevance-labelled memory universe was not frozen, so missed relevant experience is `CANNOT_MEASURE`.

**Decision policy.** Run the cheapest zero-cost hostile baseline before scoring explicit hard graphs with a new potential.

**Falsifier/verification.** Since `A` is already a generator, counted cyclic-intersection cost is zero. The union of any selected subset of columns restricts to exactly that subset on `A`, so the trace family is the full power set and has cardinality `2^N` and VC dimension `N` at zero counted cost.

**Outcome/residual.** Absolute free-base trace cardinality/entropy/VC coordinates are falsified as standalone lower-bound coordinates. The residual is a normalized, target-relative or incremental coordinate with a legal per-intersection marginal accounting law.

**Failure class and gluing.** `REPRESENTATION + DECOMPOSITION`. The finite combinatorial calibration succeeds locally; no P-vs-NP lower bound or root bridge is glued.

**Saturation / novelty / motif.** Retained seven-axis semantic novelty is explicitly zero on all axes in the lane receipt; novelty class is `compositional` at shadow authority. Positive motif: `ZERO_COST_BASELINE_FIRST` — test whether the proposed quantity can already be arbitrarily rich before the counted operation is used.

## Riemann Hypothesis — RH-ANA-003

**Atom/signature.** Open PR #118 audits whether current aggregate zero-density, zero-free, and mollifier/proportion controls can by themselves produce the universal all-index Li-sign bridge.

**Decomposition and actual fibre/search universe.** The frozen fibre contains the exact RH/Li target, Bombieri–Lagarias/Guinand–Weil sensitivity, current zero-density and zero-free inputs, mollifier proportions, and a planted symmetric off-line quartet as an inference-form calibration.

**Retrieved memory.** Tools retrieved and selected: `T-RH-LI-PREFIX-QUARTET-CALIBRATION`, `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`. Failures retrieved and selected as guidance: `F-RH-ANA-001-FINITE-LI-PREFIX`, `F-RH-ANA-002-SUZUKI-NORM-NO-WEAKER-BRIDGE`, `F-XM001-POINTWISE-GAP-COLLAPSE`; the cross-problem failure is scope-limited to bridge-audit procedure. Later-found missed-memory IDs in the bound review are empty.

**Decision policy.** Run a single-exception falsifier before inventing a new prime-side inequality or optimizing an aggregate estimate.

**Falsifier/verification.** A planted quartet closed under conjugation and functional-equation reflection remains logically compatible with aggregate rarity/boundary-exclusion statements while a left partner has Li-transform modulus greater than one. The planted set is not asserted to be the actual zeta zero set.

**Outcome/residual.** `REFUTED_ROUTE_LOCAL / PARTIAL_SUCCESS_AS_OBSTRUCTION`: aggregate rarity alone does not supply exceptional-set extinction or exact neutralization. Residual: an exceptional-zero-sensitive exact cancellation/extinction/neutralization mechanism uniform in Li index.

**Failure class and gluing.** `REPRESENTATION + BRIDGE_GLUING`: “few” is not “none,” and an aggregate count discards contribution-level information needed by a universal sign target.

**Saturation / novelty / motif.** Retained vector `K=3, O=0, E=1, B=1, R=2, P=1, M=0`. Positive motif: `SINGLE_FATAL_EXCEPTION_FIRST`.

## Navier–Stokes — NS-B1a3b1 R2

**Atom/signature.** Current-main metrics for `NS-B1a3b1-C001-R2` audit whether a log-BMO consumer survives the finite-I blow-up zoom and whether its producer/global interfaces close.

**Actual fibre and memory.** The exact recorded fibre hash is `sha256:0c9d8dd85e7dc09ea19bd94fb74aea31f415977bb6a54fc8afc69d3aa66f47f7`. Eight success/analogue items were retrieved, five selected and three rejected/deferred. Eight failure experiences were retrieved, five selected and three rejected. One prior episode and two tools were retrieved; zero motifs were recorded. Missed relevant memory is `CANNOT_MEASURE` because no exhaustive relevance-labelled universe was frozen.

**Routing effect.** The observable pre-memory preference was to push direction regularity through the finite-I blow-up passage. Memory changed the action to split consumer zoom stability from producer generation and audit global/far-field/time/state-space inputs first.

**Verification/outcome.** The zoom representation lemma succeeded. Outcome is `PARTIAL_SUCCESS / REPRESENTATION_BRIDGE_VERIFIED_PRODUCER_AND_GLUING_OPEN`.

**Failure class and gluing.** No local mathematical failure for the zoom lemma. The remaining failure is explicitly local-to-global: global Lorentz/far-field consumer input, plus a pre-singularity versus ancient-Liouville state-space mismatch.

**Saturation / novelty / motif.** Retained vector `K=1, O=0, E=0, B=1, R=1, P=1, M=0`. Positive motif: `SCALE_STABLE_CONSUMER_MOVES_OBSTRUCTION_UPSTREAM`.

**Process negative history.** The lane detected and repaired an intermediate result-trace hash mismatch before PR integration. This is tooling/provenance evidence; the local mathematics did not change.

## Yang–Mills — YM-E4b2b

**Atom/signature.** Open draft PR #140 tests the generic inference from ordinary tightness/weak convergence plus clustering to the relative Osterwalder–Schrader form domination required by the cited continuum argument.

**Actual fibre/search universe.** The lane freezes one same-cylinder reflection-positive probability-family interface, the relative form inequality, weak convergence/tightness, clustering, and the source-bound Appendix-D/Section-7 claim neighborhood. Numerics are not used for theorem authority.

**Decision policy.** Before attempting stronger Yang–Mills-specific estimates, test whether the proposed generic implication is true in a transparent reflection-positive probability model.

**Falsifier/verification.** A product model has tightness, weak convergence, pointwise OS-form convergence and exact independence/clustering, yet a cutoff vector has positive form while its limit form is zero, ruling out a finite uniform relative-domination constant in that generic setting.

**Outcome/residual.** Generic `tightness + weak convergence + clustering => relative OS domination` is falsified. Residual: a genuinely relative same-theory regulator/OS-form theorem or exact null-inclusion plus bounded extension. No Yang–Mills-specific derivation is refuted.

**Failure class and gluing.** `REPRESENTATION + BRIDGE_GLUING`. No continuum mass-gap theorem is locally disproved; the tested generic bridge fails.

**Saturation / novelty / motif.** The lane records zero retained semantic novelty on all seven axes. Positive motif: `RELATIVE_FORM_BEFORE_ABSOLUTE_CONVERGENCE`.

**Process negative history.** Its original cycle-metrics payload hash was circularly derived from an intermediate in-memory object. An append-only correction preserved the negative history and changed no mathematics. Current RAKL main already has lifecycle-aware artifact-contract coverage and non-self-referential method-telemetry hashes, so this observation is not used to open a duplicate framework issue.

## Hodge conjecture — H4d1c

**Atom/signature.** Open PR #119 asks whether full tangent surjectivity onto a possibly nonreduced Hodge-locus scheme is actually the correct root-facing target when pointwise rational variational algebraicity concerns the reduced support.

**Decomposition and fibre.** The lane separates `T_sch`, `T_red`, and smooth reduced locus; exact signed rational witness bookkeeping; proper witness-incidence domination; generic differential surjectivity; higher Artin/formal lift; and global continuation.

**Retrieved/selected experience.** Dan's nonreduced Noether–Lefschetz examples and Kloosterman's complete-intersection variational-Hodge setting are selected as scoped mathematical controls. Earlier H4d1/H4d1a detector failures prevent equating Hodge persistence with witness mobility. Cross-domain analogies were rejected because they do not preserve scheme structure/category/coefficient data.

**Decision policy.** Audit target strength before inventing a tangent-surjectivity mechanism.

**Falsifier/verification.** Nonreduced Hodge-locus controls plus the elementary distinction between a scheme and its reduction show that full scheme tangent data can be strictly stronger than support-level point reachability.

**Outcome/residual.** `PARTIAL_SUCCESS / REPRESENTATION_BOUNDARY_CORRECTION / ROUTE_PRUNING`. Successor target is exact-category proper witness-incidence domination of the irreducible reduced Hodge branch; higher-order/formal/global obligations remain separate.

**Failure class and gluing.** `REPRESENTATION`, specifically target scheme-structure overreach. Local target normalization succeeds; witness construction and global gluing remain open.

**Saturation / novelty / motif.** Retained vector `K=1, O=0, E=1, B=1, R=1, P=1, M=0`. Positive motif: `TYPE_SCHEME_LEVEL_BEFORE_TANGENT_TARGET`.

## Birch and Swinnerton-Dyer — BSD-A1a1 R4

**Atom/signature.** Open PR #142 normalizes the missing bridge to the exact target cell `NUMBER_FIELD + WEIGHT_TWO_ELLIPTIC_CURVE + COMPLEX_S_DERIVATIVE_ORDER_TWO + ROOT_FAITHFUL_ARITHMETIC_OUTPUT`.

**Decomposition/search universe.** The lane separates three overloaded “higher Gross–Zagier” directions: higher complex derivative order in a function-field setting, higher cycle/weight/codimension formulas, and higher p-adic/Hida deformation derivative order. It also keeps the downstream Mordell–Weil/regulator/Sha/Tamagawa/torsion gluing obligation separate.

**Retrieved/selected/rejected experience.** Function-field and p-adic higher-derivative results are retained as structural analogues/DifferenceWitnesses, not target-cell evidence. Current Castella–Hsieh/related rank-two generalized-Kato sources are target-near but not reversed beyond their stated hypotheses. Bounded source completeness remains `CANNOT_MEASURE`.

**Decision policy.** Normalize the exact derivative coordinate and output category before expanding the literature search.

**Outcome/residual.** No theorem was located in the bounded exact target cell; this is not a literature-wide absence claim. Residual: find an exact complex-second-derivative arithmetic carrier or a comparison theorem identifying an existing arithmetic object with that carrier, then separately close arithmetic gluing.

**Failure class and gluing.** `ONTOLOGY + REPRESENTATION/SOURCE` at the local search boundary; downstream global arithmetic gluing remains separate.

**Saturation / novelty / motif.** Retained vector `K=1, O=0, E=0, B=0, R=1, P=0, M=0`; novelty class `ontology`. Positive motif: `NORMALIZE_TARGET_COORDINATE_BEFORE_SOURCE_EXPANSION`.

## XM009 cross-problem transfer/falsifier

**Source atom.** Hodge `H4d1b`, especially `F-H4D1B-FIRST-ORDER-NOT-FORMAL`.

**Target atom.** BSD `BSD-A1a1-THETA-ORDER-COMPARISON`, exact `COMPLEX_S_DERIVATIVE_ORDER_TWO` child target.

**Common abstraction.** An exact kth-order target may be transported only through a comparison faithful to the relevant kth-jet/filtration. A similarly named derivative or a weaker truncation is not sufficient.

**Enabling assumptions.** In the one-variable calibration, require an exact source-bound germ identity `g(U)=u(U)f(phi(U))`, the same distinguished point, `u(0) != 0`, `phi(0)=0`, nonzero linear coefficient of `phi` for order preservation, exact lower-jet vanishing, and a nonzero leading target coefficient.

**Disanalogies / DifferenceWitness.** Hodge first-order tangent/dual-number lifting is not BSD arithmetic; complex `s` is not a p-adic/Hida/anticyclotomic or function-field parameter; Hodge obstruction complexes, Shtuka/Frobenius geometry, Selmer classes and regulators do not transfer. Only the truncation/coordinate-faithfulness audit transfers.

**Cheapest falsifier.** For `f(T)=T^2`, a small perturbation `T^2+epsilon*T` has order one for every nonzero epsilon, while the exact ramified substitution `phi(U)=U^2` changes order two to four. The positive control is an exact unramified substitution `phi(U)=cU+O(U^2)`, `c != 0`, times a unit.

**Verified local lemma.** If `ord_T f=r` and `ord_U phi=m` with nonzero leading coefficients, then `ord_U(f(phi(U)))=r*m`. This elementary local-algebra statement closes only the calibration.

**Target result.** No consulted primary source in the bounded BSD packet supplied an exact unramified complex-`s` germ comparison with a root-faithful arithmetic output. This is explicitly not a literature-wide nonexistence result.

**Episode.** Hodge depth failure and BSD derivative-coordinate ontology were retrieved; a local order-transport criterion was proved and tested before another source expansion.

**Diagnosis.** `REPRESENTATION + BRIDGE_GLUING`: the missing object is the exact parameter/germ comparison, not another occurrence of the phrase “second derivative”.

**Proposal lesson.** `PL-XM009-JET-FAITHFULNESS-BEFORE-ORDER-TRANSFER` at proposal/shadow authority only.

**Observed failure.** `F-XM009-BSD-CROSS-COORDINATE-JET-COMPARISON-MISSING`, `OBSERVED_ONLY / SEARCH_CONTROL_SCOPE`; not a BSD obstruction theorem.

**Positive motif.** `M-XM009-TRUNCATION-DEPTH-FIRST`.

**Prior experience that changed routing.** Selected: `F-H4D1B-FIRST-ORDER-NOT-FORMAL`, `D-BSD-A1A1-HIGHER-GZ-ONTOLOGY-MISMATCH-R4`, `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`, and XM008's interface-preservation pattern. Rejected/deferred: XM006 pointwise-to-diagonal uniformity (different quantifier defect), XM007 state-projection congruence (already exercised/no projection collision needed), and the XM002 retrieval-miss record (coverage warning but not the algebraic discriminator). The pre-memory preference was a broader search for another higher/second-derivative formula; memory changed the action to the jet-faithfulness discriminator first.

**Outcome/residual.** `PARTIAL_SUCCESS_ROUTE_REFINEMENT / JET_COMPARISON_OBLIGATION_EXPOSED`. The local algebra succeeds; the BSD local bridge, source completeness, and downstream global arithmetic gluing remain open.

**Saturation and novelty.** `KNOWLEDGE=OPEN`, `OPERATOR=FLAT`, `EXPERIENCE_PATTERN=REOPENED`, `OBSTRUCTION=FLAT`, `RELATION=REOPENED`, `PATH=REOPENED`, `META_METHOD=FLAT`. Conservative retained vector `K=0, O=0, E=1, B=0, R=1, P=1, M=0`. If classified at all, this is `representation/transfer`, structural rank zero, with no mathematical novelty claim.

## Cross-lane quantified snapshot

For the six selected consequential lane episodes above, explicit project-local retained vectors are available for all six. Their sum is a **lower-bound observer aggregate**, not canonical RAKLV3State growth and not literature novelty:

```text
KNOWLEDGE          6
OPERATOR           0
EXPERIENCE_PATTERN 2
OBSTRUCTION        3
RELATION           5
PATH               3
META_METHOD        0
```

Including the current XM009 proposal/shadow episode yields:

```text
KNOWLEDGE          6
OPERATOR           0
EXPERIENCE_PATTERN 3
OBSTRUCTION        3
RELATION           6
PATH               4
META_METHOD        0
```

Coverage and rates:

- selected consequential science-lane episodes inspected: `6`;
- current cross-observer episode: `1`;
- explicit seven-axis vector coverage for the six selected lane episodes: `6/6`;
- canonical materialized `RAKLV3State.state_fingerprint()` coverage observed across the six application lanes: `0/6`; application-defined hashes are not relabelled canonical state fingerprints;
- exact repeated-failure count/rate: `CANNOT_MEASURE` — no exhaustive canonical episode denominator with a uniform cross-lane failure ontology was frozen;
- exact retrieval-miss rate: `CANNOT_MEASURE` — most lanes lack an exhaustive relevance-labelled universe; the RH bound review records zero later-found misses, while older XM003 preserved a historical cross-problem miss;
- global successful/failed reuse counts/rates: `CANNOT_MEASURE` — proposal, unmerged, canonical, tool and failure identities have mixed authority and no single governed denominator;
- exact local-success/global-gluing-failure lower bound among the six science episodes: at least `1` (Navier–Stokes R2); including the XM009 abstract local-algebra calibration gives at least `2`. These are lower bounds, not rates;
- same-context role separation receives zero independent-review credit;
- causal/action-routing evidence is positive and explicit for Navier–Stokes R2 and XM009, but a cross-lane action-change rate is `CANNOT_MEASURE` because the other lanes do not all expose a comparable pre-memory counterfactual.

## Recurring process evidence

Four recurring process patterns are now supported as method observations, not universal mathematical laws.

1. **Cheap hostile falsifiers often dominate broad theorem invention early.** PNP uses a zero-cost shattering world; RH uses one fatal exception; YM uses a relative-form null-vector model; Hodge uses a nonreduced/support distinction; XM009 uses small-perturbation and ramified-coordinate controls.
2. **Representation faithfulness and global gluing are distinct failure families.** A local coordinate, tangent, norm, convergence or scale statement may be valid while the root-facing consumer interface remains open.
3. **Specific negative history can materially change routing.** This is directly recorded in NS R2 and XM009, and structurally in RH's saturation-driven switch to a single-exception audit.
4. **Operational assurance failures recur but current framework main now has explicit ownership surfaces.** `method_telemetry`, `episode_admission`, and lifecycle-aware `artifact_contract_coverage` address the previously observed telemetry/admission/hash/chronology failures. Continued application repairs under an older execution pin are not, by themselves, evidence of a current-main framework reasoning defect.

## RAKL_METHOD_CASE_STUDY

**Research method used.** Exact root/atom decomposition; frozen fibre; primary-source claim extraction; structural analogues with assumptions and DifferenceWitnesses; dual success/failure memory; same-context expert role separation; counterexample-first discriminator selection; explicit local-versus-gluing split; seven-axis saturation; proposal/shadow episode, diagnosis, lesson/failure and public trace separation; conservative metrology.

**What worked.** Hodge's first-order/formal-depth failure and BSD's derivative-coordinate ontology jointly selected a cheaper algebraic discriminator than another broad source search. The order-under-substitution lemma gives an exact positive/negative calibration. Cross-lane negative history repeatedly prevented redundant route retries.

**What failed or remains open.** The bounded BSD packet still lacks the required exact complex-`s` germ comparison; source coverage completeness is unmeasured; the application execution pin cannot be treated as execution of newer current-main v3 surfaces; all Millennium roots remain open.

**Failure taxonomy.** XM009 local algebra: success. Target: representation/gluing residual. Cross-source completeness: retrieval/source `CANNOT_MEASURE`. Root verification: incomplete. Framework: no new defect established in this round.

**v3 features that helped.** Separate immutable episode evidence from linked method telemetry; explicit routing influences/rejections; DifferenceWitness-style transfer scoping; seven saturation axes; shadow/canonical episode admission distinction; lifecycle-aware artifact-contract assurance; explicit `CANNOT_MEASURE` rather than inferred precision.

**Feature still missing/unavailable at the application-observer level.** No canonical materialized cross-lane `RAKLV3State` and no exhaustive relevance-labelled cross-Millennium retrieval universe are available to this observer, so canonical growth, recall, repeated-failure, and reuse rates remain unidentifiable.

**Framework-improvement hypothesis.** `NO_NEW_CHALLENGER_THIS_ROUND`. The cheapest next discriminator is operational adoption: after a governed application pin sync, run a fresh artifact-producing research cycle through current `method_telemetry`, `episode_admission`, and `artifact_contract_coverage` with planted stale-hash/lifecycle/chronology/unowned-artifact controls. Only a reproducible failure under current semantics should reopen a narrow framework defect. Expected benefit: distinguish implementation adoption lag from a live framework gap. Possible regression if implemented too broadly: false positives or freezing legitimate evolving state. No RAKL issue is opened from this cycle.

No theorem, root certificate, reusable ResearchTool, independent review, or framework authority is promoted.
