# RAKL_METHOD_CASE_STUDY — XM010 / R6 cross-Millennium observer

**Authority:** measurement + proposal/shadow research control only. No framework, theorem, root, independent-review, or promotion authority.  
**Current v3 semantic basis at consequential freeze:** RAKL method `3.0.0`, `SzeChunYiu/RAKL@eeca4ea13ad7e2b2bc2fd4d7420ad05a81f654ca`. RAKL advanced during the cycle; compare-to-close is recorded in `RAKL_CYCLE_METRICS`.  
**Application base:** `SzeChunYiu/RAKL_math@47f56df0492339097a651d40b6c7289c4e2d4034`.

## Observer episode

**Atom/signature.** `XM010-NS-B1a3b1b-AMPLITUDE-MORPHOLOGY-CONGRUENCE`: transfer only the PNP projection-congruence operation into the Navier–Stokes `NS-B1a3b1b` representation cut. The decomposition was: (1) source representation operation; (2) target weak-`L^{3/2}` amplitude coordinate; (3) source-bound one-center `lambda^{-1/2}` morphology outcome; (4) exact hostile DifferenceWitness; (5) separate NSE-specific, direction, far-field, state-space and Type-II residuals.

**Actual fibre/search universe.** Current RAKL v3 version/TaskEpisode/storage-admission/method-spec surfaces; current RAKL_math main/root/control surfaces; PNP PR #78; PNP PR #128; RH PR #118; NS PR #131 and issue #145; YM PR #144; Hodge PR #143; BSD merged PR #142/current successor metrics; XM007/XM008/XM009 cross-problem history; current Grujić arXiv:2607.08866v2 and Albritton–Barker arXiv:1811.00502. Exhaustive cross-problem memory coverage is not available, so missed-memory rate is `CANNOT_MEASURE`.

**Retrieved/selected.** Selected routing evidence: `PNP-O9d12a2a1a1a/PR78`, `NS-B1a3b1a/PR131`, `NS-B1a3b1b/#145`, `XM007/PR95`. Rejected or scope-limited: `XM008/PR125` (source substitution, wrong cut), `YM-E4b2a` (typing analogue but weaker than geometric falsifier), `H4d1c1/PR143` (completion/descent gluing, wrong cut), `XM009/PR139` (closed PR with no durable changed files; noncanonical provenance). Prior NS representation failures changed routing away from another generic norm-only producer search. The PNP congruence episode changed the *form* of the discriminator. Because no pre-memory action preference was prospectively frozen, causal RAKL action-change attribution remains `CANNOT_MEASURE`.

**Decision policy.** Counterexample first: before attempting an equation-specific finite-`I` theorem, test whether the coarse amplitude representation is even congruent for the registered morphology outcome. This rotates away from a saturated family of scalar/norm-only producer retries.

**Falsifier/verification.** A smooth divergence-free one-core field and a separated two-core field are constructed from the same compact template. Multiplying each of two disjoint copies by `2^{-2/3}` gives exact equality of the `L^{3/2,infinity}` vorticity-magnitude norm. At a common scale-matched threshold, the one-core superlevel fits in one `C lambda^{-1/2}` ball while the two-core superlevel contains two points distance two apart and fails the same containment. Analytic Lorentz scaling and Euclidean radius estimates are the proof; finite distribution calibration is regression only. Seven same-context expert roles reviewed representation, PDE scope, Lorentz scaling, morphology geometry, adversarial overreach, provenance, and v3 assurance. Independent-review credit: zero.

**Outcome/residual.** `PARTIAL_SUCCESS_ROUTE_PRUNING / REPRESENTATION_CONGRUENCE_FAIL`. Weak-`L^{3/2}` amplitude alone is not a faithful carrier of the one-center morphology on the hostile smooth divergence-free class. This does **not** falsify `NSE + finite I -> morphology`. Residuals are explicitly separated: local NSE-specific morphology/recentering, local direction/log-BMO, local-to-global far-field/Lorentz, pre-singularity-to-ancient state-space gluing, and Type II.

**Episode -> diagnosis -> obstruction/lesson.** Episode `EP-XM010-PNP-NS-MORPHOLOGY-CONGRUENCE-20260811-R6` records what happened. Diagnosis `D-XM010-AMPLITUDE-MORPHOLOGY-NONCONGRUENCE-R6` identifies loss of core multiplicity/separation in the projection. Failure `F-XM010-NS-WEAK-L3_2-AMPLITUDE-MORPHOLOGY-NONCONGRUENCE` is the scoped failed action, not a blacklist. Obstruction `O-XM010-NS-MORPHOLOGY-COORDINATE-OR-EQUATION-RIGIDITY` states what must be supplied next. Candidate lesson `L-XM010-TEST-PROJECTION-CONGRUENCE-BEFORE-CONSUMER-MAP-v1` guides future search only. Proposal motif `MOTIF-XM-PROJECTION-CONGRUENCE-DIFFERENCEWITNESS` records the positive reusable research pattern.

**Failure category / novelty / saturation.** Failure category: `representation`, with gluing failures recorded separately rather than merged into the local diagnosis. Defensible shadow solved-route novelty class: `transfer`; proof novelty none. Protected retained semantic novelty is zero on all seven axes because no retention/promotion gate ran. Shadow observations reopen `EXPERIENCE_PATTERN`, `OBSTRUCTION`, `RELATION`, and `PATH`; the family `weak_L3_2_amplitude_alone -> one_center_morphology` is flattened.

## Comparable lane episodes

### Riemann Hypothesis — RH-ANA-003

Atom: all-index Li positivity versus aggregate zero-density/zero-free exceptional-set control. Decomposition: aggregate rarity, boundary exclusion, one-exception Li sensitivity, exact cancellation/gluing. Fibre includes Bombieri–Lagarias/Voros, Guth–Maynard, Bellotti–Trudgian–Yang, mollifier work, and prior RH failure episodes. Retrieved tools `T-RH-LI-PREFIX-QUARTET-CALIBRATION` and `T-XM-ROOT-BRIDGE-STABILITY-AUDIT`; three failure experiences selected; one cross-problem failure was scope-limited rather than transferred mathematically. Decision: planted single-exception falsifier before a new inequality. Outcome: route-local refutation/obstruction; aggregate rarity does not supply universal no-exception/all-index sign. Failure class: representation + theoretical gluing. Residual: exact exceptional-zero-sensitive cancellation/extinction/neutralization uniform in `n`. Retained vector reported by that lane: `K=3,O=0,E=1,B=1,R=2,P=1,M=0`. Positive motif: universal goals should be tested against one admissible exception before aggregate-to-universal promotion. Authoritative record: `riemann_hypothesis/09_trace/RAKL_METHOD_CASE_STUDY_RH_ANA_003_20260811.json` on PR #118.

### P versus NP — O9d12a2a1a1d

Atom: quotient-respecting absolute base-trace richness versus counted intersection cost. Decomposition: exact graph generator family, zero-cost target, free-union trace family, cardinality/entropy/VC controls. Fibre includes current PNP contracts, parent closure episodes/failures, PR #113 shadow warning, and Cavalar–Oliveira ECCC TR25-033. Retrieved one tool, four failures, two episodes; seven IDs selected and four alternatives rejected. Decision: test `t=0` before explicit hard-graph scoring. Outcome: absolute base richness is already maximal at zero counted intersection cost, so the route is pruned to target-relative/incremental coordinates. Failure class: representation/accounting. Shadow local novelty class: compositional. Protected retained vector: all seven axes zero. Positive motif: zero-cost substrate calibration before assigning a charged-complexity score. Authoritative record: `p_vs_np/10_case_study/RAKL_CYCLE_METRICS_O9d12a2a1a1d_20260811.json` on PR #128.

### Navier–Stokes — NS-B1a3b1a

Atom: bare finite-`I` numerical producer signature -> uniform vorticity amplitude/direction consumer signature. Decomposition separates local numerical embedding, equation-specific upgrade, far-field/global Lorentz gluing, and pre-singularity/ancient state-space. Fibre includes Albritton–Barker, Grujić v2, prior NS derivative/geometry/far-field failures, two episodes and two cross-problem/provenance tools. Four analogues retrieved (two selected/two rejected); seven failures retrieved (five selected/two deferred). Decision changed from direct interpolation to a smooth divergence-free time-frequency hostile falsifier. Outcome: bare norm-only embedding refuted; NSE-specific upgrade remains open. Failure class: local representation; global gluing remains separate. Novelty class: representation. Retained vector: `K=0,O=0,E=1,B=1,R=1,P=1,M=0`. Positive motif: duty-cycle/time-frequency probe for spacetime-to-time-slice representation mismatch. Authoritative record: `navier_stokes/10_case_study/NS-B1a3b1a_C001_R1_RAKL_CYCLE_METRICS_20260811.json` on PR #131.

### Birch–Swinnerton-Dyer — BSD-A1a1 R4

Atom: exact target cell for a complex analytic rank-two -> root-faithful arithmetic carrier. Decomposition normalizes “higher Gross–Zagier” into complex derivative order, weight/codimension, and p-adic deformation order, then keeps number field/weight two/complex `s`-derivative order two fixed. Fibre includes one prior episode, two failures, one root-bridge tool and bounded current primary sources; one noncanonical memory record rejected. Decision: target-cell source falsifier rather than repeating scalar theta-order or reversing arithmetic assumptions. Outcome: no theorem located in the bounded exact cell; no nonexistence claim. Failure/source class: ontology + source coverage; same-theory arithmetic gluing remains separately open. Route-diagnostic novelty class: ontology. Retained vector: `K=1,O=0,E=0,B=0,R=1,P=0,M=0`. Positive motif: coordinate-disambiguation before literature transfer. Authoritative successor metrics are merged on main at `birch_swinnerton_dyer/07_memory/BSD_A1a1_RAKL_CYCLE_METRICS_CURRENT_V3_SUCCESSOR_20260811_R4.json`.

### Rational Hodge — H4d1c1

Atom: formal effectivity split after a compatible all-Artin-order exact witness tower. Decomposition separates local tower construction, formal completion effectivity, exact Hilbert/Chow class identity, and completion-to-branch descent. Fibre includes CDK, exact Stacks effectivity interfaces, prior first-order Hodge failures, one episode and proposal shadow branch evidence. Selected four records and rejected three tangent/analogue records. Decision changed from another tangent-surjectivity mechanism to an exact stored-theorem transfer plus residual split. Outcome: conditional local effectivity over the completion is supplied by Grothendieck existence; local-to-global branch descent remains open. Failure category after the local success: representation + descent/gluing residual, explicitly separated. Novelty class: transfer; no proof novelty. Retained vector: `K=0,O=0,E=1,B=1,R=1,P=1,M=0`. Positive motif: split formal effectivity from neighborhood descent instead of carrying one undifferentiated “algebraization” residual. Authoritative record: `hodge/deformation/10_case_study/H4d1c1_C001_RAKL_CYCLE_METRICS_20260811.json` on PR #143.

### Yang–Mills — YM-E4c

Atom: type-check the written arbitrary-`O(4)` restoration bridge and its lattice-sum normalization. Decomposition separates local arithmetic scaling, finite-lattice representation of a generic rotation, and same-subsequence OS1 gluing. Fibre includes the current Faizal–Shabir primary PDF surfaces, issue #141, prior repeated source-acquisition episodes/failures, and six same-context roles. Eleven memory records retrieved, four selected, seven rejected. Decision rotated away from another metadata-only Bałaban acquisition cycle and toward a source-local repair audit. Outcome: the written normalization remains mismatched and the generic `O(4)` action is not type-bound on the stated hypercubic link configuration in the audited proof segment; OS1 gluing stays open. Failure class: source-proof + representation/gluing. No solved mathematical subproblem novelty class. Retained vector: `K=1,O=0,E=0,B=1,R=1,P=1,M=0`. Positive motif: domain/type-check an operator before estimating its defect. Authoritative record: `yang_mills/10_case_study/YM-E4c_RAKL_CYCLE_METRICS_20260811T1837Z.json` on PR #144.

## Cross-lane empirical aggregates

The six latest records above are comparable enough to report a **gross lower-bound observational aggregate**, not canonical lattice growth. Latest comparable episode count is `6` (one newly recorded episode in each selected lane receipt). Reported retained semantic novelty totals are `K=5,O=0,E=3,B=4,R=6,P=4,M=0`. This sum is descriptive only: lane retention semantics and protected-state materialization are heterogeneous, and no independent retained-novelty audit was run.

Reported successful-reuse IDs sum to `18` before cross-lane identity deduplication (`PNP 2 + RH 2 + NS 7 + YM 3 + Hodge 3 + BSD 1`). A deduplicated successful-reuse rate is `CANNOT_MEASURE` because a common governed denominator/universe is absent. Rejected/failed-reuse rate is also `CANNOT_MEASURE` because lanes type “rejected analogue”, “deferred failure”, “rejected action”, and “scope-limited memory” differently. Retrieval-miss rate is `CANNOT_MEASURE`: no compared lane has a governed coverage-complete cross-Millennium relevance universe. Repeated-failure rate is `CANNOT_MEASURE` for the same denominator reason.

A conservative local-success/global-gluing-failure lower bound is `2`: Hodge `H4d1c1` closes completion effectivity under exact hypotheses while branch descent stays open, and NS `NS-B1a3b1a` closes the bare representation route while global tail/far-field gluing stays open. YM also exposes a local source defect plus an OS1 gluing residual, but because the local result is a failure rather than a local success it is not added to this lower-bound count.

Every compared record contains at least one `CANNOT_MEASURE` field, most commonly token/context resources, exhaustive retrieval coverage, canonical protected-state fingerprints, or causal action counterfactuals. Therefore Paper-5 growth/attribution curves must retain `CANNOT_MEASURE` coverage rather than silently impute zero.

## Cross-lane diagnosis

The strongest recurring empirical pattern is **representation/interface loss before deep theorem failure**. PNP loses charged-cost meaning in an absolute free substrate; RH loses universal quantifier/phase information in aggregate exception counts; NS loses time-slice geometry in coarse amplitude/norm summaries; YM lacks a typed generic-rotation action at finite cutoff; Hodge must distinguish formal completion effectivity from branch descent. These are not one mathematical obstruction and must not be merged into a universal blacklist. They do, however, repeatedly reward a common process: freeze the downstream outcome, type the producer representation, then run the cheapest DifferenceWitness/interface falsifier before inventing a stronger theorem.

The second positive motif is **local-success/global-gluing separation**. Hodge and NS show that a genuinely useful local result can coexist with an open global interface. Treating either as “almost solved” would corrupt both routing and metrology.

## Framework-improvement hypothesis

**Status:** evidence-backed measurement hypothesis, but **no new RAKL issue opened** because current `RAKL_VERSION.json` already lists `cross_problem_memory_coverage_receipt` as a known open upgrade gap and the present evidence does not show a separate authority defect.

**Target module:** RAKL metrology/cross-problem memory coverage, adjacent to the `memory`/`benchmarking` process surfaces and Paper-5 measurement contract.

**Hypothesis:** add a governed `MethodTelemetryComparisonPacket` that references existing content-bound per-lane receipts and standardizes only (a) episode identity, (b) selected/rejected/scope-limited memory roles, (c) denominator provenance, (d) local-success versus global-gluing flags, and (e) `CANNOT_MEASURE` coverage. Do not force semantic novelty or failure taxonomies to a single scalar.

**Expected benefit:** deduplicated longitudinal reuse/miss/repeated-failure curves can be computed without reconstructing counts from prose; Paper-5 attribution can distinguish observed gross sums from canonical protected-state growth.

**Possible regression:** a rigid comparison schema can create false comparability, incentivize metric gaming, or coerce heterogeneous scientific failures into one taxonomy.

**Cheapest prospective discriminator:** on the next two observer cycles, emit a packet that only points to already-hashed lane receipts and asks whether episode count, deduplicated successful reuse, local-success/global-gluing lower bound, and `CANNOT_MEASURE` coverage can be computed without inventing a denominator. Reject the schema if it requires reinterpretation of a lane’s scientific semantics.

## Next action

For Navier–Stokes, do not retry weak-`L^{3/2}` amplitude as a faithful morphology carrier. Search for a source-bound/equation-specific finite-`I` -> one-center/recentering theorem or a richer geometric producer coordinate and force it to survive `MOTIF-XM-PROJECTION-CONGRUENCE-DIFFERENCEWITNESS`. Across problems, continue using this motif only with target-specific assumptions and a DifferenceWitness; experience remains search guidance, never theorem authority.