# Cross-Millennium verified-discovery memory

This workspace stores **cross-problem research-process transfers**, not a seventh Millennium root and not a claim that the six open problems share one mathematics.

Every retained transfer must name the source atom/result, target atom, common abstraction, enabling assumptions, explicit disanalogies, DifferenceWitness, cheapest target falsifier, and actual authority. Literal mathematical tools may be reused only when their registered preconditions hold.

## XM001 — root-bridge stability audit

The first strict atom learns from merged P-vs-NP C024 without exporting its LP mathematics. C024 showed a valid local fractional certificate whose route-level proxy loses integral cross-fusion correlation. XM001 asks whether the **calibration policy** transfers: before strengthening a surrogate, test the exact bridge that must preserve it to the root.

The first target calibration is Yang–Mills `YM-S1` (issue #85). The abstract sequence `r(a,L)=exp(-a^2)` proves that a positive dimensionless finite-lattice gap for every `a>0`, even uniform in `L`, can have physical normalization `-a^{-1}log r=a -> 0`. This retires only the weak inference from pointwise finite-cutoff positivity to a positive physical continuum gap. It says nothing about whether actual Yang–Mills dynamics realize the sequence.

Recorded failure: `F-XM001-POINTWISE-GAP-COLLAPSE` at `SUPPORTED` target-transfer diagnosis authority.  
Active Yang–Mills residual: `YM-S1-SCALED-UNIFORM-BRIDGE` — state/source the weakest target-specific scaled, volume/limit-uniform theorem that survives the falsifier.

BSD issues #90/#91 appeared after the XM001 pre-candidate context was frozen; they were intentionally not backfilled into that chronology.

## XM002 — second-domain reuse audit

The later merged BSD-A1a1 packet supplied the fresh, structurally different reuse that XM001 required before expertise compilation. The target bridge is not a cutoff limit: it separates exact order in complex `s` from exact order in the anticyclotomic `T` direction. Its memory review imported only the XM001 diagnostic operation, supplied a new DifferenceWitness, and used the exact source implication boundary as the target falsifier rather than importing Yang–Mills mathematics.

That reuse split the previously compressed Kato-nonvanishing bridge into the distinct upstream residuals `BSD-A1a1-THETA-ORDER-COMPARISON` and `BSD-A1a2-LOCALIZATION-POSITIVE-RANK-BRIDGE` before a new theorem candidate was invented. This satisfies XM001's preregistered practical-success criterion: the audit exposed an earlier missing preservation theorem in a mathematically disanalogous target.

The retained method tool `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` is therefore promoted narrowly from `VERIFIED_LOCAL` to `CONDITIONALLY_REUSABLE`. Every future reuse still requires a fresh DifferenceWitness and target-specific falsifier; the tool is not proof-backed and does not assert a universal common obstruction.

Candidate expertise chunk: `E-XM-ROOT-CRITICAL-COORDINATE-AUDIT-001`. Its deep cue is a valid local/surrogate result followed by an unproved map to a distinct root-critical coordinate (limit, normalization, realization, completeness, integrality, assembly, or representation-faithfulness). A third-domain surface-shifted retrieval/transfer test remains required before any stronger maturity claim.

Framework drift discovered during XM002 was isolated to `SzeChunYiu/RAKL#116`; no problem-specific evidence was moved into the framework repository.

No Millennium root status changes here. Same-context expert review is not independent review, and proposal/breakthrough mechanisms mint no theorem authority.
