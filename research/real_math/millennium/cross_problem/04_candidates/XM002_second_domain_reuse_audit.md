# XM002 — second-domain reuse audit for root-bridge stability

**Atom:** `XM-ROOT-BRIDGE-REUSE-002`  
**Date:** 2026-08-11  
**Framework authority inspected:** `SzeChunYiu/RAKL@15f1c3affe5bf85ba41ff0ab65b25ba19e0d28a3`  
**Application base:** `SzeChunYiu/RAKL_math@d8ac4102285c4ed1ba0fbd5d8818dc4c4731a8cc`  
**Authority:** `CROSS_DOMAIN_REUSE_AUDIT / CONDITIONALLY_REUSABLE_TOOL_PROMOTION / NO_MATHEMATICAL_THEOREM / NO_ROOT_AUTHORITY`

## Object and question

XM001 created `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` after an exact P-vs-NP-to-Yang–Mills calibration. It explicitly withheld expertise compilation until at least one additional **structurally different** lane reused the audit with a fresh DifferenceWitness and showed that the audit pruned a route or exposed a missing theorem earlier than ordinary surrogate optimization.

The merged BSD-A1a1 packet now provides that test. This cycle asks only:

> Does the BSD-A1a1 use satisfy the registered tool preconditions strongly enough to count as a successful second-domain reuse, and if so what is the narrowest justified reusable authority?

This is a research-method audit, not a theorem about any Millennium problem and not a claim that Yang–Mills and BSD share mathematics.

## Evidence frozen before this audit

### Original source/calibration

`XM001` abstracted a lesson from P-vs-NP C024: a correct local/surrogate certificate can erase a coordinate needed by the root lift. Its first target was Yang–Mills `YM-S1`. The exact adversarial family

`r(a,L)=exp(-a^2)`

keeps a positive dimensionless finite-cutoff gap while the inverse-spacing-normalized physical gap equals `a -> 0`. This falsifies only the weak inference from pointwise finite-cutoff positivity to positive physical continuum gap. It does not model actual Yang–Mills dynamics.

Primary anchors:

- Cavalar–Oliveira, *Boolean Circuit Complexity and Two-Dimensional Cover Problems*, ECCC TR25-033: https://eccc.weizmann.ac.il/report/2025/033/
- Shen–Zhu–Zhu, *A stochastic analysis approach to lattice Yang--Mills at strong coupling*, arXiv:2204.12737: https://arxiv.org/abs/2204.12737

### BSD second-domain reuse

The merged BSD-A1a1 memory review selected `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` with a fresh DifferenceWitness. The target bridge is not a cutoff limit. It is a **change of analytic coordinate**:

`ord_{s=1} L(E,s)=2`

versus

`ord_T Theta_{f/K}(T)=2`.

The source-level implication audit records that Castella–Hsieh's anticyclotomic interpolation identifies values along finite-order anticyclotomic characters and, at the trivial character, central vanishing gives only positive `T`-order in the cited argument; it does not by itself identify exact `T`-order two from exact complex `s`-order two. The same source later consumes `ord_T Theta=2` in a generalized-Kato nonvanishing criterion under additional arithmetic hypotheses. Thus the compressed complex-order -> theta-order arrow was split into a distinct upstream residual before a new theorem candidate was invented.

Primary anchor:

- Castella–Hsieh, *On the non-vanishing of generalized Kato classes for elliptic curves of rank 2*, arXiv:1809.09066 / Forum of Mathematics, Sigma 10 (2022), e12: https://arxiv.org/abs/1809.09066

## DifferenceWitness: XM001 Yang–Mills -> BSD-A1a1

| Coordinate | XM001 source target | BSD-A1a1 target |
|---|---|---|
| local/surrogate coordinate | positive dimensionless finite-lattice gap | anticyclotomic theta interpolation / positive `T`-order information |
| root-critical coordinate | positive physical continuum gap after `1/a` normalization and limits | exact order two in the anticyclotomic `T` variable, upstream of generalized-Kato nonvanishing |
| missing bridge type | scale/limit/uniformity preservation | directional-order / representation-faithfulness comparison |
| target falsifier | exact elementary sequence with gap collapse | source theorem/argument itself supplies only weaker `T`-order information; generic bivariate directional-order example is inference-form calibration only |
| literal mathematics transferred? | none | none |
| shared abstraction | a valid local coordinate is insufficient unless the map to the next root-critical coordinate preserves the required property | same |

The structural difference is load-bearing: no transfer-matrix, lattice-spacing, RG, LP, set-cover, or physical mass-gap theorem enters BSD. The reusable object is only the **audit operation**.

## Tool-precondition audit

### 1. Precise surrogate object and certificate

PASS. BSD-A1a1 freezes the theta element/interpolation direction and the exact currently sourced consequence at the trivial character.

### 2. Precise target/root-critical implication and bridge map

PASS. The desired implication is explicitly written as exact complex `s`-order two -> exact anticyclotomic `T`-order two, with the variable/category change named rather than conflated.

### 3. Fresh target-specific DifferenceWitness

PASS. The merged memory review states the source/target distinction, shared abstraction, material difference and target-specific validation operation.

### 4. Known failure warning reviewed

PASS. `F-XM001-POINTWISE-GAP-COLLAPSE` is retained only as a warning about bridge sufficiency. Its Yang–Mills sequence is not reused as arithmetic evidence.

### 5. Target-specific falsifier executed

PASS at research-control authority. Instead of fabricating an arithmetic counterexample, BSD-A1a1 uses the exact source implication boundary: interpolation/central vanishing yields weaker information than the exact order-two bridge being sought. A generic two-variable function is explicitly quarantined as an inference-form calibration, not an arithmetic model.

### 6. Research-program effect

PASS. The audit split the earlier compressed `BSD-A1a-KATO-NONVANISHING-BRIDGE` into two non-equivalent residuals:

- `BSD-A1a1-THETA-ORDER-COMPARISON`;
- `BSD-A1a2-LOCALIZATION-POSITIVE-RANK-BRIDGE`.

This is exactly the registered useful effect: stop optimizing downstream machinery until the earliest unproved preservation arrow is explicit.

## Expert-cell synthesis

Six role-separated same-context passes reviewed the reuse. Full dispositions are frozen in `08_reviews/XM002_EXPERT_REUSE_REVIEW_20260811.md`.

1. **Complexity / source-calibration lead** — confirms that the P-vs-NP source lesson is about lost root-critical structure, not exported LP mathematics.
2. **Mathematical-physics / Yang–Mills lead** — confirms the first calibration is specifically scale-normalization failure and therefore structurally different from BSD.
3. **Arithmetic-geometry / BSD lead** — confirms the `s` and `T` directions are distinct objects and the cited source does not license their exact-order identification by interpolation alone.
4. **Adversarial cross-transfer lead** — rejects the stronger claim that every open-problem bridge is an instance of one universal obstruction; Hodge, RH and Navier–Stokes remain uncalibrated future targets unless they pass fresh witnesses.
5. **Formal-assurance lead** — maps the BSD reuse to the current `ResearchTool` preconditions and concludes `APPLICABLE_WITH_VALIDATION`; the validation was actually executed by the source-level implication audit.
6. **Novelty / research-process-value lead** — supports promotion only to `CONDITIONALLY_REUSABLE`; no theorem, novelty, independent-review or universal-method authority is created.

## Result

`BSD-A1a1-XM001-ROOT-BRIDGE-REUSE` is accepted as a **successful scoped second-domain reuse**.

The narrow promotion is:

`T-XM-ROOT-BRIDGE-STABILITY-AUDIT: VERIFIED_LOCAL -> CONDITIONALLY_REUSABLE`.

The operation now has evidence in two mathematically disanalogous target mechanisms:

- Yang–Mills: cutoff/units/limit preservation;
- BSD: representation/directional-order preservation.

Every future reuse still requires a fresh DifferenceWitness and target falsifier. The tool is not `PROOF_BACKED`, not universal, and not a theorem schema.

## Expertise chunk candidate

`E-XM-ROOT-CRITICAL-COORDINATE-AUDIT-001`

**Deep retrieval cues:**

- a route has a valid local, finite, source-restricted or representation-local result;
- the next root-relevant conclusion uses a different coordinate, category, normalization, limit or completeness notion;
- the bridge is being treated narratively rather than as its own theorem/obligation.

**Script:**

1. name the local/surrogate coordinate;
2. name the next root-critical coordinate;
3. write the bridge map/implication explicitly;
4. list the exact preservation property needed;
5. freeze a target-specific DifferenceWitness from prior failures/successes;
6. run the cheapest target-domain falsifier that preserves the local premises while attacking the root-critical coordinate;
7. if falsified, split the DAG at the earliest missing arrow and stop spending search budget downstream;
8. if not falsified, record only failure of that falsifier—not proof of the bridge.

**Contrastive near-misses / non-applicability:**

- two notationally different formulations that are already theorem-equivalent do not need this audit;
- a local theorem that directly proves the exact next DAG node without an unproved lift is not a bridge-instability case;
- a candidate failing inside its own stated local premises is ordinary local falsification, not this cross-coordinate failure family;
- surface similarity such as 'both use spectra' or 'both have limits' is insufficient without a precise preservation obligation.

**Maturity limit:** candidate expertise chunk only. A surface-shifted retrieval/transfer test on a third domain is still required before claiming mature retrieval reliability.

## Framework feedback discovered incidentally

Current RAKL `static/core/workflow.md` still omits `EXPERIENCE_MEMORY_REVIEW` from one always-loaded pre-candidate trace list and omits `memory_review=...` from one `plan_math_research` example, while `AGENTS.md`, the manifest, and `workflows/mathematical-research.md` require the dual-memory gate. This was isolated into `SzeChunYiu/RAKL#116`; no framework code or problem evidence is mixed into this application branch.

## Authority boundary

- No Millennium theorem or root status changes.
- No claim is made that Castella–Hsieh proves or disproves the desired exact order-comparison theorem; the audit identifies what the cited implication does and does not supply.
- The promoted tool remains a scoped research-control diagnostic.
- Same-context expert roles are not independent review.
- A third-domain transfer/retrieval test remains open before any stronger expertise maturity claim.
