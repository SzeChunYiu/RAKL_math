# XM003 — Hodge third-domain transfer and cross-problem retrieval audit

**Atom:** `XM-RETRIEVAL-COVERAGE-003`  
**Framework inspected:** `SzeChunYiu/RAKL@15f1c3affe5bf85ba41ff0ab65b25ba19e0d28a3`  
**Application branch base:** `SzeChunYiu/RAKL_math@0ad7991d8b4f05792d6d43b6e36128ef4b7eada9`  
**Authority:** `CROSS_PROBLEM_RETRIEVAL_AUDIT / PROCESS_FAILURE_SUPPORTED / THIRD_DOMAIN_TRANSFER_EVIDENCE / NO_MATHEMATICAL_THEOREM / ROOT_AUTHORITY_NONE`

## Question

XM002/PR #30 proposes promoting `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` after a second structurally different target reuse in BSD and compiles candidate expertise chunk `E-XM-ROOT-CRITICAL-COORDINATE-AUDIT-001`. It states that Hodge, RH, and Navier–Stokes remained uncalibrated future targets and leaves a third-domain surface-shifted retrieval/transfer test open before mature retrieval reliability.

This cycle asks two narrower questions:

1. did a third mathematically disanalogous target already perform a genuine target-specific reuse of the audit; and
2. if so, does that justify calling the expertise chunk mature, or does the research record expose a retrieval-process failure that blocks that stronger learning claim?

## Frozen chronology check

PR #30 is based on `d8ac4102285c4ed1ba0fbd5d8818dc4c4731a8cc`.

At that **exact base**, the file

`research/real_math/millennium/hodge_conjecture/07_memory/HM1a_RESEARCH_MEMORY_REVIEW_20260811.json`

already exists and records:

- `tool_query_status = MATCHES_FOUND`;
- `relevant_tool_ids = [T-XM-ROOT-BRIDGE-STABILITY-AUDIT]`;
- `selected_tool_ids = [T-XM-ROOT-BRIDGE-STABILITY-AUDIT]`;
- a Hodge-specific DifferenceWitness;
- a Hodge-specific cheapest regression: expand every source-bearing decomposition and reject any root-relevant piece whose only discharge is the same unsolved cycle-map surjectivity under a new label.

The associated HM1a calibration and failure/tool deltas are also part of the migrated application state. Therefore the statement in XM002 that Hodge remained uncalibrated is not a chronology issue and is not repaired by backfilling XM002. It is a retrieval/coverage miss in the synthesis itself.

## Third-domain structural mapping

### Source history

The reusable research-control operation began with a P-vs-NP lesson and was calibrated first in Yang–Mills:

- a locally valid surrogate coordinate can fail to preserve a root-critical coordinate through an extra bridge;
- in Yang–Mills the concrete bridge was finite-cutoff/dimensionless gap information to a physically normalized continuum mass gap.

BSD supplied a second disanalogous use:

- complex `s`-order and anticyclotomic `T`-order are different analytic coordinates;
- finite-character interpolation does not by itself identify exact order across those directions.

### Hodge target `HM1a`

**Target atom:** higher-codimension source-bearing decomposition/assembly for the rational Hodge conjecture.

**Local/surrogate object:** an exact filtration, graded decomposition, motivated/categorical enrichment, or correspondence decomposition that organizes target cohomology/Hodge pieces.

**Root-critical coordinate:** an actual algebraic-cycle source in the correct Chow category whose cycle class reconstructs the given rational Hodge class after the exact assembly map.

**Bridge map:** graded Chow cycle maps / algebraic correspondences plus the exact assembly/reconstruction theorem.

**Preservation property:** every root-relevant Hodge piece is discharged by a proved source construction or by a proved zero-Hodge target; no piece may remain whose only source hypothesis is the same unproved cycle-map surjectivity in localized or graded form.

### DifferenceWitness

| Coordinate | Yang–Mills calibration | BSD reuse | Hodge HM1a |
|---|---|---|---|
| locally valid object | finite-cutoff dimensionless gap | p-adic theta/interpolation data | exact decomposition / target-side structure |
| next root-critical coordinate | positive physical continuum gap | exact anticyclotomic `T`-order | actual Chow source + exact reconstruction |
| missing bridge type | normalization/limit/uniformity | directional-order/representation faithfulness | source-realization/assembly sufficiency |
| target falsifier | scaled gap-collapse family | source implication boundary | label every critical Hodge piece `KNOWN_SURJECTIVE`, `HODGE_TARGET_ZERO`, or `OPEN_ROOT_LIKE` |
| literal source mathematics transferred | none | none | none |

The disanalogies are load-bearing. No lattice theory, transfer matrix, p-adic `L`-function, or circuit/set-cover theorem is imported into Hodge theory. Only the audit operation transfers.

## Hodge-specific validation result

HM1a calibrated the source-bearing screen on both positive and hostile cases.

**Positive controls** include:

- divisors/Picard, where an actual algebraic source is available;
- Arapura's Leray-filtered Chow setting at its published scope, where graded Chow maps remain the source and some difficult obligations can disappear because the corresponding Hodge target is zero;
- special dominating-correspondence examples where a universal-sheaf-type algebraic correspondence supplies genuine source geometry.

**Negative/near-miss controls** include:

- abstract motivated/categorical enrichment that changes the target description without supplying the required Chow source;
- deformation propagation that assumes an initial algebraic witness rather than constructing one;
- an unpruned graded decomposition containing an `OPEN_ROOT_LIKE` piece whose only discharge is the original surjectivity problem in renamed form.

The calibration therefore does more than recognize vocabulary. It changes the research state by opening the `HM1b` source-span/Hodge-empty-complement constructor and recording `F-HM1a-OPEN-ROOT-LIKE-PIECE` plus local tool `T-HODGE-SOURCE-BEARING-DECOMPOSITION-SCREEN`.

Primary-source calibration is consistent with Arapura's published descriptions: the 2021 paper explicitly pulls the Leray filtration back to Chow groups and derives Hodge/Tate criteria for fibered varieties; the 2005 motivation paper describes dominating correspondences, including universal-sheaf constructions in special moduli examples. These sources support only their stated scopes and do not provide a general Hodge constructor.

## Expert cell

Six role-separated same-context passes were delegated.

1. **Research-memory architecture lead** — checked the exact PR #30 base and found the Hodge tool selection already present. Vote: `ACCEPT retrieval-miss diagnosis`.
2. **Hodge/arithmetic-geometry lead** — checked that HM1a's source-vs-target distinction is mathematically different from Yang–Mills and BSD and that Arapura is used at published scope. Vote: `ACCEPT third-domain transfer evidence`.
3. **Cross-domain method-transfer lead** — verified the common abstraction and explicit DifferenceWitness; rejected literal theorem transfer. Vote: `APPLICABLE_WITH_TARGET_VALIDATION`, with the validation already executed by HM1a.
4. **Adversarial chronology/retrieval lead** — strongest objection: Hodge evidence predates the XM002 prose in artifact timestamps, so it cannot be ignored as a later development. This strengthens the coverage-failure diagnosis. Vote: `BLOCK rewriting/backfilling XM002`.
5. **Formal-assurance/provenance lead** — distinguishes three claims: Hodge transfer occurred; XM002 omitted it; expertise-chunk retrieval reliability is mature. The first two are supported; the third is not. Vote: `BLOCK maturity promotion`.
6. **Metacognition/learning-policy lead** — classifies this as retrieval/coverage failure rather than theorem difficulty and recommends `RETRIEVAL_REHEARSAL` plus a coverage-bound framework child problem. Vote: `ACCEPT`.

These roles are same-context analytical passes, not independent reviewers.

## Result

### Accepted

`HM1a-XM001-ROOT-BRIDGE-REUSE` counts as a **third mathematically disanalogous target transfer at research-control authority**:

- Yang–Mills: limit/normalization preservation;
- BSD: analytic-coordinate/order preservation;
- Hodge: source-realization/assembly preservation.

This strengthens confidence that `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` captures a real reusable research-control pattern rather than one domain's vocabulary.

### Rejected

Do **not** yet claim mature retrieval reliability for `E-XM-ROOT-CRITICAL-COORDINATE-AUDIT-001`.

Reason: the very cross-problem synthesis that proposed the chunk failed to retrieve an already-present third-domain reuse from its own exact base. The Hodge episode validates transfer, but the system-level ability to retrieve all relevant instances remains demonstrably unreliable. A mature retrieval claim would therefore exceed the evidence.

### New failure family

Record `F-XM002-CROSS-PROBLEM-RETRIEVAL-MISS`: a cross-problem completeness/counting claim can omit relevant same-base evidence when its search universe and per-lane coverage are not bound.

### Framework child problem

RAKL issue #119 was opened separately: bind a cross-problem memory/coverage receipt before completeness claims such as `no other domain reused this tool` or `lane X remains uncalibrated`. This is framework-process work only.

## Next prospective retrieval rehearsal

The next maturity discriminator should be genuinely prospective and surface shifted:

1. choose a **newly opened** atom after its context is frozen but before its memory review;
2. expose only deep structural cues, not the tool ID or source-domain narrative;
3. query the bound global/shared tool universe;
4. success requires surfacing `T-XM-ROOT-BRIDGE-STABILITY-AUDIT` (or explicitly ruling it out) with a target-specific applicability witness before candidate generation;
5. execute the cheapest target-specific falsifier;
6. compare retrieval against a near-miss atom where the tool should be rejected;
7. bind the exact coverage universe so a missed lane cannot masquerade as `NO_RELEVANT_MATCH`.

Until that probe succeeds, retain the expertise chunk as **candidate/immature retrieval aid**, despite the now stronger three-domain transfer evidence.

## Authority boundary

- no Millennium root status changes;
- no Hodge, BSD, Yang–Mills, or P-vs-NP theorem is created;
- HM1a's published-source calibrations remain at their original scopes;
- the cross-problem tool is not promoted beyond whatever authority its canonical/pending inventory currently records;
- the failed XM002 synthesis is preserved rather than silently rewritten;
- same-context expert review creates no independent-review authority.
