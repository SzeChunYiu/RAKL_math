# H4d1 deformation-lifting handoff

Base `main`: `539487f52126c21f6cb80c6127f4bdf67fae27c2`

Root control: issue #86

Authority: `STRICT_PRE_CANDIDATE_CONTEXT_AND_MEMORY / NO_MATHEMATICAL_CANDIDATE / NO_NOVELTY_CLAIM / ROOT_AUTHORITY_NONE`

## Selected atom

`H4d1`: in a smooth projective family, after fixing a rational cohomology class that is algebraic on one fiber and remains Hodge along a Hodge-locus branch `T`, identify the weakest checkable condition that forces actual algebraic-cycle witnesses to deform so that a suitable realization-space component dominates `T`.

This is a propagation atom under `H4`. It does not address the root initial-existence gap for an arbitrary Hodge class.

## Verified information gain

The active route separates four obligations that must not be collapsed:

1. **Hodge-locus compatibility** — the flat class remains of type `(p,p)`;
2. **infinitesimal/formal witness lifting** — a chosen algebraic realization can be lifted through the relevant deformation directions;
3. **algebraization** — compatible formal data come from actual algebraic-cycle data;
4. **domination/global continuation** — the chosen witness category maps onto the desired Hodge-locus branch after monodromy and coefficient issues are handled.

Cattani–Deligne–Kaplan controls the first layer. Semiregularity results give genuine local bridges in special settings. Formal cycle-class deformation results control another part under additional structure. None of these is silently promoted to the general rational Hodge conjecture.

## Frozen next discriminator

Define a typed witness-projection interface before proposing a sufficiency criterion:

- exact witness object `W_alpha`, including a rational-coefficient/signed-cycle encoding;
- class/realization map binding witnesses to the flat cohomology class;
- projection `pi: W_alpha -> T`;
- tangent map and exact Hodge-locus tangent target;
- obstruction object/map and semiregularity-style detector when available;
- all-order formal lifting obligation;
- algebraization obligation;
- monodromy/global-continuation obligation.

Calibrate it first on:

- the divisor/Picard case;
- a semiregular solved case;
- a non-semiregular hostile case;
- a rational signed-combination case;
- a formal-only/globalization stress case.

Only after those tests distinguish the actual obstruction should a concrete H4d1 sufficiency-criterion candidate be generated.

## Expert-cell synthesis

Six same-context roles—Hodge theory, algebraic cycles, deformation/obstruction theory, adversarial falsification, formal methods, and novelty/research value—agreed that an immediate theorem candidate would risk either scope drift, vacuity, or renaming semiregularity/formal-algebraization assumptions. These are role-separated same-context passes, not independent review.

## Memory result

The current strict persisted success/failure memories on `main` are P-vs-NP-specific. The H4d1 dual-memory query therefore records explicit `NO_RELEVANT_MATCH` rather than importing domain-specific experience without an applicability/DifferenceWitness.

## Next cycle

Build and validate the witness-projection interface against the five calibration cases. If the interface exposes a smaller residual—e.g. an obstruction kernel weaker than full semiregularity, a coefficient-representation defect, or a formal-to-global gap—promote that residual into the next child atom with a fresh context/memory/trace packet.
