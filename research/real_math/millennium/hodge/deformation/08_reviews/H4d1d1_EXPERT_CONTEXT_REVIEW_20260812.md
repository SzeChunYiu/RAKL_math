# H4d1d1 same-context expert cell — finite-cover trace/descent

Authority: **same-context role-separated review only**. Independent mathematical review credit: **0**.

## Frozen object

Atom `H4d1d1` asks whether monodromy/component switching in a reduced Hodge-locus branch truly requires one globally consistent algebraic witness, or whether an exact-class witness family on a finite cover could be enough after rational trace/pushforward. Root initial algebraicity is outside this child. Context packet: `sha256:4d9f15276fe9e9f02f67402355d64d06d8dcf138348b891178a65c80b0d5f793`.

## Roles and findings

**1. Hodge/VHS geometer.** Kept the exact rational/smooth-projective scope and the fixed flat class `alpha` explicit. Objection: a cycle upstairs is useless unless its Betti class is independently identified with the pullback of `alpha`; “algebraic on every sheet” is not enough.

**2. Algebraic-cycle/intersection specialist.** Verified the source-side operation: proper pushforward on cycles is degree-weighted (Stacks 42.12), finite locally free pull-push satisfies `f_* f^* = d` (Stacks 42.15.2), and proper pushforward descends through rational equivalence (Stacks 42.20). This supports a same-domain SEARCH route, not a Hodge theorem.

**3. Deformation/formal-geometry specialist.** Reconciled the route with PR #143. A finite-cover trace does not solve `H4d1c2` (all-order witness tower), `H4d1c3` (completion-to-branch descent), or `H4d1c4` (exact category/class identity). It only proposes a different interface for the monodromy/section part.

**4. Monodromy/degeneration specialist.** Required separate treatment of generic finite-flat open versus ramified/singular branch boundary. Stacks 62.4.5 gives a useful proper-pushforward/specialization compatibility control, but it does not automatically globalize a cycle over an arbitrary singular Hodge branch.

**5. Adversarial assurance/verifier.** Rejected two overclaims: (a) reusing PR #100's first-order tangent route as if it addressed all-order descent; (b) presenting the finite-cover trace seed as strict current-v3 discovery, because that candidate-like seed appeared before Gate-C artifacts were materialized in this cycle. The gate therefore changes the action: quarantine the seed, record SEARCH as route evidence, and open a fresh successor validation atom.

## Consensus

The source-side degree-normalized pushforward operation is structurally relevant and all source preconditions are explicitly mapped. The current cycle does **not** establish the target Hodge transfer. The next clean atom is `H4d1d1a-EXACT-CLASS-TRACE-VALIDATION`, with finite-cover existence, exact Betti class identity, and branch-boundary gluing kept separate.

## Disagreement retained

The algebraic-cycle lens regards the source trace formula as routine once a finite cover and exact pulled-back cycle are supplied. The Hodge/VHS and degeneration lenses stress that those hypotheses are almost the whole target difficulty. The assurance lens therefore assigns no theorem or root authority to the route-selection result.