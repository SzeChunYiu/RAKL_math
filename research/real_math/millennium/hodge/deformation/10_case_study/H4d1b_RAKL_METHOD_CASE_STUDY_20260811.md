# RAKL_METHOD_CASE_STUDY — H4d1b

## Cycle identity

- **Atom:** `H4d1b`
- **Problem signature:** rational Hodge Conjecture, geometric/deformation lane, already-algebraic witness propagation, first-order branch obstruction.
- **Framework read:** `SzeChunYiu/RAKL@bd1a2768f0f474ff44ffa25243241f94bfaf6466`.
- **Application base:** `SzeChunYiu/RAKL_math@71f5810f0a92532fd2a172b06bb080d699a4599f`.
- **Authority:** proposal/shadow method telemetry plus source-bound published-mathematics calibration; no root authority.

## Actual fibre

The fibre was intentionally narrow:
- parent route-pruning result `F-H4D1A-SAME-DETECTOR-BRANCH-NOGO`;
- parent detector-kernel failure `F-H4D1-DETECTOR-KERNEL-GAP`;
- no promoted success-derived Hodge ResearchTool;
- Kloosterman complete-intersection Hodge/flag-Hilbert source;
- Bruzzo-Montoya toric deformation-of-pairs positive control;
- Buchweitz-Flenner/Pridham semiregularity controls;
- Movasati witness/Hodge-locus mismatch hostile control.

The current RAKL framework pin in the application matches live framework main, so no stale-pin exception was used.

## Method/operator sequence

1. `gap_discovery`: reduce the H4d1a residual from detector faithfulness to direct geometric annihilation `delta_1|T=0`.
2. `memory`: retrieve both detector-kernel failures and explicitly reject a silent retry of same-detector injectivity.
3. `source_selection_reliability`: use current primary sources and preserve withdrawn/superseded source history.
4. `mathematical_context_translation`: translate Kloosterman's flag-Hilbert/Hodge-locus coordinates into the H4d1b branch-obstruction interface.
5. `equivalence_similarity`: identify tangent-image membership as the relevant first-order geometric equivalence, while recording all non-preserved higher-order/root structure.
6. `experiment_query_selection`: choose the cheapest source falsifier, namely a Hodge tangent with no incidence preimage.
7. `review`: run six same-context role-separated passes.
8. `saturation_stopping`: stop the first-order special-source question once the source identity gives an exact preimage, while reopening strict-weaker and higher-order axes.

## Prior experience that changed routing

The most important retrieved item was `F-H4D1A-SAME-DETECTOR-BRANCH-NOGO`. It prevents wasting another cycle searching for a nonzero branch obstruction image on which the same vanishing detector is injective. The route was changed from

`Hodge persistence -> detector kernel -> try restricted detector faithfulness`

to

`independently compute Hodge tangent -> explicitly solve incidence preimage -> direct first-order witness lift`.

This is a **qualitative memory-conditioned routing observation**. It is not causal proof that RAKL, rather than the underlying LLM, caused the change; a matched model-only/reset control was not frozen for this exact episode.

## Falsifier and verification

**Frozen cheapest falsifier:** find `deltaF` in the published Hodge tangent but outside the image of the incidence differential for the pair `(Y,Z)`.

In Kloosterman's complete-intersection source, the tangent identity gives
`T_Hodge=I_e` with `I=(P_i,Q_i)`. Hence each `deltaF` has a degree-compatible decomposition
`deltaF=sum_i(P_i deltaQ_i+deltaP_i Q_i)`, producing an explicit dual-number pair deformation.

**Outcome:** `PARTIAL_SUCCESS / SOURCE_BOUND_METHOD_CALIBRATION`.

## What worked

- The parent failure was reused as a scope warning rather than a global blacklist.
- Changing representation from detector observability to incidence reachability exposed a direct source calculation.
- The source calculation closes the first-order lift question for a meaningful special family without theorem invention.
- A second special toric source supports the broader pattern that Hodge and pair-deformation loci can coincide under strong geometry-specific hypotheses.

## What failed / remains blocked

- The source is semiregular, so it does not demonstrate the desired strict weakening.
- First-order image membership does not close higher Artin orders.
- No mapping transfers complete-intersection coordinates to arbitrary rational cycles.
- Initial algebraicity, algebraization, monodromy and global continuation remain outside the atom.

## Failure category

Primary residual class after the positive calibration:
`REPRESENTATION/TRANSFER-SCOPE + HIGHER-ORDER-GLUING`.

This is not a mathematical contradiction with the source result. It is a boundary on how far the method can be transferred.

## Saturation readout

The cycle adds a concrete source-bound relation/path and therefore reopens `RELATION`, `PATH` and `OBSTRUCTION`. No axis is declared absolutely saturated.

## Novelty class

`TRANSFER_NOVEL`, structural rank `0`.

The scientific mathematics used in the calibration is published. The retained novelty is the RAKL-side transfer/representation of that source result as a direct-incidence successor to the H4d1a failure, not a claim of a new theorem.

## Framework-improvement hypothesis

No new framework defect is strong enough to justify a RAKL issue from this episode alone. The existing v3 experience design behaved as intended by:
- preserving the prior verified impossibility,
- allowing a DifferenceWitness rather than globally banning witness lifting,
- forcing explicit source/target disanalogies,
- keeping same-context review non-independent.

A measurable future framework hypothesis is narrower: **failure-conditioned route-rotation should reduce repeated structurally identical attempts without increasing unsafe transfer**. That belongs in the Paper-5 matched attribution benchmark, not an application-side framework patch.

## Next research children

1. `H4d1c`: strict-weaker nonsemiregular first-order calibration.
2. `H4d1b-HO`: separately frozen higher-Artin stability of the source incidence lift.

The root remains `OPEN_NO_SOLUTION_CERTIFICATE`.
