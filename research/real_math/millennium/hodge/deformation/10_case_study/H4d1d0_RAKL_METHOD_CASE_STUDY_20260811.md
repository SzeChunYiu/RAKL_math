# RAKL_METHOD_CASE_STUDY — H4d1d0 proper-domination noncontraction audit

**Scope:** one proposal/shadow RAKL v3 Verified Discovery cycle in the rational Hodge deformation lane. The exact root remains open. No Hodge theorem, novelty claim, independent-review credit, reusable-tool promotion, or root authority is asserted.

## Atom / signature and actual fibre

`H4d1d0` audits the pending PR #119 successor `H4d1d-REDUCED-BRANCH-PROPER-WITNESS-DOMINATION` before spending another cycle searching for a universal geometric theorem. The exact local signature is: smooth projective complex family, one irreducible reduced algebraic Hodge branch `T`, a flat rational Hodge class `alpha`, one already algebraic central witness `z0`, and exact signed-rational cycle witnesses. Root initial algebraicity, full Hodge-scheme nilpotents, higher Artin order, monodromy, degeneration and global continuation are outside this local atom.

The frozen context hash is `sha256:2f7f9cc25c81d0cf923bccbe42c53e231d7ba55a4f97b328dc9c86610febf154`; the actual fibre snapshot hash is `sha256:1bcb683112fa9827e4ee9c504e016c8f91908ca4d22014fe29cb850e4dfeb30d`. The fibre actually consulted CDK for algebraicity of fixed-class Hodge loci; Dan and Kloosterman as special-family variational-Hodge/flag-Hilbert controls; Stacks Tag `0DPH` for properness of fixed-Hilbert-polynomial relative Hilbert spaces; canonical current-main `F-H4D1A-SAME-DETECTOR-BRANCH-NOGO`; pending/noncanonical `F-HM1a-OPEN-ROOT-LIKE-PIECE` from PR #42; and the pending H4d1c/H4d1d artifacts from PR #119. No promoted success-derived ResearchTool or motif matched.

## Expert cell

Six same-context roles were used and recorded before the discriminator was executed: VHS/Hodge-locus; algebraic-cycle moduli; deformation/obstruction; properness/countability; adversarial falsification; and formal/metrology plus gluing. They are role-separated internal passes, not independent mathematical reviews.

The VHS role required an algebraic reduced branch and kept the flat local-system class explicit. The cycle-moduli role required denominator, integer-coefficient and Hilbert-polynomial typing and refused to infer the cohomology class from Hilbert polynomial alone. The deformation role separated class-level component switching from persistence of `z0`. The properness/countability role selected the countable closed-cover discriminator. The adversarial role supplied the vertical-versus-dominant two-component control. The formal/gluing role kept pending PR memory nonauthoritative and separated local mathematics from monodromy/degeneration/globalization.

## Retrieved, selected, rejected and missed experience

Selected memory changed routing. `F-HM1a-OPEN-ROOT-LIKE-PIECE` supplied a DifferenceWitness for the pattern “valid bridge, but the critical coverage obligation is the original existence problem in another representation.” Its source is correspondence/filtration decomposition; the target is proper cycle-moduli images. The disanalogy is material, so it was used only for search priority. The cheapest repeat-failure test became: assume branch-wide pointwise algebraicity and ask whether countability plus properness already forces one witness component to dominate.

Canonical `F-H4D1A-SAME-DETECTOR-BRANCH-NOGO` reinforced rotation away from detector-based repairs. Pending H4d1c experience was retrieved as a scheme-level/component-switching warning but rejected as the active discriminator because `H4d1d0` was already frozen on the reduced branch. A literal Kloosterman general-domination transfer was rejected because its complete-intersection tangent/dimension identities are source-specific. Detector-only vanishing was rejected because it does not establish class-level coverage. `missed_prior_experience = CANNOT_MEASURE`: current v3 receipts do not certify a complete retrieval universe for this lane.

## Decision policy and v3 use

The inherited action preference was `SEARCH_FOR_GENERAL_PROPER_WITNESS_COMPONENT_DOMINATION_THEOREM`. After context, expert and dual-memory review it changed observably to `COUNTEREXAMPLE_FIRST_ROOTLIKE_EQUIVALENCE_AND_SELECTED_COMPONENT_AUDIT`. The causal contribution of RAKL alone is `CANNOT_MEASURE` because no matched `MODEL_ONLY / RAKL_RESET / RAKL_SHAM_MEMORY / RAKL_LEARNING` control was run.

Current RAKL v3 was used operationally through: exact atom/context freeze; a problem-fibre snapshot; selected/rejected retrieval authority; a DifferenceWitness for cross-lane memory; a seven-event hash-chained pre-candidate trace; a current `PreActionFibreReceipt` with `EpisodeOutcome`-compatible allowed branches; an immutable proposal/shadow TaskEpisode; separate diagnosis, failure, lesson and successor obstruction; seven-axis saturation accounting; and a metrology receipt. The application framework pin is older than current RAKL main, so current main was read directly and the drift is disclosed rather than treated as an authority downgrade.

## Falsifier / verification

The pre-action receipt `734888108cce58b020ce31249cda538cfc727d732ac319319c892d7719d6542f` froze the test before native source verification. Under an irreducible reduced finite-type branch over `C` and a countable family of proper exact-class witness spaces, test whether pointwise class-level algebraicity forces one proper component to have image all of `T`; then separately test whether that forces the component containing `z0` to dominate.

Verification used CDK `arXiv:alg-geom/9402009` for algebraicity of the fixed-class Hodge locus, Stacks Tag `0DPH` for proper relative fixed-Hilbert-polynomial parameter spaces, and Dan `arXiv:1404.7519` plus Kloosterman `arXiv:2104.14845` as special-family positive controls. The countable-union step was checked independently by the standard uncountable-field argument. The selected-component implication was falsified by the hostile algebraic control `W_dom=A1 -> A1` together with a vertical component `W_vert={0}` carrying the distinguished central point.

## Outcome and residual

Outcome: `PARTIAL_SUCCESS / REPRESENTATION_NONCONTRACTION / ROUTE_PRUNING`.

At class level, under the typed countable proper exact-witness parameterization,

`pointwise algebraicity on T` iff `some exact proper witness component dominates T`.

Thus the pending “some component dominates” target is a valid representation but not an independently smaller mathematical obligation. It can still be useful when special source geometry proves it, as Dan/Kloosterman illustrate; it should not receive contraction credit merely because it is phrased in moduli language.

The selected-central-witness statement is different: class-level pointwise coverage does not imply that the component through `z0` dominates. The successor is therefore `H4d1e-SELECTED-OR-BOUNDED-COMPONENT-TRANSPORT`, seeking source geometry that forces a component through `z0`, or a canonically controlled bounded replacement family, to dominate without assuming pointwise algebraicity.

Local mathematical failure and gluing failure are recorded separately. Locally, no general source-specific component selector was found. At the gluing interface, monodromy/component permutation, singular degeneration, continuation across branches, and globalization remain open. Root initial algebraicity is untouched because this lane starts with an algebraic central witness.

## Episode -> diagnosis -> obstruction/lesson

Episode `H4d1d0-C001-PROPER-COVER-EQUIVALENCE` records the executed actions and observations. Diagnosis `D-H4D1D0-DOMINATION-IS-CLASSLEVEL-REFORMULATION` attributes the failure to representation noncontraction. Failure experience `F-H4D1D0-SOME-COMPONENT-DOMINATION-NONCONTRACTING` is a scoped warning, not a blacklist. Lesson `L-H4D1D0-TYPE-SOME-VS-SELECTED-COMPONENT` says future moduli routes must distinguish some-component, selected-component and bounded/canonically replaceable-family claims. Successor obstruction is `H4d1e-SELECTED-OR-BOUNDED-COMPONENT-TRANSPORT`.

A separate tooling failure, `F-H4D1D0-MANUAL-HASH-COPY-MISMATCH`, occurred when five per-record hash strings were manually copied into the first result-packet serialization. The original Git commit is preserved as negative history; `H4d1d0_HASH_CORRECTION_20260811.json` records exact replacements and the regression test reconstructs them. This is a tooling/serialization failure, not a mathematical failure, and no repeated-process link is asserted because no prior occurrence with the same signature was found in this cycle.

## Saturation and novelty

Defensible RAKL novelty class: `representation`; structural rank `0`. No new theorem or primitive operator was invented. Retained semantic novelty for this proposal/shadow cycle is conservative: `KNOWLEDGE=0`, `OPERATOR=0`, `EXPERIENCE_PATTERN=1`, `OBSTRUCTION=1`, `RELATION=1`, `PATH=1`, `META_METHOD=0`. The relation axis records the class-level equivalence; the obstruction axis records the selected/bounded-component transport gap; the path axis records removal of the noncontracting general-domination search; the experience-pattern axis records a second target-specific manifestation of the root-like-bridge relocation pattern, while the noncanonical HM1a source remains search guidance only.

Reopened axes: `EXPERIENCE_PATTERN`, `OBSTRUCTION`, `RELATION`, `PATH`. Flattened this cycle only: `KNOWLEDGE`, `OPERATOR`, `META_METHOD`. These zeros are not absolute saturation claims.

## Framework-improvement hypothesis

Proposal only: for moduli/incidence research, `ProblemFibre` or a mathematical context extension should type `witness_component_scope` as at least `SOME_COMPONENT`, `SELECTED_COMPONENT`, or `BOUNDED_REPLACEMENT_FAMILY`, and a route-contraction audit should ask whether `SOME_COMPONENT` is logically equivalent to the original pointwise existence target under proper/countable parameterization. This would have rejected the noncontracting formulation earlier while preserving special-family incidence methods as useful proof representations. No framework change or tool promotion is authorized by this single case.
