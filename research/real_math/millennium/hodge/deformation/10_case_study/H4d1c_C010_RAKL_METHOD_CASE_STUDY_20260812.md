# RAKL_METHOD_CASE_STUDY — H4d1c-C010

## Subject and authority

- RAKL semantic source of truth at cycle freeze: `SzeChunYiu/RAKL@d594e6864f49ecf6dac394173082fbf0174b422e`.
- Method: `3.0.0`; package `0.1.0`; constitution epoch `v3-authority-hardening-20260811`.
- RAKL_math causal base: `6016dd6a87d87b18d8f5498e2537b043a8468c04`, tree `0f00e15a3848b2fb986cf07702f6357872ed0bc4`.
- Application pin is stale at `5dc0627f039e8f3e1cdcb7e05cd7603860afc554`; current semantic RAKL was read directly, so the stale pin cannot weaken current rules. Protected runtime admission against the current semantic subject is therefore not claimed.
- Root issue #6 remains `OPEN_NO_SOLUTION_CERTIFICATE`; independent mathematical review credit `0/3`.

## Atom / signature / actual fibre

Active atom: `H4d1c-C010`, child of `O-H4D1C-RELATIVE-HILBERT-SMOOTHNESS-OR-COUPLED-DOMINANCE`.

Exact consumer: for a chosen local marked reduced irreducible Hodge branch `H -> S` and an exact signed-`Q` witness incidence `W -> S`, study

`W_H := W x_S H -> H`.

The actual fibre consulted current same-lane C007/C006/C001 negative history, current open shadow C009/PR #350 and C008/PR #340, Stacks smooth/base-change facts, and Kloosterman/Nishinou positive special-family controls. C004 was retrieved but rejected as a different proxy-level issue. Cross-Millennium episodes were rejected because their consumer morphisms and base-change semantics differ. Absolute missed-memory recall is `CANNOT_MEASURE` because the connector did not provide a content-bound exhaustive current-work universe.

## Methods / operators / motifs tried

Canonical `method_specs.py` surfaces used: `decomposition`, `routing`, `search_query_generation`, `source_selection_reliability`, `claim_extraction`, `mathematical_context_translation`, `equivalence_similarity`, `contextual_theory_gluing`, `gap_discovery`, `experiment_query_selection`, `memory`, `review`, `saturation_stopping`, and `prompting_context_policy`.

The route policy was same-domain `SEARCH`, counterexample first. The main operator was **consumer restriction by Cartesian base change**: test the strong ambient condition only after typing the downstream Hodge-branch consumer. No learned motif or promoted tool was invoked. `JUMP` and `GLUE` were unnecessary for the discriminator; `LIFT` was blocked because SEARCH had an exact cheap test.

## Expert cell

Seven same-context roles were used: VHS/Hodge-locus, Hilbert/embedded deformation, cycle/coefficient, formal deformation, special-family transfer, adversarial falsification, and RAKL/metrology. The key disagreement was useful: the Hilbert role retained ambient relative smoothness as a strong sufficient construction route, while the adversarial role rejected treating it as necessary. Consensus was to test necessity on the base-changed consumer and preserve signed-`Q`, properness and gluing guards.

## Falsifier / verification

Hostile world:

`S=Spec C[x,y]`, `H=W=V(x)=Spec C[y]`, with `W->S` the closed immersion.

Verification:

1. `A=C[x,y] -> B=A/(x)` is not flat: tensoring the injective map `A --x--> A` with `B` produces the zero map `B -> B`, which is not injective. Smooth implies flat, so the ambient map is not smooth.
2. `B tensor_A B ~= B`; hence `W x_S H -> H` is the identity, therefore smooth and full-image.
3. `W->S` is a closed immersion, hence proper; properness survives the base change. The discriminator therefore does not exploit a missing image-closure guard.

Outcome: `PARTIAL_SUCCESS / VERIFIED_SCOPED_NONIMPLICATION`. Ambient relative smoothness is sufficient but **not necessary** for branch-restricted smooth/full geometry.

## Episode -> diagnosis -> obstruction/lesson

- Episode: `H4d1c-C010-BRANCH-BASECHANGE-SMOOTHNESS-NOGO` (`PROPOSAL_SHADOW_STORED`).
- Diagnosis: `D-H4D1C-AMBIENT-SMOOTHNESS-OVERSTRONG-FOR-BRANCH-CONSUMER`.
- Failure observation: `F-H4D1C-AMBIENT-SMOOTHNESS-NOT-NECESSARY-FOR-BRANCH-COVERAGE`.
- Reusable obstruction proposal: `O-H4D1C-BRANCH-RELATIVE-SMOOTHNESS-OR-DIRECT-DOMINANCE`.
- Candidate lesson: `L-H4D1C-CONSUME-AFTER-HODGE-BASECHANGE-V1`.

These remain distinct. The episode is evidence; the diagnosis is the supported cause in this hostile world; the obstruction/lesson are proposal-only abstractions requiring future source-specific transfer before reuse authority.

## Local mathematics versus gluing

The C010 failure is `LOCAL_MATHEMATICAL_REPRESENTATION_AND_ROUTING`: a condition was imposed on a larger ambient morphism than the actual branch consumer. It is **not** a local-to-global gluing failure.

Separate gluing obligations remain: proper/closed image, irreducible-component binding, monodromy, singular degeneration/specialization, completion-to-neighborhood descent and global continuation. Exact signed-`Q` class identity, source-family completeness, higher-order/formal lifting where needed, algebraization/effectivity and root initial algebraicity remain separate mathematical/root obligations.

## Saturation / novelty

Protected retained semantic novelty is conservatively zero on all seven axes:

- `KNOWLEDGE=0`
- `OPERATOR=0`
- `EXPERIENCE_PATTERN=0`
- `OBSTRUCTION=0`
- `RELATION=0`
- `PATH=0`
- `META_METHOD=0`

Reason: this cycle stores only proposal/shadow research state; there is no canonical episode admission, protected lesson/tool promotion, or independent semantic-novelty audit. Raw files/commits/PRs receive zero learning credit.

Axis state changes are still informative routing telemetry: `RELATION` and `PATH` reopen around branch-relative consumer geometry; `META_METHOD` reopens because a chronology process failure repeated. The path `AMBIENT_RELATIVE_SMOOTHNESS_OVER_S_AS_NECESSARY_HODGE_BRANCH_TARGET` is flattened. No claim of absolute saturation is made.

Mathematical novelty class: `RAKL_TRIVIAL / EXPLICIT_COUNTEREXAMPLE / STRUCTURAL_RANK_0`; there is no literature-novelty claim.

## What worked / what failed

What worked: v3 consumer/QoI typing plus same-domain negative memory prevented another tangent-proxy pass and redirected the target from the ambient relative Hilbert map to the actual Hodge-branch base change. The C007 consumer-image lesson was successfully reused without importing theorem authority from open shadow work. The elementary hostile world decisively separated sufficient from necessary conditions.

What failed: strict discovery chronology. The hostile base-change idea appeared in private automation scratch before the durable fibre was materialized. This repeats `F-H4D1C-C008-PREFREEZE-HYPOTHESIS-EXPOSURE` and `F-H4D1C-C009-PREFREEZE-HYPOTHESIS-EXPOSURE`; therefore no prospective RAKL discovery/novelty credit is claimed.

## Framework case-study result

Current v3 authority, consumer typing, shadow-admission and saturation semantics behaved as intended. The missing integration feature is a host-enforced pre-scratch freeze. Repetition across C008/C009/C010 warranted isolated framework issue `SzeChunYiu/RAKL#464`, proposing a minimal pre-scratch `PreActionFibreReceipt` hook for autonomous runners. The proposal must not mint authority or weaken existing gates; it requires a prospective matched process discriminator.
