# RAKL_METHOD_CASE_STUDY — H4d1c C006 ramification proxy audit

## Method and active atom

Atom `H4d1c-f` tested whether surjectivity of the incidence differential at one selected witness is a necessary proxy for the root-aligned reduced image-germ consumer. The frozen fibre bound the exact rational/smooth-projective root, C004 projection history, open-shadow C005 source-image route, coefficient/category guard, ramification boundary, and downstream formal/global exclusions.

Current RAKL v3 was used operationally: `decomposition`, `routing`, `memory`, `equivalence_similarity`, `contradiction_diagnosis`, `gap_discovery`, `experiment_query_selection`, `review`, `saturation_stopping`, and `software_architecture_execution`, plus the mathematical-research Gate-C obstruction–transformation review. The selected shortcut mode was `SEARCH`, reusing the same-domain verified C004 transformation `consumer_projection_alignment`; cross-domain proposal-only material was rejected as unnecessary and nonauthoritative.

## Expert-cell findings

The VHS lead required one locally marked reduced Hodge target and rejected monodromy/global extrapolation. The Hilbert/Chow lead insisted that actual image, not source tangent size, is the QoI. The singularity/ramification lead proposed the cheapest boundary test at a ramified point. The coefficient lead kept coupled signed-`Q` semantics separate from componentwise persistence. The formal/algebraization lead kept reduced local image distinct from all-Artin/effectivity/descent. The adversarial RAKL lead enforced proposal/shadow storage and `0/3` independent-review credit.

## Falsifier and verification

The falsifier asked for a morphism with full reduced image but rank-deficient selected-point differential. `pi(t)=t^2` supplies a stronger example: the scheme-theoretic image is all of `A^1` because `Q[u] -> Q[t]` is injective, while the cotangent map at zero sends `u` to `t^2=0 mod (t^2)`, so `d pi_0=0`.

## Outcome and residual

Outcome: `PARTIAL_SUCCESS / VERIFIED_SCOPED_PROXY_NOGO`. The inference that full image forces selected-point differential surjectivity is false. New residual: `O-H4D1C-GENERIC-NONVERTICALITY-OR-DIRECT-IMAGE-DIMENSION`. The path “selected-point differential surjectivity as a necessary branch-coverage condition” is flattened; `OBSTRUCTION`, `RELATION`, and `PATH` reopen around direct image geometry or generic/suitably chosen rank control.

Failure category: local mathematical + representation + verification-proxy. It is **not** a local-to-global/gluing failure. Downstream lifting, algebraization, completion descent, monodromy, singular degeneration and globalization remain open.

## Experience routing and lessons

Selected prior experience: `MATH-HODGE-C004-PROJECTION-KERNEL-NONIMPLICATION`, `FM-HODGE-SOURCE-ENLARGEMENT-NOT-PROJECTION-GAIN`, and open-shadow `H4d1c-C005-CLOSED-TOPDIM-SOURCE-IMAGE-CERTIFICATE`. Rejected: C001 coefficient cancellation as nondispositive for this proxy, H4d1a same-detector no-go as a different discriminator, and the RAKL cross-domain counterexample-first seed as proposal-only and unnecessary. Missed prior experience remains `CANNOT_MEASURE` because no complete current-work coverage receipt was materialized.

Episode, diagnosis, failure, obstruction and candidate lesson are separate records. The candidate lesson is representation-class: ramification decouples a selected-point differential from the actual image germ. Experience guides future routing only; no canonical lesson/tool promotion is requested.

## What worked / failed / framework hypothesis

What worked was consumer-first reformulation plus the cheapest ramification boundary case. What failed was a verification proxy: a first-order rank test was too strong as a *necessary* condition for nonlinear image coverage. Current v3 helped by forcing exact consumer binding, same-domain transformation reuse, disanalogy accounting, and a pre-candidate obstruction–transformation review.

A framework-improvement hypothesis is warranted but not yet a defect claim: structural mapping/consumer specifications could explicitly distinguish `POINTWISE_DIFFERENTIAL`, `GENERIC_DIFFERENTIAL`, and `ACTUAL_IMAGE_GERM` evidence so nonlinear ramification cannot be silently collapsed into a single “reachability” coordinate. This should be benchmarked prospectively before any framework promotion.

A matching ramification hypothesis existed in scratch before the public current-v3 freeze, so this run does **not** claim strict causal RAKL discovery attribution. Current v3 governed validation, scope, routing evidence and preservation of negative history.
