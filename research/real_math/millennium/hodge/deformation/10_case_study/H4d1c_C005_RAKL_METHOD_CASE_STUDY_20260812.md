# RAKL_METHOD_CASE_STUDY — H4d1c C005 source-image completeness

**Authority:** proposal/shadow case-study evidence only. No mathematical promotion, novelty certification, independent-review credit, framework promotion, or Hodge root authority.

## Active atom and signature

`H4d1c-e` asks for the smallest exact certificate that converts a genuinely varying algebraic-witness source into local coverage of one Hodge-locus component after C004 proved that source-tangent enlargement can be vertical. The root scope remains the rational Hodge conjecture for smooth projective complex varieties. This atom is conditional propagation from an already algebraic central signed rational witness; root initial algebraicity is untouched.

Frozen fibre: `sha256:96633bf85520c261d7651566ef7980cb49cfbb2588c3c7c94cb53123f4cbc9f2`. Frozen context: `sha256:7f8f5ddb80081f3b0330db77e3dc84668add79bf757b9bf1f48924409f7e091f`.

## Expert cell and delegated findings

The **VHS/Hodge-locus lead** required a single reduced irreducible local Hodge component and a local marking of the flat rational class; without those bindings, dimension comparisons can silently switch components or classes.

The **Hilbert/Chow deformation lead** replaced source-tangent size by the reduced image germ itself. A closed source image is the decisive geometric object; proper/projective incidence is one route to closedness, but does not by itself prove dimension or class identity.

The **algebraic-cycle coefficient lead** reused C001 negatively: total rational Hodge persistence does not imply componentwise persistence. Any tuple source must therefore carry the exact signed total-class equation, not inherit it from the Hodge condition.

The **deformation/formal lead** separated reduced set-theoretic coverage from nonreduced/scheme-theoretic equality, higher-Artin lifting, compatible formal witnesses and algebraization.

The **variational-analogue lead** selected Kloosterman's complete-intersection flag-incidence theorem as the closest positive source-family shape and retained Buchweitz--Flenner semiregularity only as a disanalogous positive mechanism, not a general signed-rational transfer.

The **adversarial verification/RAKL lead** deleted each certificate hypothesis in turn: nonclosed full-dimensional images, one component of a reducible target, wrong-class domination, and reduced-equality-with-formal-defect all show distinct failure modes. This is same-context role separation and receives zero independent-review credit.

## RAKL v3 process surfaces actually used

From current `method_specs.py`, the cycle used `decomposition` to isolate H4d1c-e, `routing` to rotate away from the flattened source-tangent family, `search_query_generation` and `source_selection_reliability` for bounded primary-source calibration, `claim_extraction` for exact Kloosterman/CDK/Buchweitz--Flenner scope, `mathematical_context_translation` and `equivalence_similarity` for analogue/disanalogy typing, `gap_discovery` for the consumer-image cut, `experiment_query_selection` for the cheapest assumption-deletion falsifiers, `synthesis` for the scoped sufficient certificate, `memory` for C001/C003/C004 reuse, `review` for the same-context hostile cell, and `saturation_stopping` to keep the root and downstream fibres open.

No authority-promotion surface was invoked. No cross-Millennium transfer was needed.

## Methods/operators/motifs tried

The successful sequence was consumer-first image reformulation -> irreducible-germ dimension lemma -> hypothesis deletion -> positive solved-family calibration -> coefficient/formal/global boundary audit. The key change relative to C004 was to stop asking whether new witness tangent directions survive `d pi` one direction at a time and instead ask whether the **closed reduced image germ** already has full dimension in one irreducible target component.

The direct lemma succeeded: if `L` is a closed reduced subgerm of a reduced irreducible germ `H` and `dim L = dim H`, then `L=H`. Exact class binding turns that geometric equality into conditional local algebraic-witness coverage for `alpha`.

What failed under hostile deletion was equally informative. Full dimension without closedness is not coverage; full dimension in a reducible target may cover only one component; domination by witnesses of a different class is irrelevant; reduced image equality does not supply formal or scheme-theoretic lifting.

## Retrieved, selected, rejected and missed experience

Selected prior experience that changed routing:

- `H4d1c-C004-TOTAL-WITNESS-PROJECTION-FAITHFULNESS`: directly prohibited treating source tangent enlargement as branch-coverage evidence.
- `O-H4D1C-TOTAL-WITNESS-NONVERTICAL-HODGE-BRANCH-COVERAGE`: supplied the active residual.
- `L-H4D1C-TANGENT-ENLARGEMENT-NEEDS-PROJECTION-WITNESS-V1`: forced consumer-image typing.
- `H4d1c-C001-RATIONAL-COEFFICIENT-CANCELLATION`: blocked componentwise persistence as a hidden coefficient shortcut.
- `H4d1c-C003-TOTAL-WITNESS-CONIC-TANGENT-ESCAPE`: retained only as evidence that total-witness representations can be richer internally, not as base-coverage evidence.

Retrieved but rejected:

- H4d1a same-detector no-go: valid negative history but a different consumer (detector injectivity).
- Buchweitz--Flenner as a general signed-rational transfer: rejected because semiregularity/coherent-module hypotheses do not supply a universal coupled signed-Q-cycle incidence.
- Cross-Millennium gluing analogies: rejected because exact same-context Hodge memory already exposed the decisive cut, so applicability/DifferenceWitness work would add cost without decision value.

Missed prior experience: none observed in the bounded current-main/PR/literature check. Repository search found no C005/source-image-certificate duplicate; that bounded negative search is not a literature-completeness claim.

## Episode -> diagnosis -> obstruction/lesson

**Episode:** `H4d1c-C005-CLOSED-TOPDIM-SOURCE-IMAGE-CERTIFICATE` records the actual fibre, retrievals, route, falsifiers and outcome.

**Diagnosis:** `D-H4D1C-IMAGE-COMPLETENESS-IS-CLOSED-TOPDIM-PROBLEM` states why the consumer-image formulation works and which assumptions are logically load-bearing.

**Obstruction:** `O-H4D1C-SAME-CLASS-CLOSED-TOPDIM-SOURCE-IMAGE` is the remaining mathematical task: construct a genuinely varying signed-Q witness source with exact class binding, closed image and top-dimensional image in each target component.

**Candidate lesson:** `L-H4D1C-CLOSED-TOPDIM-IMAGE-COVERS-IRREDUCIBLE-BRANCH-V1` is math-first and reusable only as proposal/shadow search guidance until protected admission. The lesson is not the existence of files, commits or green tests.

## Verification and falsification

Verification was direct, not numerical. The proof uses the strict dimension drop for proper closed subsets of a reduced irreducible analytic/algebraic germ. Hostile boundary checks separately remove closedness, irreducibility/component binding, exact-class binding and reduced-scope discipline. Primary-source checks calibrate, but do not prove, transfer: CDK makes the Hodge locus a geometric target; Kloosterman supplies a special-family flag-Hilbert image-completeness pattern; Buchweitz--Flenner supplies a semiregularity-shaped positive analogue with explicit object/coefficient disanalogy.

No computation is used as proof. Same-context review is not independent review.

## Outcome, residual and failure taxonomy

Outcome: `PARTIAL_SUCCESS / VERIFIED_SCOPED_SUFFICIENT_CERTIFICATE`.

The scoped lemma has no local mathematical failure under its hypotheses. The residual is source-specific geometry, not theorem closure. Boundary failures are representation/class-binding and local-to-base image-gluing failures. Global local-to-global continuation was **not attempted** and remains separately open. Higher Artin lifting, algebraization, monodromy beyond the local marking, singular degeneration/specialization, global continuation/domination and root initial algebraicity remain open.

The solved subproblem is conservatively classified `COMPOSITIONAL`, structural rank 0: it composes standard irreducible-dimension theory with the C004 consumer-image interface and introduces no new primitive operator or ontology.

## Seven-axis saturation audit

Retained semantic novelty is conservatively `KNOWLEDGE=0`, `OPERATOR=0`, `EXPERIENCE_PATTERN=0`, `OBSTRUCTION=0`, `RELATION=1`, `PATH=1`, `META_METHOD=0`. `RELATION` reopens because the exact relation “closed + top-dimensional + same-class image inside one irreducible branch => reduced branch coverage” is now explicit in this Hodge workstate. `PATH` reopens because source-family completeness can now be attacked through actual image dimension rather than source tangent enlargement. The prior path “source tangent enlargement as branch-coverage evidence” remains flattened.

These counts are proposal/shadow metrology, not canonical learning or novelty authority. Raw repository growth receives zero learning credit.

## What v3 helped and what remains missing

V3 helped through the proposal/shadow TaskEpisode boundary, explicit QoI/consumer typing in `method_specs.py`, immutable negative history, scoped authority, same-context-vs-independent review separation, and seven-axis saturation semantics. The decisive benefit in this cycle was consumer typing: it made `pi(W)` rather than `T_w W` the object that must be faithful to the root-relevant question.

No framework defect requiring a RAKL issue was found. A concrete framework-improvement hypothesis is nevertheless warranted: add a first-class **image-certificate checklist** under `mathematical_context_translation`/`equivalence_similarity` for routes whose consumer is an image or realization map, with fields for class/identity binding, closedness/properness, component binding, dimension/dominance, reduced-vs-formal scope, and monodromy. This would make the C004->C005 repair cheaper to rediscover while remaining proposal-only and non-promotional.

## Next action

Freeze one source-specific varying-family atom. The preferred discriminator is a nontrivial relative Hilbert/Chow/flag source carrying a coupled signed rational class equation; prove closedness/properness and top-dimensional image componentwise. If that succeeds, freeze higher-Artin lifting separately before any algebraization or global continuation claim.
