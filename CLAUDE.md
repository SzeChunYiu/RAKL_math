# RAKL Math assistant entrypoint

Follow `AGENTS.md`. This is the application repository; reusable schemas,
runtime behavior and assurance policy remain authoritative in the exact RAKL
framework revision pinned by `config/rakl-framework-pin.json`.

Do not claim that an open problem is solved without the pinned framework's full
root-solution certificate and genuinely independent review. Preserve nulls,
refutations, failed attempts and supersession lineage.

